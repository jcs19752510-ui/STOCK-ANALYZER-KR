# Public API 서버 기본 URL(REQ-002 종목 상세 화면 등 서버 컴포넌트가 이 값으로
# fetch한다). 로컬에서 `uvicorn services.public_api.main:app --port 8000`으로
# 백엔드를 띄운 경우 기본값 그대로 사용 가능.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
