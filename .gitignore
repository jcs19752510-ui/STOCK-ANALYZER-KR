__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
*.egg-info/
.venv/
venv/
.env

# 하네스 규칙 K(중단-안전 정리, 13단계 업그레이드로 J→K 재정렬됨 — DEC-021 참고)
# 검증/테스트 단계(06/07/08/09/10/13)가 만드는 venv/임시 DB/임시 설정 등 일회성 아티팩트 전용 디렉터리.
.harness-tmp/
.venv_*/
*.sqlite3
db.sqlite3

# CI 헤드리스 실행 로그 (automation/run-harness-agent.sh 산출물)
docs/harness/_ci-logs/

# 일일 배치 스케줄러(scripts/run_daily_batch.ps1) 실행 로그 — 로컬 전용
logs/

# 화면 캡처(오류/설정 화면 등)에 인증키·서비스키 등 민감정보가 찍히는 경우가 있어
# 실수로 커밋되지 않도록 새 파일은 기본적으로 git 추적에서 제외한다.
99.화면오류/*.png

# frontend (Next.js)
frontend/node_modules/
frontend/.next/
frontend/out/
frontend/next-env.d.ts
frontend/*.tsbuildinfo
frontend/.env.local
