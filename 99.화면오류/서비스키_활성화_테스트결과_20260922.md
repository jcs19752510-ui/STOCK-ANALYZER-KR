# 공공데이터포털 서비스키 활성화 테스트 결과서

- **테스트 일시**: 2026-09-22 12:19 ~ 12:25 KST
- **테스터**: Claude (AI 어시스턴트, 사용자 요청에 따른 실측 테스트)
- **대상 API**: 공공데이터포털 `금융위원회_주식시세정보`(GetStockSecuritiesInfoService, 오퍼레이션 `getStockPriceInfo`), 데이터셋 ID 15094808
- **테스트 목적**: 2026-09-22(오늘)로부터 약 13.5시간 전(2026-09-21 22:53 KST)에 사용자가 발급받아 `.env`의 `GOV_DATA_PORTAL_SERVICE_KEY`에 설정한 실 서비스키가 활성화되어 정상 응답을 반환하는지 재검증
- **선행 테스트**: 2026-09-21 22:53 KST 1차 테스트에서 `SERVICE_KEY_IS_NOT_REGISTERED_ERROR`(코드 30) 확인 — 당시엔 "발급 직후 전파 지연"으로 잠정 판단했음

## 1. 테스트 환경

| 항목 | 값 |
|---|---|
| 서비스키 길이 | 64자 (URL 특수문자 `+`/`/`/`=` 없음 — 인코딩 이슈 배제) |
| 서비스키 출처 | data.go.kr 마이페이지 "서비스 정보" 화면(스크린샷으로 사용자 확인), `.env`에 설정된 값과 문자 단위로 완전히 일치 확인 |
| 호출 방식 | curl 직접 호출(코드 경유 없이 실측 — 코드 버그와 API/계정 문제를 분리하기 위함) |
| 기준 엔드포인트 | `https://apis.data.go.kr/1160100/service/GetStockSecuritiesInfoService/getStockPriceInfo` (코드 기본값, 2026-09-21 세션에서 웹상 다수의 독립적인 현재 운영 중인 구현 사례와 대조해 경로 자체는 정상으로 판단 완료) |

## 2. 테스트 케이스 및 결과

| # | 테스트 조건 | 목적 | 결과 |
|---|---|---|---|
| TC-1 | `resultType=json`, `basDt=20240102`(과거 확정 거래일), `likeSrtnCd=005930` | 기본 정상 케이스 재확인 | **FAIL** — `SERVICE_KEY_IS_NOT_REGISTERED_ERROR`(code=30) |
| TC-2 | TC-1과 동일 조건, `resultType=xml`만 변경 | JSON 파서 문제인지 게이트웨이 자체 문제인지 분리 | **FAIL** — 동일 오류(XML 봉투로) — resultType 무관 확인 |
| TC-3 | `basDt=20250918`(더 최근 날짜)로 변경 | 특정 날짜 데이터 미배포 문제와 키 문제를 분리 | **FAIL** — 동일 오류 — 날짜 무관 확인 |

**3건 전부 동일하게 `errMsg=SERVICE_KEY_IS_NOT_REGISTERED_ERROR, returnReasonCode=30`** — resultType/날짜와 무관하게 **키 자체가 이 오퍼레이션에서 거부**되고 있음을 확인.

## 3. 추가 확인 사항 (배제한 원인들)

- ❌ URL 인코딩 문제 — 키에 특수문자 없음, 배제
- ❌ 요청 경로(엔드포인트) 오류 — 2026-09-21 세션에서 이미 다수의 현재 작동 중인 실사용 사례와 대조 완료, 이 경로 자체가 틀렸을 가능성은 낮음
- ❌ `resultType`/날짜 파라미터 문제 — TC-2/TC-3으로 배제
- ⚠️ **미확정**: 발급 직후 전파 지연(propagation delay) — 13.5시간 경과는 일반적으로 보고되는 지연시간(수십 분~수 시간) 범위를 넘어서고 있어, 이 가설의 신뢰도가 낮아지고 있음
- ⚠️ **미확정**: data.go.kr 마이페이지 상 실제 "활용신청 현황"이 승인 완료 상태인지 — **이 부분은 사용자 계정 로그인이 필요해 제가 직접 확인할 수 없음**

## 4. 결론 및 원인 판단

**아직 원인 미확정(Inconclusive).** 코드/설정 쪽 문제는 이번 테스트로 대부분 배제했고, 남은 가능성은 data.go.kr 계정/승인 상태 쪽입니다. 13.5시간이 지나도 동일 오류가 계속되는 건 "단순 전파 지연"만으로 설명하기엔 시간이 길어, **계정 쪽 확인이 필요한 단계**로 판단합니다.

## 5. 권장 다음 조치 (사용자 확인 필요 — 제가 대신 로그인할 수 없는 영역)

1. data.go.kr 로그인 후 **마이페이지 > 활용신청 현황**에서 이 API(`금융위원회_주식시세정보`)의 승인 상태가 정확히 "승인"으로 표시되는지 확인
2. 상세보기에서 발급된 "일반 인증키"가 `.env`에 넣은 값과 완전히 동일한지 재대조(혹시 재발급되어 값이 바뀌었을 가능성)
3. 위 두 가지가 모두 정상인데도 계속 실패하면, data.go.kr 문제해결지원센터(1:1 문의 또는 `opendata_help@nia.or.kr`)에 "자동승인인데 SERVICE_KEY_IS_NOT_REGISTERED_ERROR가 지속된다"고 문의 필요 — 이는 제가 대신할 수 없는 사용자 계정 기반 조치입니다

## 6. 재검증 계획

위 조치 후 사용자가 알려주시면, 제가 즉시 동일한 3개 테스트 케이스(TC-1~3)를 재실행해 활성화 여부를 확인하겠습니다.
