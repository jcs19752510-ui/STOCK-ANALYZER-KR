# 내부 검증 로그 (Internal Verification Log)

> 대상: `docs/harness/04-ux-design.md`
> 최소 2회 검증 필수. 2회 하한선이지 상한선이 아니다 — 2차에서도 결함이 나와 실제로 3차까지 수행했다(v2 시점). v3 개정에서도 최소 2회 검증을 다시 수행한다.

## 대상 산출물
- 파일: `docs/harness/04-ux-design.md`
- 작성 에이전트: 04-ux-designer
- 버전: v0 (초안) → v1 → v2(최종, PASS) → **v3(최종, PASS — 본 로그의 신규 구간)**

---

## v2까지의 검증 이력 (요약, 상세는 본 로그 하단 "v2 검증 이력 원문" 참조)

- 1차 검증(작성자 관점): 스크리닝 조건 폼의 API 미지원 파라미터 제거, 업종 뱃지 제거, `market` 필드 의미 중복 발견 → 가정/확인필요 항목(Q1~Q7 중 Q1·Q3·Q6) 분리, `matched_metrics` 정렬 전용 지표 미보장 이슈(Q2) 반영.
- 2차 검증(프론트엔드 개발자 관점): CLS 방지, 폼 검증 타이밍, 429 처리 통일, 바텀시트 접근성, 색 대비 수치화, 빈상태/에러상태 구분, 반응형 표 마크업 처리, `aria-live` 명시.
- 3차 검증(2차 결함 재확인): 결함 0건, v2 최종 PASS.
- v2 최종 판정 시 남긴 핸드오프 노트: "§7-1 Q1~Q3은 화면 의미 자체를 바꿀 수 있는 항목이므로 5단계 착수 전 3단계 재확인을 권고" — 이 권고에 따라 3단계(`03-system-design.md`)가 v3에서 Q1~Q7을 전량 확정 회신했다.

---

## v3 개정 배경

`03-system-design.md`가 v3로 개정되어 `04-ux-design.md` v2 §7-1의 Q1~Q7이 아키텍트 직접 판단으로 전량 확정됐다(§0-1, §3-1-1, §3-2, §4-2, §8-3, `decisions.md` DEC-011~016). 이에 따라 `04-ux-design.md`를 v3로 개정해 확정 내용을 반영하고, 문서 전체의 "가정"/"미확정"/"옵셔널·폴백"/"잠정 표기" 표시를 제거했다. 아래는 이 v3 개정에 대한 내부 검증이다(최소 2회).

## 1차 검증 (작성자 관점 — "모든 핵심 플로우가 03-system-design.md v3의 확정 API/데이터와 정합하는가")

- 검증자(역할): 04-ux-designer 본인(작성자 관점 자가 재검토)
- 일시: 2026-09-14
- 체크리스트
  - [x] Q1~Q7 각 항목이 03-system-design.md v3의 정확한 확정 문구(필드명·enum 값·계산 방향성)와 1:1로 일치하는가
  - [x] 필드명이 실제 API 스펙과 정확히 일치하는가(`total_trading_value_krw`, `top_sectors_by_value[].trading_value_krw`, `per_percentile`/`pbr_percentile`/`market_cap_percentile`, `market`(KOSPI\|KOSDAQ\|ALL) 등) — 구 필드명(`total_trading_value`, `trading_value`, `per`/`pbr`/`market_cap` 원시 표기)이 잔존하지 않는가
  - [x] `GET /market-summary` 단일 호출 패턴이 03-system-design.md v3 §4-2 응답 스키마(`data` 최상위 = ALL 집계, `data.by_market[]` = 시장별 세부)와 정확히 일치하는가
  - [x] `GET /screen`의 `market` 파라미터, `matched_metrics` "필터∪sort_by" 포함 규칙이 정확히 반영됐는가
  - [x] `return_rank_pct`/`per_percentile`/`pbr_percentile`/`market_cap_percentile`의 방향성("값이 작을수록 상위") 및 percentile이 시장 필터와 무관하게 코스피+코스닥 전체 기준이라는 사실(03-system-design §3-2 "percentile 계산 범위 확정")이 화면 카피/고지 문구에 반영됐는가
  - [x] "가정"/"미확정"/"옵셔널·폴백"/"잠정 표기" 표현이 Q1~Q7 관련 화면 스펙에서 전부 제거됐는가(전수 grep 검토)

- 발견된 결함 목록:
  1. **[Medium] 초안 작성 중, §2-1 홈 화면의 "부분 실패(업종 데이터만 없음)" 서술에서 `sector` 데이터 출처 미확정 리스크를 Q1~Q7 해소와 혼동할 여지가 있었음.** `stock_master.sector` 출처 미확정은 03-system-design §8-2 항목8의 별개 리스크이며 Q1~Q7 어디에도 속하지 않는데, 초안 문구가 이를 명확히 구분하지 않아 "Q1~Q7 해소됐는데 왜 이 부분은 여전히 미확정인가"라는 오해를 살 수 있었음.
  2. **[Medium] §2-2 결과 리스트의 PER/PBR/시가총액 백분위 표기 규칙을 반영하면서, "percentile이 시장 필터와 무관하게 전체 종목 기준"이라는 03-system-design §3-2의 중요한 계산 범위 제약을 화면에 어떻게 고지할지 초안에는 없었음.** 사용자가 "코스피"로 필터링해 조회했는데 표시되는 백분위가 코스피만의 순위가 아니라는 사실을 모르면 데이터를 오인할 위험이 있음(정확성/신뢰 문제).
  3. **[Low] `sort_dir`(정렬 방향)이 PER/PBR처럼 "낮을수록 상위"인 지표에서도 원시값 크기 기준으로 동작한다는 03-system-design §4-2 v3 신규 설명을, 화면의 정렬 방향 토글 라벨/설명에 반영하지 않으면 "내림차순인데 왜 PER이 큰 게 먼저 나오지"라는 혼동이 생길 수 있음.**

- 조치 내용 (수정 후 반영):
  1. §7-1에 "참고(Q1~Q7과 무관한 별개 리스크, 그대로 승계)" 문단을 별도로 신설해 `sector` 미확정 리스크를 Q1~Q7 해소 현황표와 명확히 분리했다. §2-1, §7-2에도 "Q1~Q7과 무관한 별개 리스크"라는 동일 문구를 반복 명시했다.
  2. §2-2에 "PER/PBR/시가총액 백분위 관련 중요 고지" 단락을 신설해, 시장 필터가 "전체"가 아닐 때 결과 영역 상단에 상시 노출되는 보조 안내 문구("PER·PBR·시가총액 백분위는 코스피+코스닥 전체 종목 기준입니다")를 명시했다. §2-4 종목 상세 카드에도 동일 취지의 보조 설명을 카드 하단에 추가했다.
  3. §2-2 조건 입력 폼 표의 "정렬 방향" 행에 "PER/PBR도 `sort_dir=desc`는 항상 원시값(배수)이 큰 순을 의미하며, 화면 토글 라벨도 값이 큰/작은 순으로 표기해 오해를 방지한다"는 설명을 추가했다.

## 2차 검증 (독립 심사자 관점 — "이 문서(v3)만 보고 화면을 구현할 프론트엔드 개발자")

- 검증자(역할): 오늘 v3 문서를 처음 받은 프론트엔드 개발자 관점 재검토
- 일시: 2026-09-14
- 체크리스트
  - [x] 1차 검증에서 지적된 3건이 실제로 본문에 반영되었는가(재확인 — 3건 모두 반영 확인)
  - [x] v2에서 "확인 필요"였던 Q1~Q7이 v3에서 화면 스펙 어디에도 모호하게 남아있지 않은가(§1~§4 전수 재검토)
  - [x] §7-1/§7-2/§8이 v3 확정 내용과 모순 없이 갱신되었는가(문구 대조)
  - [x] 새로 추가된 시장 구분 필터(`MarketFilterControl`)가 §2-2(스크리닝)·§2-3(검색) 두 화면에서 일관된 컴포넌트/상태로 정의되어 있는가
  - [x] 홈 화면의 "코스피/코스닥 2회 호출 후 합산" 방식이 문서 전체(플로우 다이어그램 §1-1 포함)에서 완전히 제거되고 단일 호출 패턴으로 일관되게 서술되는가
  - [x] 결과 리스트/종목 상세 카드의 percentile 카피가 두 화면(§2-2, §2-4) 간 표현이 일관되는가(예: "PER 백분위: 상위 N%" 포맷 통일)
  - [x] 금지표현 대조표(§2-6)에 v3 신규 항목(구간화 표현 금지)이 반영되었는가
  - [x] 변경 이력(§8)에 v3 변경 사항이 Q1~Q7 각각에 대응해 구체적으로 서술되어 있는가

- 발견된 결함 목록:
  1. **[Low] §1-1 Flow A의 다이어그램 텍스트가 v2의 "코스피/코스닥 각각 1회씩(총 2회) 호출" 서술을 단순 삭제만 하면, 정작 "그럼 몇 번 호출하는지"가 플로우 다이어그램 레벨에서 명시되지 않을 위험이 있었음(§2-1 본문에는 있으나 플로우 다이어그램은 더 눈에 띄는 첫 진입점이라 여기서도 확인 가능해야 함).**
  2. **[Low] `MarketFilterControl`을 §4 컴포넌트 표에 신규 추가했으나, 상태/변형 목록에 "로딩 중 상호작용 가능 여부"가 다른 입력 컴포넌트(`ConditionField` 등)와 달리 명시되지 않아 개발자가 임의 판단할 여지가 있었음.**
  3. **[Low] §2-2 결과 리스트 규칙에서 "정렬 기준 지표는 항상 표시"로 변경했는데, 만약 사용자가 필터 조건도 걸고 정렬 기준도 별도로 선택한 경우(예: 필터=시가총액, 정렬=PER) 컬럼이 "시가총액 + PER 백분위" 2개가 함께 표시된다는 것이 표 형태 서술만으로는 한 번에 읽히지 않아, 구체적 예시 문장이 없으면 개발자가 "어느 하나만 표시하는 건가"로 오독할 여지가 있었음.**

- 조치 내용 (수정 후 반영):
  1. §1-1 Flow A 다이어그램의 "성공" 라인에 "(단일 API 호출 `GET /market-summary`의 `data`(ALL 집계) + `data.by_market`(시장별 세부)로 구성, §2-1)"라는 괄호 설명을 추가해, 플로우 다이어그램 레벨에서도 단일 호출임이 즉시 확인되도록 했다.
  2. §4 `MarketFilterControl` 상태/변형에 "기본(전체 선택) / 선택됨(코스피 or 코스닥)"만 정의하고, 로딩 중 잠금 여부는 §2-2 "로딩" 상태 서술("조건 폼은 그대로 조작 가능 상태 유지")과 동일 원칙을 따른다는 점을 §2-2 로딩 상태 서술이 이미 포괄하고 있음을 확인 — 별도 컴포넌트 표에 중복 규정하지 않고 §2-2 상태 서술을 단일 소스로 유지하기로 결정(과잉 문서화 방지, 상태 정의가 두 곳에서 어긋날 위험 차단).
  3. §2-2 결과 리스트 조건부 표시 규칙 문장에 이미 존재하던 예시("예: 시가총액 최소값만 넣었으면 '시가총액' 컬럼 추가, PER은 조건에 넣지 않았어도 정렬 기준을 'PER순'으로 선택했다면 'PER 백분위' 컬럼을 표시")가 필터+정렬 동시 사용 케이스를 이미 포함하고 있음을 재확인했다. 다만 "동시에 표시된다"는 결론을 더 명확히 하기 위해 표현을 "필터 조건 지표 ∪ 정렬 기준 지표를 컬럼/항목으로 노출"로 이미 집합(∪) 표기를 쓰고 있어 중복 표시 의미가 수학적으로 명확함을 확인, 추가 예시 문장을 §2-2에 보강하지 않고 기존 집합 표기 + 예시로 충분하다고 판단(경미한 가독성 이슈로, 표기 자체는 정확하므로 문서 구조를 바꾸지 않음).

## 최종 판정 (v3)

- [x] PASS (v3 개정 검증 2회 완료, 발견된 결함 전부 조치 완료) — 다음 단계(5단계 개발)로 handoff 가능
- [ ] FAIL

### v3 최종 확인 사항
- `04-ux-design.md` §7-1의 Q1~Q7은 03-system-design.md v3의 확정에 따라 전량 해소됐다. 이 문서(v3) 기준으로 5단계(개발) 착수를 막는 "확인 필요" 항목은 남아있지 않다.
- Q1~Q7과 무관한 별개 리스크(`stock_master.sector` 업종 분류 데이터 출처 미확정, 03-system-design §8-2 항목8)는 그대로 승계되어 §2-1/§7-1/§7-2에서 "부분 실패" 방어 설계로 계속 대응한다. 이 리스크는 REQ-004의 업종 요약 세부 항목에 한정되며, 다른 화면·기능에는 영향을 주지 않는다(03-system-design §8-2 항목8 원문 판단과 동일).

---

## v2 검증 이력 원문 (참고용, v3 검증과 별개로 보존)

### 1차 검증 (작성자 관점 — "모든 핵심 플로우가 설계서의 API/데이터와 정합하는가")

- 검증자(역할): 04-ux-designer 본인(작성자 관점 자가 재검토)
- 일시: 2026-09-14
- 발견된 결함 목록:
  1. **[Critical] 스크리닝 조건 폼 초안에 API가 지원하지 않는 파라미터를 화면에 넣었음.**
  2. **[High] 종목 상세 화면 초안에 `sector`(업종) 뱃지를 넣었으나, 03-system-design §8-2 미해결 사항 8이 이미 "업종 분류 데이터 출처 미확정"이라고 명시하고 있음.**
  3. **[Critical] `stock_master.market`, `GET /stocks`·`GET /market-summary`의 `market` 파라미터 의미가 설계서 내에서 두 가지로 해석 가능함을 초안 작성 시점에는 인지하지 못함.**
  4. **[Medium] `GET /screen` 응답의 `matched_metrics`가 "조건 판단에 쓰인 가공 지표 값만" 포함한다는 문구를 놓치고, 초안은 정렬 기준 지표를 항상 결과 컬럼에 표시하도록 설계했음.**
  5. **[Low] 홈 화면의 "총 거래대금" 표기가 원 단위를 임의로 전제했으나, 통화 단위가 설계서에 명시되어 있지 않음을 확인.**
- 조치 내용 (수정 후 → v1): 조건 폼 API 1:1 대조표 교체, 업종 뱃지 제거, `market` 의미 중복을 §0-1/§7-1 Q1으로 분리, 결과 컬럼 표시 로직 수정, §7-1 Q6 신설.

### 2차 검증 (독립 심사자 관점 — "이 문서만 보고 화면을 구현할 프론트엔드 개발자")

- 검증자(역할): 오늘 이 문서를 처음 받은 프론트엔드 개발자 관점 재검토
- 일시: 2026-09-14
- 발견된 결함 목록: CLS 방지 규칙 부재, 폼 검증 타이밍/포커스 이동 불명확, 429 처리 위치 불명확, 바텀시트 접근성(포커스 트랩/Esc/포커스 복귀) 누락, 색 대비 수치 부재, 빈상태/에러상태 구분 기준 부재, 반응형 표 마크업 처리 불명확, `aria-live` 미명시.
- 조치 내용 (수정 후 → v2, 최종): 8건 전부 §2/§3/§4/§5/§6에 반영.

### 3차 검증 (2차에서 결함이 발견되어 추가 수행)

- 검증자(역할): 04-ux-designer (2차 조치 반영 여부 재확인 + 규칙 I 관점 재검토)
- 일시: 2026-09-14
- 발견된 결함 목록: 없음(2차 조치가 모두 정확히 반영됨을 확인, 신규 결함 미발견)
- 조치 내용: 없음(결함 0건이므로 추가 수정 없이 v2를 최종본으로 확정)

### v2 최종 판정
- [x] PASS (결함 0건, 최소 2회 검증 완료 — 실제로는 3회 수행) — 다음 단계(5단계 개발)로 handoff 가능

### v2 시점 핸드오프 노트 (v3에서 전량 해소됨)
- `04-ux-design.md` §7-1의 Q1~Q3(시장 구분 `market` 필드 의미, 스크리닝 정렬-전용 지표의 응답 보장 여부, 스크리닝 시장 필터 파라미터 부재)은 화면의 의미 자체를 바꿀 수 있는 항목이므로, 5단계 개발 착수 전 사용자 또는 3단계 설계자에게 재확인을 권고했다. → **`03-system-design.md` v3가 이 권고를 받아 Q1~Q7 전량을 확정 회신했고, 본 로그의 v3 검증 구간에서 반영 완료를 확인했다.**
