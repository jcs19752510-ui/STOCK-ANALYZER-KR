# 내부 검증 로그 (Internal Verification Log)

## 대상 산출물
- 파일: `docs/harness/02-planning.md` (+ 동시 생성/갱신물: `docs/harness/traceability.md`, `docs/harness/decisions.md`)
- 작성 에이전트: 02-planning-writer
- 버전: v0 (초안) → v2 (최종, 최초 PASS) → **v3 (규칙 F 피드백 루프 재작업)**

## 1차 검증 (작성자 관점 자가 재검토)
- 검증자(역할): 20년차 기획자 (작성자 본인 관점)
- 일시: 2026-09-14
- 체크리스트
  - [x] 입력 계약(01-trend-analysis.md v2 PASS + 프로젝트 자체 스캔)을 실제로 반영했는가 — §0에 스캔 결과(그린필드 확인) 명시, §3에 1단계 §2/§4/§5/§6/§8 전 항목을 반영 위치와 함께 매핑
  - [x] 출력 계약에 명시된 10개 섹션이 빠짐없이 존재하는가 — §1~§10 모두 존재
  - [x] 1단계 시사점(§8 5개 항목)이 기획서 어디에 반영됐는지 추적 가능한가 — §3 "1단계 시사점 ↔ 2단계 반영 매핑" 표로 명시적 자가 점검 완료, 5개 항목 전부 REQ-ID/섹션과 연결됨
  - [x] 규칙 I 대응 방안(리프레이밍/불특정다수/1:1배제/손실보전금지/면책문구, 직전거래일 정의/대체거래소 반영/데이터갱신지연)이 "제약조건" 서술에만 그치지 않고 정식 REQ-ID(REQ-005~010, 012)로 `traceability.md`에 등록됐는가 — 확인됨, 비고란에 "규제 민감 도메인 대응(규칙 I)" 명시
  - [x] 가정 목록의 누락 여부 — 데이터소스, 무료모델, 초기규모, 기술스택, 휴장일캘린더, 수익모델, 계정기능, 페르소나 근거까지 A1~A10으로 커버됨
  - [ ] → [발견] §3 본문에 인코딩 오류로 글자가 깨짐("정면 경�다") 발견
  - [x] 비가역적 결정(데이터소스, 무료모델, 명칭 리프레이밍)을 가정으로 슬쩍 채우지 않고 `decisions.md`에 근거와 함께 기록했는가 — DEC-001, DEC-002, DEC-003으로 기록 확인
- 발견된 결함 목록:
  1. §3 본문 인코딩 오류("정면 경�다") — 문서 가독성 저해
- 조치 내용: (수정 후 → v1)
  1. `sed`로 "정면 경쟁(종합 시세+뉴스 포털)을 하지 않고"로 정정, 문서 전체에 동일 오류(`�`) 재검색해 추가 발견 없음 확인

## 2차 검증 (독립 심사자 관점 — "이 기획서만 보고 설계서를 쓸 아키텍트")
- 검증자(역할): 3단계 시스템 설계자(03-system-designer) 관점 — 오늘 처음 이 문서를 받아본 심사자
- 일시: 2026-09-14
- 체크리스트
  - [x] 1차 검증 지적 사항이 실제로 반영되었는가 — 인코딩 오류 재확인 결과 추가 발견 없음
  - [x] 문서만 보고 3단계 작업자가 추가 질문 없이 설계를 시작할 수 있는가 — 기술스택은 위임하되(§8-A5) 전제 조건(무료/오픈소스, 배치 파이프라인, 모바일 우선)을 명시해 설계 착수 가능
  - [ ] → [발견] §5 KPI 마지막 행("데이터 미갱신 시 사용자 인지 소요 시간")이 목표치가 "화면상 즉시 확인 가능"처럼 서술적이라 측정 불가능한 목표에 가까움
  - [ ] → [발견] §9(반응형 UI, REQ-013)까지 있으나 브라우저/기기 지원 범위(레거시 브라우저 지원 여부 등)에 대한 가정이 §8에 없어, 3~4단계 담당자가 임의 판단할 위험이 있음
  - [x] 엣지 케이스 누락 없음 — 휴장일 상충, NXT 마감 다원화, 데이터 지연, 규제 프레이밍 전부 REQ/리스크에 반영
  - [x] 되돌리기 어려운 결정(데이터소스, 무료모델, 명칭 리프레이밍)에 대한 근거가 `decisions.md`에 명시되어 있는가 — DEC-001~003 확인, 근거 서술 있음
  - [x] 보안/규제 관점에서 명백히 위험한 내용이 없는가 — REQ-007~010이 Must have로 고정되어 있고 9단계 Critical 결함 처리 규정을 §7 리스크 1·6에 명시
  - [x] `traceability.md`의 REQ-ID와 `02-planning.md`의 REQ-ID·작업단위(UNIT) 매핑이 서로 모순 없이 일치하는가 — REQ-001~021 전 항목 상호 대조 완료, 불일치 없음
- 발견된 결함 목록:
  1. §5 KPI 마지막 행이 정량적 목표가 아니라 서술형이라 측정 불가능
  2. 브라우저/기기 지원 범위에 대한 가정 누락
- 조치 내용: (수정 후 → v2, 최종)
  1. 해당 행을 "설계 원칙 준수(Pass/Fail, REQ-006 표기 준수 여부를 4단계 체크리스트+9단계 검증으로 판정)" 방식으로 재정의해 측정 가능하게 변경
  2. §8에 A11(브라우저/기기 지원 범위: 모던 브라우저 최신 2개 버전 + 모바일 최신 OS 우선, 레거시 제외) 가정을 추가

## v2 시점 최종 판정 (재작업 전)
- [x] PASS (결함 0건, 최소 2회 검증 완료) — 3단계(시스템 설계)로 handoff 완료

---

## 재작업 트리거 (규칙 F 피드백 루프)
- 일시: 2026-09-14
- 트리거: 3단계 시스템 설계자가 착수 전 필수 확인을 수행한 결과, 채택 데이터소스(공공데이터포털 "금융위원회_주식시세정보" API)의 실제 이용약관 원문이 DEC-001/§6/§8-A2의 전제("원칙적으로 상업적 이용 가능")와 정반대(상업적 목적 불문 제3자 재배포 엄격 금지)임을 발견 → 사용자가 "가공된 지표/스크리닝 중심 재정의"를 승인 → v3 재작업 착수

## 3차 검증 (v3 재작업 — 작성자 관점, 근본 원인 반영 여부 자가 점검)
- 검증자(역할): 20년차 기획자 (작성자 본인 관점)
- 일시: 2026-09-14
- 체크리스트
  - [x] 사용자 지시의 6개 반영 항목이 모두 문서에 실제로 존재하는가 — (1) REQ-002/003/004 재정의: §4-1 표, `traceability.md` 확인. (2) "원본 재게시 금지" 설계 원칙 명문화: §4-3 신설 확인. (3) 법적 미확정성 명시 + REQ-022(Must have) 배포 게이트 신설: §4-1, §4-3-5, §6, §7-4·9, §8-A12 전체에서 일관되게 서술됨. (4) `decisions.md` DEC-004 append(DEC-001 대체 아닌 수정 형태로 상호 참조): 확인. (5) `traceability.md` REQ-002/003/004 텍스트 갱신 + REQ-022 신규 등록: 확인. (6) §10 변경이력에 v3 행 추가: 확인.
  - [x] "해석 주의사항"(이 경우에도 = 유료 구매 시 한정 해석 가능성, 재배포 자체 금지 가능성)이 원문 그대로 문서에 반영되어 왜곡 없이 전달되는가 — §4-3, v3 개정 배경 섹션에 원문 그대로 인용 후 해석 주의사항을 사용자 지시 문구와 거의 동일하게 반영함을 확인
  - [x] 이 문서가 법적 해석을 확정짓지 않았는가(에이전트가 "안전하다/위험하다"를 단정하지 않았는가) — §4-3-5, §7-9, §8-A12에서 반복적으로 "확정하지 않는다", "법적으로 안전함이 확정됐다고 서술하지 않는다"고 명시. 위반 문구 없음(재검색 완료)
  - [x] §3(가치제안)·§5(성공지표)가 재정의된 범위와 일치하는가 — §3에 "가공된 지표는 설계 원칙" 문구 추가, "실시간이 아니라"는 기존 표현 유지(원본 재게시와 무관하므로 정정 불필요 확인), §5에 "데이터 가공 원칙 준수(0건)" KPI 및 "배포 게이트 충족(Pass 필수)" KPI 신설
  - [x] REQ-022가 "제약조건"·"리스크" 서술로만 남지 않고 정식 REQ-ID(Must have)로 `traceability.md`에 등록됐는가 — 확인, MoSCoW 요약 문구에도 REQ-022 포함되도록 갱신
  - [x] REQ-002/003/004 변경이 §9 작업단위(UNIT-06/07/08) 설명에도 일관되게 반영됐는가 — 확인, 코드 리뷰 완료 조건에 "§4-3 위반 여부 확인"도 추가함
  - [ ] → [발견] §4-1 In-Scope 표에서 REQ-022 항목의 "비고" 칸이 다소 길어 표 가독성이 떨어짐 — 그러나 정보 누락보다 가독성 이슈이므로 Critical은 아님, 2차 검증에서 재평가하기로 함
- 발견된 결함 목록:
  1. (경미) REQ-022 비고 칸 가독성 — 내용 누락은 아님
- 조치 내용: 1차 검증 시점에는 내용 정확성이 가독성보다 우선이라 판단해 유지, 2차 검증(아키텍트 관점)에서 실제로 설계 착수에 지장이 있는지 재확인하기로 함

## 4차 검증 (v3 재작업 — 독립 심사자 관점, "이 개정 기획서만 보고 재개할 3단계 설계자")
- 검증자(역할): 3단계 시스템 설계자(03-system-designer) 관점 — v3를 처음 받아보는 심사자, 특히 "왜 재작업이 필요했는가"와 "무엇이 바뀌었는가"를 정확히 구분해서 읽어야 하는 입장
- 일시: 2026-09-14
- 체크리스트
  - [x] 3차 검증에서 지적된 REQ-022 비고 칸 가독성 문제가 실제로 설계 착수를 막는가 — 표 자체는 다소 길지만 §4-3에 동일 내용이 산문 형태로 다시 설명되어 있어 표만 봐도, 산문만 봐도 이해 가능. 설계 착수에 실질적 지장 없음 → Critical 아님, 수정 보류(문서 전체 가독성보다 원문 인용의 정확성이 이 사안에서는 더 중요하다고 판단)
  - [x] v3 문서만 읽고, 3단계 설계자가 "REQ-002/003/004를 어떻게 다시 설계해야 하는가"를 추가 질문 없이 알 수 있는가 — §4-3에 5개 항목으로 구체적 설계 지침(원본 재게시 금지, 파생지표만 노출, 근거지표 위주 표시, 요약통계 중심, 법적 불확실성 유보)이 명시되어 있어 가능
  - [x] "재정의했으니 법적으로 안전하다"는 식의 과잉 확신 문구가 문서 어디에도 없는가 — 전체 재검색 결과 없음. 오히려 §4-3-5, §7-9, §8-A12에서 반복적으로 불확실성을 명시해 과잉 확신 방지가 잘 되어 있음
  - [x] REQ-022가 5단계 작업단위(UNIT)와 혼동되지 않도록 명확히 구분되어 있는가 — §4-1 비고, §9 마지막 단락에서 "코드 구현 항목이 아니라 배포 게이트"임을 반복 명시, `traceability.md`에도 작업단위 칸에 "N/A (11단계 배포 게이트)"로 명확히 구분
  - [x] `decisions.md` DEC-004가 append-only 원칙을 지켰는가(DEC-001을 삭제/수정하지 않고 새 항목으로 추가했는가) — 확인, DEC-001 원문은 그대로 두고 말미에 "[v3 갱신 — DEC-004 참조]" 문구만 추가하는 방식으로 최소 침습적으로 갱신, DEC-004는 별도 신규 행으로 추가됨
  - [x] `traceability.md`의 REQ-002/003/004/REQ-022가 `02-planning.md`와 REQ-ID·비고·상태 면에서 상호 모순 없이 일치하는가 — 상호 대조 완료, 불일치 없음
  - [x] §10 변경이력 v3 항목이 "무엇을·왜"를 모두 담고 있어 이후 담당자가 재구성 가능한가 — 6개 세부 변경사항과 근본원인·규칙F 근거가 함께 서술되어 있어 충분
  - [x] 사용자에게 질문이 필요한 잔여 항목이 남아있지 않은가 — 사용자가 이미 방향(가공 지표 중심 재정의)을 승인했고, 나머지는 그 범위 내에서 에이전트가 구체화하는 것으로 지시받았으므로 신규 질문 불요. 유일하게 남은 미확정 사항(법적 해석 자체)은 질문이 아니라 REQ-022 게이트로 정식 이관되어 있어 적절함
- 발견된 결함 목록: 없음 (3차에서 발견된 가독성 이슈는 Critical 판정에서 제외, 개선 여지로만 기록)
- 조치 내용: 해당 없음 (결함 0건)

## v3 최종 판정
- [x] PASS (v3 기준 결함 0건, 최소 2회 검증 완료 — 누적 4회) — 3단계(시스템 설계)가 이 개정된 기획서를 기반으로 재개 가능
- 잔여 비-차단 개선 여지: REQ-022 비고 칸 가독성(경미, Critical 아님) — 향후 리라이트 시 표를 분리하는 방식으로 개선 가능하나 현재 PASS 판정을 막지 않음

## v5 검증 (하네스 12→13단계 버전 업그레이드 — 범위 한정 재검증, Tier: High)
> DEC-021에 따라 이 문서가 재개봉됨. 변경 범위가 (a) "11단계 배포" 문자열의 기계적 정정(12단계로 이동)과 (b) REQ-023(규칙 J 비해당) 신규 등록 두 가지로 명확히 한정되어 있으므로, 문서 전체를 처음부터 다시 검증하지 않고 이 델타에 한정해 규칙 B(High 등급 — 완화 미적용, 최소 2회) 검증을 수행한다.

### 1차 검증 (작성자 관점 — 변경 범위 자가 재검토)
- 검증자(역할): 02-planning-writer 관점
- 일시: 2026-09-17
- 체크리스트
  - [x] §4-1 REQ-022, §6, §7, §8, §0의 "11단계 배포" 참조 8곳이 전부 "12단계 배포"로 일관되게 정정됐는가 — `grep -n "11단계" 02-planning.md` 결과 0건으로 확인
  - [x] REQ-023의 "규칙 J 비해당" 판정 근거(REQ-015의 챗봇 배제, REQ-002~004의 결정론적 계산)가 실제로 이 문서의 기존 내용과 모순 없이 일치하는가 — §4-2 REQ-015 원문, §4-1 REQ-002~004 원문 재대조 완료, 모순 없음
  - [x] REQ-023 추가로 인해 MoSCoW 요약(§4-1 하단)이 갱신 필요한지 — REQ-023은 Out-of-Scope이므로 Must/Should 목록에 영향 없음, 갱신 불요를 확인
  - [x] §10 변경이력 v5 항목이 "무엇을·왜"를 모두 담아 이후 담당자가 재구성 가능한가 — `decisions.md` DEC-021 참조가 명시되어 있어 충분
- 발견된 결함 목록: 없음
- 조치 내용: 해당 없음 (결함 0건)

### 2차 검증 (독립 심사자 관점 — "이 v5만 처음 받아본 3단계 설계자")
- 검증자(역할): 03-system-designer 관점 — v5 변경분만 보고 이후 단계에 지장이 없는지 확인하는 입장
- 일시: 2026-09-17
- 체크리스트
  - [x] 정정된 "12단계 배포" 참조가 실제 파이프라인 표(§2, 신규 ORCHESTRATOR.md)와 일치하는가 — 신규 ORCHESTRATOR.md 파이프라인 표(11=문서화, 12=배포, 13=배포후검증)와 대조 완료, 일치
  - [x] REQ-023이 5단계 작업단위(UNIT) 목록(§9)에 잘못 편입되지 않았는가 — §9 UNIT 목록 재확인, REQ-023은 Out-of-Scope 표에만 존재하고 UNIT 목록에는 없음(REQ-022와 동일하게 코드 작업이 아니므로 적절)
  - [x] 이미 PASS 처리된 UNIT-01~06의 판정(위 v0~v4 검증 이력)이 이번 v5 변경으로 소급 무효화되지 않는가 — 변경 범위가 문자열 정정+신규 Out-of-Scope REQ 추가뿐이라 기존 REQ-001~022의 실질 내용·MoSCoW·완료된 UNIT 산출물에 영향 없음을 재확인
  - [x] 사용자에게 질문이 필요한 잔여 항목이 없는가 — DEC-021에 이미 사용자가 명시적으로 승인(DEC-020 번복)했으므로 신규 질문 불요
- 발견된 결함 목록: 없음
- 조치 내용: 해당 없음 (결함 0건)

## v5 최종 판정
- [x] PASS (v5 변경 범위 기준 결함 0건, 최소 2회 검증 완료) — 하네스 13단계 구조로 계속 진행 가능
- 범위 밖 확인: 이 v5 검증은 REQ-001~022의 기존 내용을 재검증한 것이 아니며(이미 v0~v4에서 PASS 완료), 순수 버전 업그레이드 델타만 검증했다. 기존 UNIT-01~06 산출물 재작업 불필요.
