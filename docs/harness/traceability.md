# 요구사항 추적 매트릭스 (Requirements Traceability Matrix)

> 2단계(`02-planning.md`)에서 생성. 기능 범위(In-Scope) 항목마다 REQ-ID를 부여했고, 규칙 I(규제 민감 도메인 대응)에 따라 1단계에서 발견된 규제 대응·도메인 운영 규칙도 별도 REQ-ID로 등록했다. Out-of-Scope 항목도 사유와 함께 포함한다.
> 이후 단계에서 새 ID를 임의로 추가하지 않는다. 새 요구사항이 필요하면 규칙 A에 따라 질문하거나 규칙 F(피드백 루프)로 2단계 산출물을 수정한다.
> **v3 갱신(규칙 F 피드백 루프)**: `02-planning.md` v3에서 REQ-002/003/004의 내용이 "원본 시세 재게시"에서 "가공된 지표/요약 통계 제공"으로 재정의됐고, 신규 REQ-022(데이터 라이선스 배포 전 확인 게이트)가 등록됐다. 상세 근거는 `decisions.md` DEC-004 참조.
> **3단계 갱신**: `03-system-design.md` v2(PASS, 내부검증 2회 완료) 완료에 따라 "설계 매핑(설계서 §)" 컬럼을 전 REQ-ID에 대해 채웠다.
> **3단계 v3 갱신(규칙 F 피드백 루프)**: 4단계(UX설계)가 `04-ux-design.md` §7-1(Q1~Q7)에서 발견한 기술 스펙 공백 7건에 대응해 `03-system-design.md`가 v3로 개정됨에 따라, REQ-002/003/004의 "설계 매핑" 컬럼을 갱신했다. 상세 근거는 `decisions.md` DEC-011~016 참조.
> **6단계 갱신(UNIT-01)**: `docs/harness/units/unit-01-test.md`(06-unit-tester) 완료에 따라 REQ-005/REQ-012의 "구현 상태"/"단위테스트" 컬럼을 갱신했다. REQ-005는 핵심 함수 `get_last_trading_day()`의 경계조건 결함(DEF-001)으로 **FAIL**, REQ-012(YAML 파싱/CLI)는 **PASS**(단 CLI의 실 DB upsert 경로는 Postgres 환경 부재로 미검증). 결함의 근본 원인은 3단계 설계서(§3-3)로 추적되어 규칙 F에 따라 재작업이 필요하다. 상세 근거는 `decisions.md` DEC-017 참조.
> **3단계 v4 갱신(규칙 F 피드백 루프)**: 6단계가 제기한 DEF-001(REQ-005 핵심 함수 결함)에 대응해 `03-system-design.md`가 v4로 개정됨에 따라(§3-3 전면 재작성 — 마감 시각 기준, §7-2 신선도 cron 정합화), REQ-005의 "설계 매핑"/"구현 상태" 비고를 갱신했다. 근거는 `decisions.md` DEC-018 참조. **이 v4 설계를 반영한 재구현·재검증(5·6단계)은 아직 수행되지 않았으므로 REQ-005는 여전히 진행 중(Done 아님) 상태다.**
> **6단계 재검증 갱신(규칙 F 피드백 루프, UNIT-01 재작업 완료)**: 5단계가 `03-system-design.md` v4를 반영해 `shared/calendar_service/last_trading_day.py`를 재구현(`market_hours.py` 삭제)한 뒤, 6단계(`docs/harness/units/unit-01-test.md` v2)가 DEF-001/DEF-002 해소 여부를 독립 재검증했다. 두 결함 모두 **Fixed**로 확인했으며, REQ-005/REQ-012의 "구현 상태"/"단위테스트" 컬럼을 갱신했다. 단 AC-3 불릿4(CLI 실 upsert)·AC-5(실 마이그레이션)는 PostgreSQL/Docker 미가동 환경 제약으로 이번에도 미검증 상태이므로, UNIT-01은 완전한 Done이 아니라 **CONDITIONAL PASS**로 전환됐다(조건: Postgres 실기동 확인). 상세 근거는 `docs/harness/units/unit-01-test.md`(v2) §8 참조.
> **6단계 v3 재검증 갱신(실제 PostgreSQL 확보에 따른 AC-3/AC-5 확정 검증)**: 코디네이터가 로컬 Docker에 실제 PostgreSQL 16(컨테이너 `stock-screener-db`, 역할 `migrator`/`batch_worker`/`api_service`)을 준비함에 따라, 6단계(`docs/harness/units/unit-01-test.md` v3)가 v2까지 "미검증"으로 남겨뒀던 AC-3 불릿4(CLI 실 upsert)·AC-5(실 마이그레이션 적용/롤백)를 실제 DB로 **확정 PASS** 처리했다(마이그레이션 적용/롤백, 730건 upsert, 재실행 idempotency 전부 실제 쿼리로 확인). 이 과정에서 코드 자체의 신규 결함 2건을 발견했다: **DEF-003(Medium, Open)** — 마이그레이션이 `reference` 스키마에 대해 GRANT 문을 전혀 실행하지 않아, 역할이 분리된 프로덕션 환경에서는 GRANT 없이 CLI 실행이 100% 실패함(재현 확인). **DEF-004(Low, Open)** — `scripts/load_calendar.py`의 upsert가 `updated_at`을 갱신하지 않아 감사 추적성이 저하됨(재현 확인). 두 결함 모두 Fake 기반 단위테스트로는 발견할 수 없었던 실제 인프라 전용 결함이다. `api_service`의 `raw_internal` 접근 거부는 UNIT-02 산출물이 아직 없어 대역(proxy) 스키마로만 보조 검증했고(원리 확인, 실제 코드 검증 아님), UNIT-02 완료 후 재검증이 필요하다. 종합 판정은 **CONDITIONAL PASS 유지**(조건이 "Postgres 미검증"에서 "DEF-003 Open 해결"로 전환됨) — DEF-003이 Fixed 되기 전에는 9~11단계(보안검증/배포테스트/배포) 진입 불가로 명시. 상세 근거는 `docs/harness/units/unit-01-test.md`(v3) §8/최종 판정 요약 참조.
> **6단계 v4 재검증 갱신(DEF-003/DEF-004 수정에 대한 독립 재검증, UNIT-01 최종 판정)**: 5단계가 DEF-003(GRANT 코드화, 신규 리비전 `0002_grant_reference_privileges.py`)·DEF-004(`updated_at` 갱신 추가)를 수정한 뒤, 6단계(`docs/harness/units/unit-01-test.md` v4)가 **5단계의 자체 검증 결과를 신뢰하지 않고 독립적으로 재현**했다: (1) `alembic downgrade base`로 완전히 초기화한 뒤 처음부터 `alembic upgrade head`(0001+0002)를 재실행해 GRANT가 마이그레이션 자체의 동작임을 확인(우연이 아님), (2) `api_service`의 UPDATE/INSERT/DELETE 거부를 5단계보다 범위를 넓혀 재현, (3) 5단계와 다른 오염값으로 `updated_at` 실제 갱신을 재현, (4) `pytest tests/unit`(36건)·`ruff check .`·실제 Postgres 기반 Public API 엔드투엔드로 DEF-001/DEF-002 회귀 없음 및 기존 AC 전체 회귀 없음을 확인. **신규 결함 0건.** DEF-001(High)/DEF-002(Low)/DEF-003(Medium)/DEF-004(Low) **전부 Fixed로 최종 확인**되어 UNIT-01은 **PASS(조건부 아님)**로 최종 판정됐다. 남은 리스크(raw_internal 실제 재검증-UNIT-02 이관, uvicorn 실기동 미검증, 역할 프로비저닝 문서화 공백, `updated_at`이 매번 갱신되는 특성)는 결함이 아니라 후속 유닛/문서화 과제로 인계한다. 상세 근거는 `docs/harness/units/unit-01-test.md`(v4)/최종 판정 요약 참조.
> **5단계 갱신(UNIT-02)**: `docs/harness/units/unit-02-note.md` 작성 완료에 따라 REQ-011의 "구현 상태" 컬럼을 "5단계 구현 완료 — 6단계 단위테스트 대기"로 갱신했다. Ingestion Batch(`services/ingestion_batch/`), `raw_internal.raw_ohlcv`/`raw_fundamentals` + `public_serving.batch_run` 마이그레이션(0003/0004, GRANT 포함)을 구현하고 로컬 Docker `stock-screener-db`로 스키마/GRANT/upsert/서킷브레이커 escalation을 실제 검증했다. **실 서비스키 미보유로 정상 응답 파싱 경로는 실제 API로 검증하지 못했다**(게이트웨이 오류 응답 형태는 실 엔드포인트를 직접 호출해 실측). raw_fundamentals 적재는 이번 유닛 범위에서 구현하지 않음(스키마만 생성) — **(2026-09-15 정정, 6단계 DEF-005 지적 반영)** PER/PBR은 해당 오퍼레이션에 미제공, 시가총액(`mrktTotAmt`)은 제공되나 이번 유닛에서는 raw_fundamentals 적재 로직 자체를 범위에서 제외함(가공 로직은 UNIT-06/07/08 소관). 최초 작성 시 "PER/PBR/시가총액 전부 미확인"으로 서술했던 것은 부정확했다(코드 결함 아님, 문서 서술 정확도 문제). 상세는 `unit-02-note.md` §0-3 참조. 6단계 단위테스트가 완료되면 이 항목을 다시 갱신해야 한다.
> **5단계 갱신(UNIT-03)**: `docs/harness/units/unit-03-note.md` 작성 완료에 따라 REQ-001의 "구현 상태" 컬럼을 "5단계 구현 완료 — 6단계 단위테스트 대기"로 갱신했다. `public_serving.stock_master`(마이그레이션 0005, GRANT 포함) + `GET /api/v1/stocks?query=&market=` + 시드 스크립트(`scripts/seed_stock_master.py`)를 구현했다. **핵심 이슈였던 "종목 마스터 시드 데이터 출처"는 새 외부 API를 도입하지 않고, UNIT-02가 이미 연동한 공공데이터포털 `getStockPriceInfo` 응답의 `itmsNm`(종목명)/`mrktCtg`(시장구분) 필드를 재사용하는 것으로 확정했다**(`unit-03-note.md` §0 참조 — 이 판단은 사용자 확인 없이 5단계가 직접 결정했으며, 근거는 노트에 상세 기록). 이를 위해 UNIT-02 산출물(`services/ingestion_batch/gov_data_client.py`)에 동작 보존 리팩터링(`_fetch_raw_items()` 추출) + 신규 메서드(`fetch_stock_master_snapshot()`)를 추가했고, UNIT-02의 기존 13개 테스트를 수정 없이 재실행해 회귀 없음을 확인했다. 설계서 §3-2에 없는 `stock_master.updated_at` 컬럼을 DEF-004 재발 방지 목적으로 추가했고, `listing_date`는 채택 데이터 소스에 상장일 정보가 없어 nullable로 구현했다(§8-2 항목8의 `sector` 처리 원칙과 동일 — 상상으로 채우지 않음). 로컬 Docker `stock-screener-db`로 마이그레이션/GRANT/`updated_at` 갱신/API 종단간 동작을 직접 검증했다. 상세는 `unit-03-note.md` §0~§7 참조.
> **6단계 갱신(UNIT-02)**: `docs/harness/units/unit-02-test.md`(06-unit-tester) 완료에 따라 REQ-011의 "단위테스트" 컬럼을 갱신했다. 5단계의 자체 보고("59개 pass", "raw_internal 권한 분리 확인")를 신뢰하지 않고 독립 재검증했다: (1) `alembic downgrade 0002`→`upgrade head` 전체 사이클을 다시 실행해 스키마/GRANT가 우연이 아니라 마이그레이션 자체의 동작임을 재확인, (2) proxy가 아니라 **실제** `raw_internal.raw_ohlcv`/`raw_fundamentals`에 `api_service`로 직접 SELECT를 시도해 `permission denied for schema raw_internal`을 재현(UNIT-01 v3의 대역 검증보다 진전), (3) `public_serving.batch_run`에 `api_service`로 INSERT 시도 시 거부됨을 확인해 읽기전용 경계도 검증, (4) 정상 응답 파싱이 가정한 필드명/응답 봉투 구조를 data.go.kr 공식 Swagger 메타데이터(`data.go.kr/data/15094808/openapi.do`, 인증 없이 열람 가능)와 직접 대조해 상상으로 지어낸 스펙이 아님을 확정, (5) 재시도 백오프를 모의 sleep이 아닌 실제 시간 경과로, 서킷브레이커 3일 연속 실패 에스컬레이션을 실제 네트워크+실제 CLI 프로세스로 종단간 재현, (6) UNIT-01 `get_last_trading_day()`가 재구현 없이 그대로 재사용되고 있음을 소스 경로로 확인(회귀 없음). **신규 결함 0건(코드 기준). DEF-005(Low, Open)**: note가 "PER/PBR/시가총액 전부 미확인"이라고 서술했으나, 공식 스펙 대조 결과 **시가총액(`mrktTotAmt`)은 실제로 `getStockPriceInfo`가 제공함**이 확인되어 서술 정확도 이슈로 등록(코드 결함 아님, AC 위반 아님, raw_fundamentals 적재 자체는 이번 유닛 범위 밖이므로 배포 차단 사유 아님 — 후속 유닛 설계 참고사항으로 인계). **종합 판정 PASS**(다음 단계 07 통합테스트 진행 가능). 상세는 `unit-02-test.md` §4~§8 참조.

> **6단계 갱신(UNIT-03)**: `docs/harness/units/unit-03-test.md`(06-unit-tester) 완료에 따라 REQ-001의 "구현 상태"/"단위테스트" 컬럼을 갱신했다. 5단계의 자체 보고("79개 테스트 pass", "GRANT/`updated_at` 이번엔 처음부터 반영")를 신뢰하지 않고 독립 재검증했다: (1) `alembic downgrade base`로 전 스키마를 완전히 제거한 뒤 처음부터 `alembic upgrade head`(0001~0005)를 재실행해 `stock_master` GRANT(`batch_worker=arw`/`api_service=r`)가 마이그레이션 자체의 동작임을 재확인(이 과정에서 `reference.market_calendar` 730행이 일시 삭제되는 부작용이 발생했으나 즉시 `scripts/load_calendar.py`로 복구, `unit-03-test.md` §3에 투명하게 기록), (2) `api_service`의 INSERT/UPDATE/DELETE 3종 전부에 대해 실제 쓰기 거부를 재현(5단계는 INSERT만 실측), (3) `updated_at` 실제 갱신을 5단계와 다른 오염값(카카오, 5단계는 삼성전자)으로 독립 재현, (4) `itmsNm`/`mrktCtg` 필드가 UNIT-02 문서를 재인용한 것이 아니라 data.go.kr 공식 Swagger 원문에 실제로 존재함을 6단계가 직접 재확인(`mrktCtg` 값이 영문 표기라는 추가 근거도 발견), (5) `listing_date` nullable 처리가 03-system-design.md §3-2/04-ux-design.md §2-3 원문과 충돌 없음을 문서 대조로 확정, (6) `pytest tests/unit`(79건)·`ruff check .` 회귀 없음 확인. **신규 결함 2건 발견**: **DEF-006(Low, Open)** — `GET /stocks`의 검색어 처리가 SQL LIKE 와일드카드 문자(`%`,`_`)를 이스케이프하지 않아 이런 문자를 입력하면 전체 종목이 매칭됨(SQL 인젝션은 아님, 파라미터 바인딩 안전성은 별도 확인). **DEF-007(Low, Open)** — 시드 스크립트가 매 실행마다 `is_active`를 무조건 `true`로 재설정해, 운영자가 수동으로 내린 상장폐지 처리가 다음 시드 실행으로 되돌아갈 수 있음이 문서화되어 있지 않음(코드 결함 아님, 문서화 미비). 두 결함 모두 AC 위반 아니고 배포 차단 사유 아니다 — **종합 판정 PASS**(다음 단계 07 통합테스트 진행 가능). 상세는 `unit-03-test.md` §4~§9 참조.

> **5단계 갱신(UNIT-04)**: `docs/harness/units/unit-04-note.md` 작성 완료에 따라 REQ-006/007/008/009/010의 "구현 상태" 컬럼을 갱신했다. 이번 유닛에서 프론트엔드(Next.js 16 + TypeScript, `frontend/`)를 최초로 스캐폴딩했다(그동안 UNIT-01~03은 백엔드만 존재). 구현 내용: (1) REQ-007 — 루트 레이아웃(`frontend/src/app/layout.tsx`)에 sticky·닫기버튼 없음·API 응답과 무관하게 즉시 렌더되는 `DisclaimerBanner` + 푸터 이중 노출 + `/about` 상세 페이지, 배너 문구가 백엔드 `DISCLAIMER_TEXT`(UNIT-01~03에서 이미 구현된 `envelope.py`)와 문자 단위로 동일함을 스크립트로 직접 대조 확인. (2) REQ-006 — `DataFreshnessBadge` 컴포넌트가 `meta.data_freshness`를 그대로 표시(자체 계산 없음). (3) REQ-008 — 중앙 카피 리소스 `copy.ko.json` + `frontend/scripts/lint-forbidden-copy.mjs`를 `npm run build`의 `prebuild` 훅으로 연결하고, **금지어("추천 종목입니다")를 실제로 주입해 빌드가 exit code 1로 실패하는 것과 원복 후 통과하는 것을 양쪽 다 직접 실행해 확인**(설계 문서화에 그치지 않음). (4) REQ-009 — 신규 코드를 추가하지 않고, 기존 구현된 API 3개(`health`/`stocks`/`calendar/last-trading-day`) 전체를 사용자 식별 관련 키워드로 재검색해 그런 파라미터가 없음을 재확인. (5) REQ-010 — 결제/광고 SDK 의존성이 어디에도 추가되지 않았음을 확인(부재 자체가 구현). `npm run build`/`npm run lint`/`npx tsc --noEmit` 전부 통과, `npm run dev`로 실제 렌더링 결과(배너/스킵링크/about 본문)를 curl로 직접 확인했다. 상세는 `unit-04-note.md` 참조. **6단계 단위테스트 전까지는 PASS 확정이 아니다.**
> **6단계 갱신(UNIT-04)**: `docs/harness/units/unit-04-test.md`(06-unit-tester) 완료에 따라 REQ-006/007/008/009/010의 "단위테스트" 컬럼을 갱신했다. 5단계의 자체 보고를 신뢰하지 않고 독립 재현했다: (1) REQ-007 — `npm run build && npm run start`로 실제 프로덕션 렌더링을 띄워 `/`·`/about` 양쪽 HTML에서 배너 마크업·정확한 문구·닫기 버튼 부재(grep 0건)·말줄임 부재(컴파일된 CSS 청크에 `text-overflow` 없음)를 직접 확인, `DISCLAIMER_TEXT`와 `copy.ko.json` 값을 Python으로 문자 단위 등가 비교(True), `/about` 6개 `<h2>` 전부 확인. (2) REQ-008 — 5단계가 확인한 "추천 종목입니다" 외에 "매수신호"/"손실보전"/"수익보장"/"PER 상위 20% 구간"/"확실한" 5종을 독립적으로 각각 주입해 전부 exit 1 재현 후 원복 재확인, `npm run build` 전체 실행 시 `prebuild` 단계에서 완전히 중단되고 `next build` 로그가 전혀 나타나지 않음을 확인, `FORBIDDEN_TERMS`/`FORBIDDEN_PATTERNS`가 04-ux-design.md §2-6 대조표 7행 전부를 커버함을 대조 확인. (3) REQ-006 — 코드 리뷰로 자체 계산 없음을 재확인하고, 임시 QA 라우트(검증 후 즉시 삭제, 삭제 후 빌드 재현으로 원상복구 확인)로 AC-2의 4개 prop 조합(정상/지연/`freshness=null`/`session_close_at=null`)을 실제 렌더링해 전부 기대 텍스트와 정확히 일치함을 확인 — 5단계 note에는 이 실제 렌더링 검증 로그가 없었음(TS 컴파일만 확인한 상태였음). (4) REQ-009 — 5단계의 grep 기반 정적 검증이 "코드에 없는 코드"(예: 다른 위치의 미들웨어)까지는 배제하지 못한다고 판단해, FastAPI `TestClient`로 동일 쿼리에 `Authorization` 헤더 + `session_id`/`user_id` 쿠키를 붙인 요청과 붙이지 않은 요청의 응답이 완전히 동일함을 실제 HTTP 계층에서 증명(블랙박스 보강), 알 수 없는 `user_id` 쿼리파라미터도 무시됨을 확인. (5) 백엔드 회귀 — `ruff check .` 전부 통과, `pytest tests/unit` 79건 전부 통과(신규 실패 없음). **신규 결함 2건 발견(둘 다 AC 문면 범위 밖에서 위험 기반으로 추가한 테스트로 발견)**: **DEF-001(Medium, Open)** — `formatKstDateTime()`이 빈 문자열/깨진 ISO 문자열을 받으면 예외 없이 방어하지 못하고 `RangeError`를 던져, `DataFreshnessBadge`를 실 데이터에 연결하는 순간 백엔드가 이상값을 보내면 화면 전체가 크래시할 잠재력이 있음(현재는 dead code라 즉시 영향 없음). **DEF-002(Medium, Open)** — `lint-forbidden-copy.mjs`가 물리적 줄 단위로 스캔해, 금지어가 실제 줄바꿈으로 분리된 멀티라인 문자열(예: 템플릿 리터럴)에 들어가면 탐지하지 못하고 빌드를 통과시킴(현재 카피는 전부 단일 라인 JSON이라 즉시 발생하는 위반 사례는 없음). **종합 판정 CONDITIONAL PASS** — REQ-006/007/009/010은 AC 기준 결함 0건으로 PASS, REQ-008은 AC 문면 자체는 결함 0건이나 CI 게이트의 구조적 우회 가능성(DEF-002)이 발견되어 CONDITIONAL PASS로 하향. 조건: UNIT-06/07/08이 `DataFreshnessBadge`를 실 데이터에 연결하기 전 DEF-001 해결, 카피 리소스에 멀티라인 문자열 도입 전 DEF-002 해결. 상세는 `unit-04-test.md` §4~§9, `verify-log_unit-04-test.md` 참조.
> **5단계 재작업 갱신(UNIT-04, 6단계 CONDITIONAL PASS 결함 조치, 2026-09-15)**: `unit-04-test.md`가 발견한 DEF-001(Medium)/DEF-002(Medium)를 코디네이터 지시에 따라 즉시 조치했다(상세는 `unit-04-note.md` "재작업 이력" 절 참조). **DEF-002(우선 처리)**: `frontend/scripts/lint-forbidden-copy.mjs`가 물리적 줄 단위로 스캔해 금지어가 실제 줄바꿈으로 분리되면(TC-027 재현 시나리오) 탐지를 우회할 수 있던 구조적 허점을, 파일 전체에서 공백류 문자를 제거한 정규화 문자열 위에서 검사하도록 바꿔 해소했다(원본 위치 추적용 `indexMap`으로 정확한 줄번호는 그대로 유지). **직접 재현 검증**: `` `손실보\n전` ``(실제 줄바꿈 포함) 템플릿 리터럴을 담은 임시 파일을 만들어 실행한 결과 수정 전에는 "통과"였던 것이 수정 후 **exit code 1**로 정확히 탐지됨을 확인했고, 파일 삭제 후 재실행해 정상 통과함도 확인했다. 기존 TC-020~026(5종 금지어)도 전부 회귀 없이 재확인했다. **DEF-001**: `formatKstDateTime()`이 빈 문자열/깨진 ISO 문자열에 `RangeError`를 던지던 것을, `Number.isNaN(date.getTime())` 가드로 예외 대신 `INVALID_DATE_FALLBACK_TEXT`("기준시각 확인 불가")를 반환하도록 수정했다. **직접 재현 검증**: Node에서 `formatKstDateTime('')`/`formatKstDateTime('not-a-date')`가 예외 없이 폴백 문구를 반환하고, 정상 ISO 입력(`'2026-09-11T15:30:00+09:00'`)은 기존과 동일하게 `"2026-09-11 15:30"`을 반환함을 확인(정상 경로 회귀 없음). 조치 후 `npx tsc --noEmit`/`npm run lint`/`npm run build`(prebuild 포함) 전부 통과, 백엔드 회귀(`ruff check .`, `pytest tests/unit` 79건) 전부 통과 재확인. **6단계의 독립 재검증 전까지는 REQ-006/REQ-008 최종 PASS 확정이 아니며**, 아래 표의 "구현 상태"만 갱신하고 "단위테스트" 컬럼(6단계 소유)은 임의로 수정하지 않았다.
> **6단계 갱신(UNIT-05)**: `docs/harness/units/unit-05-test.md`(06-unit-tester) 완료에 따라 REQ-013의 "구현 상태"/"단위테스트" 컬럼을 갱신했다. 5단계의 자체 보고(note §3 "실제 브라우저 확인 필요")를 신뢰하지 않고, 헤드리스 Chrome(DevTools Protocol)으로 320~1500px 9개 뷰포트에서 실제 컴퓨티드 레이아웃(`getBoundingClientRect`/`getComputedStyle`/`scrollWidth`)을 측정해 768px 정확한 전환(하단 고정 탭바 ↔ 상단 탭)과 1280px 본문 1200px 중앙 정렬(x좌표까지 정확히 일치)을 실측으로 확정했다(TC-009/010). 코디네이터가 의심하라고 지시한 두 가지 범위 판단 — (1) 포커스 트랩을 "모바일 바텀시트(`ConditionFilterPanel`, UNIT-07 범위)"에 한정한 판단, (2) `sm`/`lg` 브레이크포인트를 콘텐츠 종속 사유로 미구현한 판단 — 을 각각 `04-ux-design.md` §5-3/§6 원문과 직접 대조해 임의 축소가 아님을 확정했다(TC-026/027). 플레이스홀더 라우트(`/screener`,`/stocks`)가 404 없이 존재하고(TC-005) "미구현 상태"를 명확한 텍스트로 안내함(빈 화면 아님, TC-028)을 확인했으며, 정식 `EmptyState` 컴포넌트 미사용은 설계서에 이 시나리오 자체가 정의되어 있지 않아 위반이 아니라고 판단하되 후속 유닛(UNIT-06/07) 참고사항으로 남겼다. UNIT-04 회귀(면책 배너·금지표현 린트)는 신규 라우트/카피 파일에 실제로 금지어를 2회(카피 리소스, 신규 라우트 소스 파일 각각) 주입해 탐지·원복을 직접 재현해 확인했다(TC-018/019). 위험 기반으로 AC 범위를 넘어 nav 밖 라우트(`/about`)·완전 미존재 라우트(404)·prefix 충돌 라우트(`/screenerxyz`)·미구현 하위 동적 라우트(`/stocks/005930`) 4종에서 `isActive()`의 오탐/크래시 여부를 추가로 스윕했으나 결함을 발견하지 못했다(TC-022~025). **신규 결함 0건. 종합 판정 PASS**(조건부 아님, 다음 단계 07 통합테스트 진행 가능). 테스트 과정에서 CLI 헤드리스 스크린샷의 캡처 타이밍 아티팩트를 발견했으나 CDP 실측+CDP 자체 스크린샷 교차검증으로 제품 결함이 아님을 확정했다(방법론 기록, TC-011). 상세는 `unit-05-test.md` §4~§9, `verify-log_unit-05-test.md` 참조.
> **5단계 갱신(UNIT-06, 2026-09-16)**: `docs/harness/units/unit-06-note.md` 작성 완료에 따라 REQ-002의 "구현 상태" 컬럼을 "5단계 구현 완료 — 6단계 단위테스트 대기"로 갱신했다. **이번 유닛이 이 프로젝트에서 처음으로 Derivation Batch(`services/derivation_batch/`, raw_internal→public_serving 경계를 넘나드는 유일한 컴포넌트, §1-2)를 구현했다.** 구현 내용: (1) `public_serving.derived_metrics_daily`/`current_published_batch`(마이그레이션 0006, GRANT 포함) 신설 — `derived_metrics_daily`에는 원본 시세 컬럼이 존재하지 않고 `per_raw`/`pbr_raw`/`market_cap_raw_krw`는 API 응답 화이트리스트(Pydantic 스키마에 필드 자체를 선언하지 않음)에서 항상 제외됨(§4-3 원칙 실제 구현), (2) `return_rank_pct`(내림차순 순위/총계 백분위) 등 표준경쟁순위 기반 순위·백분위 계산과 이동평균 괴리율·거래량 이상치 스코어(표본표준편차)를 `services/derivation_batch/compute.py`에 순수 함수로 구현(DB 없이 단위테스트 가능), (3) `GET /api/v1/stocks/{code}/metrics` 신규(REQ-002 API, 404/424/503×2 에러 매핑, 신선도/지연 영업일수 계산 헬퍼 `services/public_api/data_freshness.py` 신설 — 향후 UNIT-07/08 재사용 목적), (4) 프론트엔드 `/stocks/[code]` 실제 화면(04-ux-design.md §2-4 MetricCard 5종, `ErrorState`/`EmptyState`/`LoadingSkeleton` 공통 컴포넌트 최초 구현) — UNIT-05가 만든 GlobalNav/Header 셸을 그대로 재사용. **`unit-02-note.md` §2-3이 "세 번째 필요 시점에 shared로 승격 권고"했던 `SqlCalendarRepository` 중복을 이번에 실제로 `shared/calendar_service/sql_repository.py`로 승격**(기존 두 재노출 지점은 하위호환 유지, 회귀 테스트 79건 무변경 재통과로 확인). **raw_internal.raw_ohlcv에 실제 데이터가 없는 상태**(UNIT-02가 API 키 미보유로 검증 못함)에서 로컬 Docker PostgreSQL로 (a) "활성 종목 없음"/(b) "종목은 있으나 원본 시세 없음" 두 실패 경로가 명시적으로 처리됨을 실제 DB로 확인했고, (c) 이 코드 검증 과정에서 `raw_ohlcv.market` 컬럼을 평범한 `String`으로 잘못 선언해 실제 Postgres ENUM 비교 시 `UndefinedFunction` 오류가 나는 실제 결함을 발견해 즉시 수정했다(`raw_models.py`). 이어서 **명시적으로 "테스트 픽스처"로 구분한** `scripts/dev_seed_fixture_metrics.py`(신규, 저장소 커밋 — 개발자 로컬 전용, 운영 파이프라인 아님)로 합성 시계열을 적재해 Derivation Batch 전체 파이프라인(SUCCESS/현재발행포인터 갱신)과 Public API 응답(정상/지연 경고/404/503 DATA_PIPELINE_STALE)을 실제 PostgreSQL+실제 FastAPI+실제 Next.js 프로덕션 빌드로 종단간 검증했다. 상세는 `unit-06-note.md` 참조. **6단계 단위테스트 전까지는 PASS 확정이 아니다.**
> **6단계 갱신(UNIT-06, 2026-09-16)**: `docs/harness/units/unit-06-test.md`(06-unit-tester) 완료에 따라 REQ-002의 "구현 상태"/"단위테스트" 컬럼을 갱신했다. 5단계의 자체 보고를 신뢰하지 않고 실 PostgreSQL·실 HTTP·실 프로덕션 빌드로 독립 재현해 AC-1~AC-10 전 항목과 코디네이터 지시 6개 항목(원본 미노출/방향성/ENUM버그/REQ-006·007/EmptyState·ErrorState/REQ-001 공백 인지)을 전부 확인했다(43개 TC 전부 Pass). 신규 결함 DEF-010(Low, Deferred, Next.js 16 소프트 404 트레이드오프, AC 비위반)만 발견되어 배포 차단 사유가 아니다. **종합 판정 PASS**(다음 단계 07 통합테스트 진행 가능). 상세는 `unit-06-test.md` 참조.
> **하네스 v5 버전 업그레이드 갱신(2026-09-17)**: 하네스가 12단계(규칙 A~J)에서 13단계(규칙 A~K, 11단계 문서화/인수인계 신설)로 업그레이드됨에 따라 REQ-022의 "12단계 배포" 참조를 정정하고, 신규 규칙 J(AI/LLM 기능 내장 대응) "비해당" 판정을 REQ-023으로 신규 등록했다. 이미 완료된 UNIT-01~06의 구현 상태/단위테스트/통합테스트 판정 이력(위 narrative 전체)은 소급 수정하지 않는다 — 각 항목이 기록된 시점의 단계 번호(예: "9~11단계") 그대로 유지하는 것이 역사적으로 정확하다. 근거는 `decisions.md` DEC-021, `02-planning.md` v5 참조.
> **6단계 v2 재검증 갱신(UNIT-04, 최종 — DEF-001/DEF-002 Fixed 확인, 규칙 F 피드백 루프 종료, 2026-09-16)**: `docs/harness/units/unit-04-test.md` v2(06-unit-tester)가 5단계의 DEF-001/DEF-002 조치 결과를 **독립적으로 재검증**했다(5단계 자체 보고를 신뢰하지 않음). **DEF-002**: v1이 발견한 것과 동일한 시나리오("손실보\n전" 실제 줄바꿈 분리)뿐 아니라, 들여쓰기 혼합 3줄 분리("확실한")·정규식 패턴이 탭/개행에 걸쳐 분리된 경우("PER/PBR...")까지 변형해 전부 exit 1로 탐지됨을 확인해 특정 사례 하나만 땜질된 것이 아니라 구조적으로 해결됐음을 확정. 정규화 로직이 과민 반응하지 않는지도 확인하기 위해 자연스러운 문단 줄바꿈이 있는 정상 카피(11줄)와 인접 단어 경계 근접 케이스를 별도로 주입해 오탐 0건을 확인. 기존 단일 라인 금지어("매수신호") 회귀도 재확인. **DEF-002 Fixed 확정**. **DEF-001**: Node `--experimental-strip-types`로 `formatKstDateTime`을 직접 실행해 빈 문자열/malformed 문자열/범위초과 날짜/공백 문자열 4종 전부 예외 없이 폴백("기준시각 확인 불가") 반환을 확인하고 정상 ISO 입력의 회귀 없음도 확인, 나아가 실제 `DataFreshnessBadge` 컴포넌트를 임시 QA 라우트에 렌더링(`npm run build`/`start`)해 4개 malformed/empty 조합이 실제 화면에서도 크래시 없이(빌드 성공, HTTP 200) 기대 폴백 텍스트를 표시함을 확인(v1에는 없었던 컴포넌트 레벨 검증). **DEF-001 Fixed 확정**. 회귀 확인: REQ-007 배너/about 렌더링, `DISCLAIMER_TEXT` 문자 단위 일치, CSS `overflow-wrap` 유지, `npm run lint`/`npx tsc --noEmit`/`npm run build`(정상 11개 파일)/`ruff check .`/`pytest tests/unit`(79 passed, 신규 실패 없음) 전부 재확인해 회귀 없음. **신규 결함 2건 발견(둘 다 AC 범위 밖, 위험 기반 테스트로 6단계가 능동적으로 추가 발견)**: **DEF-008(Low, Open)** — `formatKstDateTime`이 TypeScript 타입 계약을 우회하는 런타임 `null`/`0` 값을 받으면 예외도 폴백도 아닌 잘못된 날짜("1970-01-01 09:00", Unix epoch)를 반환함. 현재 실제 호출부(`DataFreshnessBadge.tsx`)는 `session_close_at`을 호출 전 null 분기 처리하고 `generated_at`은 백엔드가 non-Optional로 강제해 정상 계약 하에서는 발생하지 않으나, UNIT-06~08이 실 데이터 연결 시 타입 단언을 남용하면 재현 가능 — 방어 코드 보강 권고. **DEF-009(Low, Open)** — 금지어 스캐너가 소스 텍스트를 그대로 읽어 JS 문자열 이스케이프 시퀀스(실제 줄바꿈이 아니라 백슬래시+`n` 텍스트)를 해석하지 않으므로, 이스케이프로 분리된 금지어는 여전히 탐지 실패(DEF-002의 회귀가 아니라 원래부터 존재했던 블랙리스트 텍스트 매칭 방식의 별개 한계, JSON 카피 리소스 특성상 즉시 발생 가능성은 낮음). **종합 판정: PASS(조건부 아님)** — DEF-001/DEF-002가 AC 신규 불릿 기준으로 완전히 해소됐고, 신규 DEF-008/DEF-009는 Low·AC 비위반·현재 영향 없음을 근거와 함께 명시해 이 저장소의 기존 판정 관행(UNIT-01 DEF-004, UNIT-02 DEF-005, UNIT-03 DEF-006/007과 동일하게 Low·Open은 PASS를 막지 않음)과 일관되게 최종 PASS로 판정했다. 상세는 `unit-04-test.md` v2 §4-2/§6/§8, `verify-log_unit-04-test.md`(3차 검증 라운드) 참조.

| REQ-ID | 요구사항/기능 (기획서 §) | 설계 매핑 (설계서 §) | 작업 단위 (unit-n) | 구현 상태 | 단위테스트 (unit-n-test) | 통합테스트 (feature-x) | 전체테스트(08) | 비고 |
|--------|--------------------------|------------------------|----------------------|------------|----------------------------|---------------------------|------------------|------|
| REQ-001 | 종목 마스터 조회/검색 (02-planning §4-1) | 03-system-design §3-2(`public_serving.stock_master`), §4-2(`GET /api/v1/stocks`) | UNIT-03(백엔드, 완료), **UNIT-09(프론트엔드 화면, 신규 — DEC-019)** | **백엔드(UNIT-03): 6단계 단위테스트 완료 — PASS(결함 2건, 전부 Low/Open, AC 비위반)** | `unit-03-test.md`: AC-1~AC-6 전 항목 독립 재검증 PASS(TC-001~TC-044). 코디네이터 지시 5개 항목 전부 확인: ①`itmsNm`/`mrktCtg` 필드 존재를 data.go.kr 공식 Swagger 원문으로 직접 재대조(TC-013), `mrktCtg` 값이 영문(KOSPI/KOSDAQ/KONEX)이라는 추가 근거도 발견(TC-014), ②GRANT/`updated_at`을 완전 초기화(`downgrade base`) 후 재구성해 재현하고 `api_service`의 INSERT/UPDATE/DELETE 3종 전부·`updated_at`을 note와 다른 오염값(카카오)으로 독립 재현(TC-016~024, DEF-003/004급 재발 없음 확정), ③`listing_date` nullable 처리가 03-system-design §3-2/04-ux-design §2-3 원문 대조 결과 REQ-001 핵심 화면(종목명·코드·시장구분만 필요)과 충돌 없음 확정(TC-038/039), ④`market` 필터가 03-system-design §4-2 원문과 정확히 일치함을 확인(TC-037)하고 검색 엣지케이스(빈 문자열/공백/대소문자/특수문자/SQL 인젝션)를 스윕하는 과정에서 **LIKE 와일드카드(`%`,`_`) 미이스케이프 결함(DEF-006, Low, Open)을 신규 발견**(TC-033/034), ⑤`pytest tests/unit`(79건)·`ruff check .` 회귀 없음 확인(TC-002/003/043), UNIT-01/02 DB 권한 경계가 완전 재구성 후에도 유지됨을 확인(TC-044). **DEF-007(Low, Open)**: `is_active`가 매 upsert마다 무조건 `true`로 재설정되어 수동 상장폐지 처리가 다음 시드 실행으로 되돌아갈 수 있음이 문서화되어 있지 않음(코드 결함 아님, 문서화 미비). 두 결함 모두 AC 위반 아니며 배포 차단 사유 아님 — **종합 판정 PASS**(다음 단계 07 통합테스트 진행 가능). | | | 다른 기능의 공통 기반. 시드 데이터 출처는 UNIT-02 데이터 소스(`getStockPriceInfo`의 `itmsNm`/`mrktCtg`) 재사용으로 확정(`unit-03-note.md` §0, 6단계가 공식 Swagger 원문으로 재확인). `listing_date`는 출처 미확정으로 nullable 처리(§8-2 항목8 sector와 동일 원칙, 6단계가 REQ-001 화면 요구사항과 무충돌 확정). DEF-006/007 상세는 `unit-03-test.md` §6 참조 |
| REQ-002 | **(v3 재정의)** 종목별 가공 지표 요약 조회 — 등락률 순위·이동평균 대비 괴리율·거래량 이상치 스코어 등 계산된 파생 지표 제공. 원본 OHLC 시세 원문을 표/차트로 그대로 재게시 금지 (02-planning §4-1, §4-3) | 03-system-design §1-1(3중 방어 아키텍처)·§1-2(Derivation Batch), §3-1-1(v3 신규 — `market` 필드가 상장시장(KOSPI/KOSDAQ)을 의미함을 표준화), §3-2(`derived_metrics_daily`, 원본 컬럼 미존재, v3에서 `return_rank_pct` 방향성/계산식 확정 및 `per_raw`/`pbr_raw`/`market_cap_raw_krw`↔`per_percentile`/`pbr_percentile`/`market_cap_percentile` 이원 컬럼 구조 신설), §4-2(`GET /stocks/{code}/metrics`, v3에서 응답 필드명을 `per`/`pbr`/`market_cap`→`per_percentile`/`pbr_percentile`/`market_cap_percentile`로 변경), §4-3(응답 스키마-DB 스키마 연결, v3에서 `*_raw` 컬럼 화이트리스트 제외 규칙 명문화) | UNIT-06 | **6단계 단위테스트 완료 — PASS(결함 1건, Low/Deferred, AC 비위반)** | `unit-06-test.md`: AC-1~AC-10 전 항목 독립 재검증 PASS(TC-001~TC-043). 5단계의 자체 보고("112개 pass", "market ENUM 버그 발견/수정", "픽스처 종단간 검증")를 신뢰하지 않고 독립 재현했다: ①원본 시세/원시값(`per_raw`/`pbr_raw`/`market_cap_raw_krw`) 미노출을 스키마(TC-007)·Pydantic 타입 계약(TC-008)·실제 런타임 API 응답(TC-009)·프론트엔드 화면(TC-025) 네 겹 모두에서 실측 확인, ②`return_rank_pct` 방향성/percentile 계산을 5단계 note를 재사용하지 않고 6단계가 직접 생성한 2,500종목 합성 데이터로 03-system-design.md §3-2 워크드 예제(125/2500*100=5.0)와 동률 표준경쟁순위(1,1,3 패턴)를 재계산해 정확히 일치 확인(TC-010/011), ③`raw_ohlcv.market` ENUM 버그 수정을 실제 빈 DB 상태에서 재현해 `UndefinedFunction` 재발 없음 확인(TC-013/014), ④REQ-006(`DataFreshnessBadge` 실 데이터 연결·지연 경고)/REQ-007(면책배너)이 정상 화면(TC-024)·404(TC-026)·EmptyState(TC-028) 3개 상태 전부에서 정상 렌더링됨을 실 프로덕션 빌드+실 백엔드로 확인, ⑤AC-6~AC-9 전체를 실 PostgreSQL/실 HTTP/실 브라우저 렌더링으로 재현해 5단계 note §7의 수치·문구와 완전히 일치함을 확인(TC-012~TC-028), ⑥`pytest tests/unit`(112건)·`ruff check .`·프론트엔드 `tsc`/`lint`/`build` 전부 독립 재실행해 회귀 없음 확인(TC-001~002/041/042). **신규 결함 1건**: **DEF-010(Low, Deferred)** — `/stocks/{code}`가 `loading.tsx`(Suspense 경계) 존재로 `notFound()` 호출 시 실제 HTTP 상태가 200으로 응답됨(Next.js 16 공식 문서화된 트레이드오프, `noindex` 자동 부여로 SEO 영향은 완화됨, TC-027 실측). AC-9가 HTTP 상태 코드를 요구하지 않아 AC 위반 아님 — 코드 버그가 아니라 `loading.tsx`(로딩 스켈레톤 UX) vs 정확한 HTTP 상태 코드 사이의 사업적 트레이드오프로 판단해 배포 차단 사유로 취급하지 않되, 9단계(보안/SEO)와 운영 모니터링 설계 시 재확인 필요 항목으로 인계. REQ-001 시퀀싱 공백(코디네이터 지시)은 6단계가 해결 대상이 아니므로 그대로 오케스트레이터에 인계(인지만 기록, TC-043). **종합 판정 PASS**(다음 단계 07 통합테스트 진행 가능). 상세는 `unit-06-test.md` §4~§9, `verify-log_unit-06-test.md` 참조. | | | 1단계 핵심기능(1). v2 "가격/거래량 흐름 요약(원본)"에서 데이터 라이선스 제약(`decisions.md` DEC-004, DEC-006)으로 재정의. v3에서 `04-ux-design.md` §7-1 Q1/Q4/Q5 대응(`decisions.md` DEC-011, DEC-014). **UNIT-06이 이 REQ를 구현**: Derivation Batch(`services/derivation_batch/`) 최초 구현, `public_serving.derived_metrics_daily`/`current_published_batch`(마이그레이션 0006), `GET /api/v1/stocks/{code}/metrics`, 프론트엔드 `/stocks/[code]`(04-ux-design.md §2-4). `current_published_batch.market`은 설계서 §3-2가 명시하지 않은 축(상장시장 vs 거래소 세션)을 이 유닛이 거래소 세션(KRX/NXT)으로 확정(근거는 `unit-06-note.md` §2). PER≤0 결측 처리(§3-2)의 실제 구현 책임은 Ingestion Batch(UNIT-02, raw_fundamentals 미적재로 현재 미검증) 소관— 6단계 2차 검증에서 발견, `unit-06-test.md` §7 참조. **REQ-001 시퀀싱 공백**: `/stocks`(검색 목록) 프론트엔드를 배정받은 유닛이 아직 없어 UNIT-05 플레이스홀더 상태로 남아 있음 — 오케스트레이터가 배정 유닛을 결정해야 함(unit-06-note.md §8, unit-06-test.md §7). 상세는 `unit-06-note.md`, `unit-06-test.md` 참조 |
| REQ-003 | **(v3 재정의)** 조건 기반 스크리닝 — 다중 조건 조합 필터링, 결과 화면은 원본 시세 원문 대량 나열 없이 조건 충족 여부·근거 가공 지표 값 위주로 표시 (02-planning §4-1, §4-3) | 03-system-design §3-1-1(v3 신규 — `market` 필드 표준화), §3-2(`derived_metrics_daily`), §4-2(`GET /api/v1/screen`, 화이트리스트 응답, v3에서 `market` 필터 파라미터 신규 추가·`matched_metrics`가 필터∪`sort_by` 지표를 포함하도록 확정·`sort_by` 기본값 `return_pct` 명시), §4-3, §5-1(인덱스 설계, v3에서 `market`/`*_raw` 컬럼 반영 갱신) | UNIT-07 | Not Started | | | | 1단계 핵심기능(2), "추천"에서 리프레이밍 + v3 데이터 가공 원칙(`decisions.md` DEC-004) 반영. v3(설계서 v3)에서 `04-ux-design.md` §7-1 Q2/Q3/Q5 대응(`decisions.md` DEC-012, DEC-013, DEC-014) |
| REQ-004 | **(v3 재정의)** 전일(직전 거래일) 시장 동향 리포트 — 개별 종목 원본 시세 나열이 아닌 상승/하락 종목 수·거래대금 상위 업종 등 가공된 요약 통계 중심 (02-planning §4-1, §4-3) | 03-system-design §3-1-1(v3 신규 — `market_summary_daily.market`이 상장시장(KOSPI/KOSDAQ/ALL)임을 표준화), §3-2(`public_serving.market_summary_daily`, v3에서 `market` enum에 `ALL` 추가 및 서버측 직접 집계 방식 확정, 금액 필드 `total_trading_value_krw`/`trading_value_krw`로 단위 명시), §4-2(`GET /api/v1/market-summary`, v3에서 `market` 파라미터 생략 시 기본값 `ALL` 확정 및 `by_market` 응답 구조 신설) | UNIT-08 | Not Started | | | | 1단계 핵심기능(3), MoSCoW: Should. v3 데이터 가공 원칙(`decisions.md` DEC-004) 반영. v3(설계서 v3)에서 `04-ux-design.md` §7-1 Q1/Q6/Q7 대응(`decisions.md` DEC-011, DEC-015, DEC-016) |
| REQ-005 | 직전 거래일 산정 로직 — "어제"=직전 거래일, 휴장일/연휴 반영 (02-planning §4-1) | 03-system-design §3-3(v4 재작성 — `get_last_trading_day()`를 마감 시각(`session_close_at`) 기준으로 전면 수정, 실행 가능한 pseudocode·경계 시각별 진리표 신설, `market` 파라미터는 거래소 세션 구분(KRX/NXT)임을 §3-1-1에서 명확화), §3-2(`reference.market_calendar`), §4-1(424 `CALENDAR_NOT_CONFIRMED` 에러 처리), §7-2(v4 — 배치 신선도 알림 cron을 §3-3 수정과 정합되게 갱신) | UNIT-01 | **Done — 6단계 v4 최종 재검증 완료, PASS(조건부 아님)** | `unit-01-test.md`(**v4, 최종**): DEF-001(High)/DEF-002(Low)/DEF-003(Medium)/DEF-004(Low) **전부 Fixed로 최종 확인**. 6단계는 5단계의 자체 검증 결과(note §0-b)를 신뢰하지 않고, DB를 완전히 초기화한 뒤 처음부터 재구성해 GRANT 자동 반영을 재현하고(TC-075), `api_service` 거부를 5단계보다 넓은 범위(UPDATE/INSERT/DELETE)로 재현하고(TC-076), 5단계와 다른 오염값으로 `updated_at` 실제 갱신을 재현했다(TC-079). `pytest tests/unit`(36건)·`ruff check .`·실제 Postgres 기반 Public API 엔드투엔드(TC-083)로 DEF-001/002 회귀 없음 및 기존 AC 전체 회귀 없음도 확인. AC-1~AC-6 전 불릿 확정 PASS. 신규 결함 0건. 남은 리스크(raw_internal 실제 재검증은 UNIT-02 이관, uvicorn 실기동 미검증, 역할 프로비저닝 문서화 공백, `updated_at` 매번 갱신 특성)는 결함이 아니라 후속 과제로 인계. | 없음 | | 규제 민감 도메인 대응(규칙 I) — 도메인 운영규칙, 1단계 §6-4. 구현 상세는 `docs/harness/units/unit-01-note.md`(v3), 테스트 상세는 `docs/harness/units/unit-01-test.md`(v4, 최종) 참조 |
| REQ-006 | 데이터 기준시각 표기 (전 화면, NXT/KRX 마감 다원화 반영) (02-planning §4-1) | 03-system-design §3-1-1(v3 신규 — 거래소 세션 구분 KRX/NXT가 상장시장 구분과 다른 축임을 표준화), §3-2(`market_calendar.session_close_at` 시장별), §3-4(`meta.data_freshness` 공통 메타데이터 설계), §4-1(공통 응답 envelope 강제) | UNIT-04 | **Done — 6단계 v2 최종 재검증 완료, PASS(조건부 아님)** | `unit-04-test.md`(**v2, 최종**): AC-2(5개 불릿 + DEF-001 재발 방지 신규 불릿) 전부 독립 재현 PASS(TC-013~017, TC-042~043). **DEF-001(Medium) Fixed 확정**: Node `--experimental-strip-types`로 `formatKstDateTime`을 직접 실행해 빈 문자열/malformed 문자열/범위초과 날짜/공백 문자열 4종 전부 예외 없이 폴백("기준시각 확인 불가") 반환 확인, 정상 ISO 입력 회귀 없음 확인, 나아가 실제 `DataFreshnessBadge`를 임시 QA 라우트에 렌더링(`npm run build`/`start`)해 4개 malformed/empty 조합이 실제 화면에서도 크래시 없이(빌드 성공, HTTP 200) 폴백 텍스트를 표시함을 확인(v1에는 없었던 컴포넌트 레벨 검증, TC-043). **신규 DEF-008(Low, Open, AC 범위 밖 위험 기반 발견)**: TypeScript 타입 계약을 우회하는 런타임 `null`/`0` 값을 받으면 예외도 폴백도 아닌 잘못된 날짜("1970-01-01 09:00")를 반환(TC-044/045) — 현재 실제 호출부는 이 경로에 도달하지 않으나(코드로 확인), UNIT-06~08 실 데이터 연결 시 방어 보강 권고. | | | 프론트엔드 컴포넌트는 아직 실제 데이터 화면(UNIT-06/07/08)에 연결되지 않음 — 연결 시점에 실사용 검증 및 DEF-008 방어 보강 필요. 규제 민감 도메인 대응(규칙 I) — 도메인 운영규칙, 1단계 §6-2/§6-6 |
| REQ-007 | 투자자문 아님 면책 문구 전 화면 상시 노출 (02-planning §4-1) | 03-system-design §6-4(프론트엔드 루트 레이아웃 강제 노출 + `meta.disclaimer` 공통 응답 필드) | UNIT-04 | **6단계 단위테스트 완료 — PASS(결함 0건)** | `unit-04-test.md`: AC-1(6개 불릿) 전부 독립 재현 PASS(TC-006~012). `npm run build && npm run start`로 실제 프로덕션 렌더링을 띄워 `/`·`/about` 양쪽에서 sticky 배너 마크업·정확한 문구·닫기 버튼 완전 부재(grep 0건, `<button` 태그 자체 없음)·말줄임 부재(컴파일된 CSS 청크에 `text-overflow` 없음, `white-space:normal`만 존재)를 직접 확인(TC-006~009). `DISCLAIMER_TEXT`(`envelope.py`)와 `copy.ko.json`의 `disclaimer.banner`/`footerFull`을 Python으로 문자 단위 등가 비교해 전부 `True` 확인(TC-010). 푸터 이중 노출(TC-011), `/about` 6개 `<h2>` 전부 정확한 문구·순서로 확인(TC-012). **v2(2026-09-16)**: DEF-001/DEF-002 재작업에 따른 회귀 재확인(`unit-04-test.md` v2 TC-046~048) — 배너/about 렌더링, `DISCLAIMER_TEXT` 일치, CSS `overflow-wrap` 전부 회귀 없음 재확인. | | | 규제 민감 도메인 대응(규칙 I), 1단계 §5-2(5). 9단계에서 미반영 시 Critical 결함 취급 — 이번 단위테스트로 실제 렌더링 기준 미반영 없음을 확인 |
| REQ-008 | 금지표현 가이드라인 ("추천"/"매수신호"/"손실보전"/"수익보장" 등 금지) (02-planning §4-1) | 03-system-design §6-4(중앙 카피 리소스 파일 `copy.ko.json` + CI 금지어 정규식 검사, 위반 시 빌드 실패) | UNIT-04 | **Done — 6단계 v2 최종 재검증 완료, PASS(조건부 아님)** | `unit-04-test.md`(**v2, 최종**): AC-3(4개 불릿 + DEF-002 재발 방지 신규 불릿) 전부 독립 재현 PASS(TC-019~026, TC-034~040). **DEF-002(Medium) Fixed 확정**: v1과 동일한 시나리오("손실보\n전" 실제 줄바꿈 분리)뿐 아니라 들여쓰기 혼합 3줄 분리("확실한")·정규식 패턴이 탭/개행에 걸쳐 분리된 경우("PER/PBR...")까지 변형해 전부 exit 1로 탐지됨을 확인(TC-034~036), 자연스러운 문단 줄바꿈이 있는 정상 카피와 인접 단어 경계 근접 케이스에서 오탐 0건 확인(TC-038/039), 기존 단일 라인 금지어 회귀 없음 확인(TC-040). **신규 DEF-009(Low, Open, AC 범위 밖 위험 기반 발견)**: 금지어 스캐너가 JS 문자열 이스케이프 시퀀스(실제 개행이 아닌 백슬래시+`n` 텍스트)는 여전히 해석하지 못해 탐지 실패(TC-041) — DEF-002의 회귀가 아니라 블랙리스트 텍스트 매칭 방식 자체의 별개 한계이며, 현재 JSON 카피 리소스 특성상 즉시 발생 가능성은 낮음. | | | 현재 스캔 대상은 `frontend/src/` 하위 `.ts`/`.tsx`/`.json`뿐 — 향후 유닛이 새 카피 파일을 다른 위치에 추가하면 스캔 범위 갱신 필요. DEF-009(이스케이프 시퀀스 우회)는 AST 기반 파싱으로 근본 해결 가능 — 후속 개선 과제로 인계. 규제 민감 도메인 대응(규칙 I), 1단계 §5-2(1)(4). 9단계에서 미반영 시 Critical 결함 취급 — 이번 재검증으로 실제 CI 게이트 미반영 없음을 확인 |
| REQ-009 | 불특정 다수 대상 비로그인 동일 정보 제공 원칙, 1:1 맞춤 조언 기능 배제 (02-planning §4-1) | 03-system-design §4-1(사용자 식별 파라미터 금지 목록), §6-1(인증/세션 없음, 요청자 무관 동일 응답) | UNIT-04 | **6단계 단위테스트 완료 — PASS(결함 0건)** | `unit-04-test.md`: AC-4(2개 불릿) 독립 재확인(TC-028~029) + 코디네이터 지시에 따른 **보강 검증**(TC-030~031). 5단계의 grep 기반 정적 검증은 "코드에 그런 코드가 없다"만 증명할 뿐 "실제로 요청자를 구분하지 않는다"는 런타임 사실은 증명하지 못한다고 판단해, FastAPI `TestClient`로 동일 쿼리에 `Authorization` 헤더 + `session_id`/`user_id` 쿠키를 붙인 요청과 붙이지 않은 요청의 응답 바디가 완전히 동일함을 실제 HTTP 계층에서 증명(TC-030), 알 수 없는 `user_id` 쿼리파라미터를 추가해도 무시되고 동일 응답이 반환됨을 확인(TC-031). **v2(2026-09-16)**: 이번 재작업이 백엔드 코드를 전혀 건드리지 않았음을 `git status`/`git diff`로 확인했고, `pytest tests/unit`(79건) 전체 재실행으로 회귀 없음을 재확인. | N/A(코드 구현 항목 아님) | | 신규 API가 추가되는 UNIT-06/07/08에서도 TC-028~031과 동일한 검증(정적 파라미터 검토 + 블랙박스 헤더/쿠키 동일성 테스트)을 반복해야 함(회귀 위험, `unit-04-test.md` §7에 명시). 규제 민감 도메인 대응(규칙 I), 1단계 §5-2(2). 9단계에서 미반영 시 Critical 결함 취급 |
| REQ-010 | 무료 공개 서비스 운영 정책 (유료화 시 유사투자자문업 신고 재검토 트리거 명문화) (02-planning §4-1) | 03-system-design §6-4(결제/광고 SDK 의도적 미포함 + 재검토 절차 문서화), §8-1(트레이드오프) | UNIT-04 | **6단계 단위테스트 완료 — PASS(결함 0건)** | `unit-04-test.md`: AC-5 독립 재확인(TC-032~033). `frontend/package.json`/`requirements.txt`/`requirements-dev.txt`를 `stripe`/`iamport`/`toss`/`kakaopay`/`adsense`/`admob`/`paypal`/`coupang` 등으로 재검색해 매칭 0건 확인(5단계보다 확장된 키워드 목록으로 재검증). **v2(2026-09-16)**: 재작업이 이 REQ와 무관함을 확인, 별도 재검증 불필요(회귀 위험 없음). | N/A(코드 구현 항목 아님) | | 규제 민감 도메인 대응(규칙 I), 1단계 §5-2(3)(6). 9단계에서 미반영 시 Critical 결함 취급 |
| REQ-011 | 데이터 소스 연동 파이프라인 (공공데이터포털 금융위원회_주식시세정보 API, 지연 데이터 처리) (02-planning §4-1) | 03-system-design §1-2(Ingestion Batch), §2-2(약관/호출한도 확인 근거 승계), §3-2(`raw_internal.raw_ohlcv`/`raw_fundamentals`), §5-4(재시도/서킷브레이커) | UNIT-02 | **6단계 단위테스트 완료 — PASS(결함 0건, DEF-005는 문서 정확도 이슈로 배포 차단 아님)** | `unit-02-test.md`: AC-1~AC-6 전 항목 독립 재검증 PASS(TC-001~TC-035). 5단계 보고를 신뢰하지 않고 GRANT/권한분리를 실제 `raw_internal` 테이블 대상 직접 재현(TC-018/019), 정상 응답 파싱 가정 필드를 data.go.kr 공식 Swagger로 교차검증(TC-021), 재시도/서킷브레이커를 실제 시간·실제 네트워크로 종단간 재현(TC-023/024), UNIT-01 `get_last_trading_day()` 재사용 회귀 없음 확인(TC-015). **DEF-005(Low, Open)**: 시가총액(`mrktTotAmt`)이 실제로는 `getStockPriceInfo`에서 제공됨을 발견(note의 "확인 못함" 서술보다 구체적) — raw_fundamentals 적재는 여전히 이번 유닛 범위 밖(AC 대상 아님), 후속 유닛 설계 참고사항으로 인계. | | | `decisions.md` DEC-001, DEC-004 참조, 1단계 §5-3/§8-3. 소스 채택은 유지, 상업적 이용 전제는 DEC-004로 수정됨. **실 서비스키 미보유로 정상 응답 파싱은 실제 API로 검증 못 함(게이트웨이 오류 응답 형태는 실 엔드포인트 호출·6단계 재실측 양쪽으로 확인). raw_fundamentals(PER/PBR) 적재 로직은 REQ-011의 의도적 부분 범위 축소로 이번 유닛에서 구현하지 않음 — PER/PBR은 소스 필드 자체가 미제공으로 확인(6단계 TC-022), 시가총액은 제공되나 이번 유닛에서 적재 로직을 만들지 않기로 한 것은 5단계의 범위 판단(YAGNI)이지 소스 불확실성 때문이 아님. `unit-02-note.md` §2, `unit-02-test.md` §6 DEF-005 참조.** |
| REQ-012 | 휴장일/특수 개장일 캘린더 관리 구조 (연 1회 이상 갱신 가능, 하드코딩 금지) (02-planning §4-1) | 03-system-design §3-2(`reference.market_calendar`), §3-3(CLI 스크립트 기반 데이터 갱신 절차, 코드 배포와 분리) | UNIT-01 | **Done — 6단계 v4 최종 재검증 완료, PASS(조건부 아님)** | `unit-01-test.md`(v4, 최종): AC-2(YAML 파싱)·AC-3(CLI 실행, 전 불릿) 확정 PASS 유지. DEF-003(GRANT 미코드화)은 5단계가 신규 리비전 `0002_grant_reference_privileges.py`로 수정했고, 6단계가 완전 초기화 후 재구성(TC-075)·`api_service` 거부 범위 확장 재현(TC-076)으로 독립 재검증해 **Fixed 확정**. DEF-004(`updated_at` 미갱신)도 5단계의 `set_={"updated_at": func.now()}` 추가를 6단계가 다른 오염값으로 재현해(TC-079) **Fixed 확정**. `pytest tests/unit`(36건)·`ruff check .` 회귀 없음(TC-082). REQ-012의 핵심 운영 절차(연 1회 이상 CLI 갱신)가 역할 분리 프로덕션 환경에서도 정상 동작함을 실제 DB로 확정 검증했다. | 없음 | | 규제 민감 도메인 대응(규칙 I) — 도메인 운영규칙, 1단계 §6-5. 구현 상세/편차/인수조건은 `docs/harness/units/unit-01-note.md`(v3) 참조, 테스트 상세는 `docs/harness/units/unit-01-test.md`(v4, 최종) 참조 |
| REQ-013 | 반응형(모바일 우선) 웹 UI (02-planning §4-1) | 03-system-design §2-1(Next.js+TypeScript 채택, `decisions.md` DEC-009), 04-ux-design.md §6(브레이크포인트 표)·§5(WCAG AA 접근성) | UNIT-05 | **Done — 6단계 단위테스트 완료, PASS(조건부 아님)**. `GlobalNav`(홈/조건 스크리닝/종목 검색, `aria-current="page"`) 신설, `Header.tsx` 확장(UNIT-04가 남긴 최소 헤더에 결합), 04-ux-design.md §6 브레이크포인트(768px 기준 모바일 하단 탭바 ↔ 데스크톱 상단 탭, 1280px 본문 최대폭 1200px) CSS 반영, `/screener`·`/stocks` 플레이스홀더 라우트 신설(404 방지, 실제 화면은 UNIT-06/07). 상세는 `unit-05-note.md` 참조 | `unit-05-test.md`: AC-1~AC-7(20개 불릿) 전 항목 독립 재현 PASS(TC-001~031). 실제 헤드리스 Chrome(CDP)으로 9개 뷰포트(320~1500px) 실측해 768px/1280px 브레이크포인트 전환을 좌표 단위로 확정(TC-009/010), 포커스 트랩/브레이크포인트 범위 축소 여부를 `04-ux-design.md` 원문과 대조해 임의 축소 아님을 확정(TC-026/027), 금지어를 2개 신규 파일에 실제 주입해 탐지·원복 확인(TC-018/019), 위험 기반 경계 케이스 4종(`isActive()` 오탐/크래시 스윕)에서 결함 미발견(TC-022~025). **신규 결함 0건.** | | UNIT-06~08이 이 `GlobalNav`/`Header`를 그대로 재사용해야 하며 임의로 새 헤더를 만들지 않는다. 플레이스홀더 라우트를 실제 화면으로 교체할 때 정식 `EmptyState` 컴포넌트 명세(04-ux-design.md §4)를 따라야 하며 UNIT-05의 임시 텍스트 패턴을 그대로 승격시키지 않는다(`unit-05-test.md` TC-028/§7 참조) |
| **REQ-022** | **(v3 신규)** 데이터 라이선스 배포 전 공식 확인 게이트 — KRX 공식 서면 확인(또는 KRX Data Marketplace 상담) 또는 자본시장법/저작권법 전문 변호사의 실제 법률 자문 완료 중 최소 하나를 12단계 배포 승인의 필수 선행 조건으로 등록 (02-planning §4-1, §4-3, §6, §7 리스크 4·9) | 03-system-design §7-4 — **의도적으로 코드/화면 설계 없음.** 12단계 배포 승인 체크리스트 항목으로만 명시(8·9·10단계 PASS와 별개의 독립 게이트, 확인 주체는 사용자) | N/A (12단계 배포 게이트, 코드 작업 아님) | Not Started (Blocking Gate) | N/A | N/A | **12단계 배포 승인 체크리스트에서 Pass 확인 필수. Fail/미확인 시 배포 자체 보류.** | `decisions.md` DEC-004 참조. MoSCoW: Must |
| REQ-014 | 실시간 시세/호가창 제공 (02-planning §4-2) | - | - | Out-of-Scope(데이터소스 지연 특성, 초기 규모 목표상 불필요, 별도 라이선스 이슈) | - | - | - | |
| REQ-015 | 1:1 상담/챗봇형 맞춤 투자조언 (02-planning §4-2) | - | - | Out-of-Scope(투자자문업 등록 대상 영역, 규제 회피를 위해 설계에서 배제) | - | - | - | 1단계 §5-2(2) |
| REQ-016 | 로그인/회원가입 및 개인화 관심종목 저장 (02-planning §4-2) | 03-system-design §0, §6-1(인증/인가 모델을 의도적으로 설계하지 않는 근거로만 언급, 별도 인증 시스템 설계 없음) | - | Out-of-Scope(MVP 단순화, 불특정 다수 대상 원칙 유지) | - | - | - | |
| REQ-017 | 유료 구독/프리미엄 스크리닝 및 광고 수익모델 (02-planning §4-2) | - | - | Out-of-Scope(초기 무료 공개 전제, 도입 시 유사투자자문업 신고 재검토 필요) | - | - | - | 1단계 §5-2(3)(6) |
| REQ-018 | 해외주식/해외지수 연동 (02-planning §4-2) | - | - | Out-of-Scope(조사 범위가 국내 코스피/코스닥 한정) | - | - | - | |
| REQ-019 | 주문/매매 체결 연동 (02-planning §4-2) | - | - | Out-of-Scope(정보 제공 서비스로 한정, 증권사 제휴/인가 필요 영역 배제) | - | - | - | 1단계 §5-3 |
| REQ-020 | 커뮤니티/종목토론 게시판 (02-planning §4-2) | - | - | Out-of-Scope(니치 포지셔닝과 배치, 모더레이션 부담 회피) | - | - | - | |
| REQ-021 | 실시간 알림/푸시 (02-planning §4-2) | - | - | Out-of-Scope(배치 갱신 구조와 상충, MVP 범위 아님) | - | - | - | |
| REQ-023 | **(v5 신설)** LLM/AI 생성 콘텐츠 기능(챗봇/자동생성/추천 문구 생성 등) (02-planning §4-2) | - | - | **규칙 J(AI/LLM 기능 내장 대응) 비해당** — REQ-015가 이미 챗봇형 조언을 배제했고, REQ-002~004는 결정론적 계산식이며 생성형 AI 경로 없음 | - | - | - | `decisions.md` DEC-021 참조 |

## 작성 규칙
- REQ-ID는 2단계에서 기획서의 기능 범위(In-Scope) 항목마다 하나씩 부여한다. 이후 단계에서 새 ID를 임의로 추가하지 않고, 새 요구사항이 필요하면 규칙 A에 따라 질문하거나 규칙 F(피드백 루프)로 상위 단계를 수정한다.
- Out-of-Scope로 명시된 항목은 매트릭스에 포함하되 상태를 "Out-of-Scope(사유)"로 표기해, 나중에 "빠뜨린 것"과 "의도적으로 뺀 것"을 구분할 수 있게 한다.
- **REQ-005, REQ-006, REQ-007, REQ-008, REQ-009, REQ-010, REQ-012는 규칙 I(규제 민감 도메인 대응)에 따라 등록된 항목이다.** 9단계 보안/규제 검증에서 이 항목들의 실제 구현/화면 반영 여부를 별도로 확인하고, 미반영 시 Critical 결함과 동일하게 취급한다(규칙 I-3).
- **REQ-022도 규칙 I(규제 민감 도메인 대응)에 따라 등록된 항목이나, 성격이 다르다.** 이는 화면/기능 구현이 아니라 **12단계(배포) 착수 전 별도 확인 절차(게이트)**이므로 5~8단계의 구현/테스트 사이클과 무관하게 존재하며, 8단계 커버리지 체크 시에도 "구현 상태"가 아니라 "12단계 배포 승인 체크리스트에서 확인되었는가"로 판정한다. 8단계에서 이 항목이 Out-of-Scope나 누락으로 처리되어서는 안 된다 — 배포를 막는 실질적 요구사항이다.
- 8단계에서 이 매트릭스를 검토해 커버리지 100%를 확인하지 못하면 PASS 판정할 수 없다(REQ-022 포함).
- "설계 매핑(설계서 §)" 컬럼은 3단계에서 채운다. **3단계(`03-system-design.md` v2, PASS) 완료 시점에 전 REQ-ID(Out-of-Scope 제외, REQ-016은 예외적으로 "설계하지 않음"의 근거를 명시)에 대해 채워졌다.** v3(규칙 F 피드백 루프, `04-ux-design.md` §7-1 Q1~Q7 대응)에서 REQ-002/003/004의 매핑을 갱신했다.
- **REQ-006/REQ-008은 UNIT-04 6단계 v2 재검증(2026-09-16)에 따라 "단위테스트"/"구현 상태" 상태가 최종 PASS(조건부 아님)로 갱신됐다.** v1 CONDITIONAL PASS의 조건이었던 DEF-001(Medium)/DEF-002(Medium)를 5단계가 조치했고, 6단계가 독립 재검증해 둘 다 Fixed로 확정했다. 재검증 과정에서 위험 기반으로 신규 발견한 DEF-008(Low, REQ-006)/DEF-009(Low, REQ-008)는 AC 비위반·현재 실사용 경로 영향 없음을 근거로 최종 PASS를 막지 않되, UNIT-06~08 실 데이터 연결 시 참고하도록 REQ-006/008 비고란에 남겼다. 상세 근거는 `unit-04-test.md` v2 §6/§8, `verify-log_unit-04-test.md`(3차 검증 라운드) 참조.
