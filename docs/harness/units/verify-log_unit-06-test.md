# 내부 검증 로그 (Internal Verification Log)

## 대상 산출물
- 파일: `docs/harness/units/unit-06-test.md`
- 작성 에이전트: `06-unit-tester`
- 버전: v1

## 1차 검증 (작성자 관점 자가 재검토)
- 검증자(역할): 06-unit-tester(작성자 본인, 20년차 QA 관점)
- 일시: 2026-09-16
- 체크리스트
  - [x] 입력 계약(unit-06-note.md §4 AC-1~AC-10)에 명시된 모든 인수 조건을 실제로 반영했는가 — §5 커버리지 표에서 AC-1~AC-10 전부 TC 매핑 확인됨
  - [x] 출력 계약(test-report-template.md)의 모든 필수 항목이 빠짐없이 존재하는가 — 1~9절 전부 채움, "해당 없음" 섹션 없음
  - [x] 상위 단계 산출물과 용어/수치/범위가 모순되지 않는가 — TC-016의 계산값(4.5576/50.0/3.9446/6.7616/604.3645 등)이 unit-06-note.md §7 로그와 정확히 일치함을 재확인
  - [x] 추측으로 채운 항목이 있는가 — DEF-010 판단이 "추측"이 아니라 실측(TC-027, 200 응답 + noindex 확인)에 근거함을 재확인. 판단이 애매한 부분은 §7에 "코디네이터 재지시 바람"으로 열어둠(임의 확정하지 않음)
  - [x] 20년차 실무자 기준으로 봤을 때 구조적/논리적 결함이 없는가 — AC 커버리지, 코디네이터 지시 6개 항목 커버리지 모두 명시적 표로 추적 가능
  - [x] 오탈자, 형식 오류, 누락된 표/그림 참조가 없는가 — 표 렌더링 확인, 섹션 번호 순서 확인
- 발견된 결함 목록:
  1. §5 커버리지 섹션에서 "AC-1→TC-003" 등 매핑 표기가 있으나, AC-2의 4번째 불릿(검증 규칙 5% 경계)에 대한 TC 번호가 처음 초안에서 누락되어 있었음
- 조치 내용: AC-2 매핑에 TC-006을 추가하고, TC-006 설명에 "AC-2 4번째 불릿"임을 명시 → v1 반영 완료(현재 파일에 이미 반영됨)

## 2차 검증 (독립 심사자 관점 — 역할 전환 재검토)
> "내가 이 문서를 오늘 처음 받아본 심사자라면?"이라는 관점에서 재검토.
- 검증자(역할): 07-integration-tester 관점을 가정한 독립 심사자(같은 에이전트가 관점 전환)
- 일시: 2026-09-16
- 체크리스트
  - [x] 1차 검증에서 지적된 항목이 실제로 반영되었는가 — TC-006/§5 매핑 확인, 반영됨
  - [x] 엣지 케이스/예외 상황이 누락되지 않았는가 — 빈/공백 종목코드(TC-033), 잘못된 날짜(TC-032), 인증 헤더 무관성(TC-034), `-0%` 포맷(TC-035) 등 AC 범위 밖 위험 기반 케이스가 포함됨을 확인. 다만 재검토 중 "PER=0(적자기업)일 때 null 처리"가 설계서(§3-2 "PER≤0은 null로 저장")에 명시되어 있는데 이번 테스트가 이 경계를 직접 실측하지 않았음을 발견 — **이 부분은 `compute.py`가 이 규칙을 구현하지 않고 `run_derivation.py`의 `StockDayMetrics.per_raw`가 `fundamentals.per`를 그대로 통과시키는 구조임을 소스 재확인**: 실제로 PER≤0 필터링 로직은 `raw_fundamentals` 적재 단계(Ingestion Batch, UNIT-02 소관)에서 처리되어야 하는 규칙으로, Derivation Batch는 이미 null인 값을 그대로 통과시키는 것으로 §3-2를 재해석하면 이 유닛의 결함이 아니다. `raw_fundamentals` 자체가 아직 실제로 적재되지 않는 상태(UNIT-02 범위 제한, §2 제외범위에 이미 명시)이므로 이 경계는 UNIT-02/07 재검증 시점으로 보류하는 것이 타당하다고 판단해 신규 결함으로 등록하지 않고 §7 리스크에 추가 문구를 보강했다.
  - [x] 문서만 보고 다음 단계(07 통합테스트) 에이전트가 추가 질문 없이 작업을 시작할 수 있는가 — §2 제외범위와 §7 리스크가 충분히 구체적(대상 파일/사유/재검증 시점)이라 판단
  - [x] 되돌리기 어려운 결정(비가역적 결정)에 대한 근거가 명시되어 있는가 — DEF-010은 되돌리기 쉬운 결정(loading.tsx 유무)임을 §7에서 명시적으로 판단해 규칙 A 격상 대상이 아님을 근거와 함께 기록
  - [x] 보안/성능/운영 관점에서 명백히 위험한 내용이 없는가 — TC-022/023(권한 경계), TC-033(빈 입력 방어), TC-034(사용자 식별 불가 원칙 회귀)에서 위험 신호 없음 확인. DEF-010의 운영 모니터링 영향(200으로 은폐될 수 있는 에러율)을 리스크로 명시한 것이 적절함을 재확인
- 발견된 결함 목록:
  1. (문서 완결성) PER≤0 결측 처리 규칙의 이 유닛 책임 범위 경계가 최초 초안에서 불명확했음(위 체크리스트 항목에 기술) — 결함이라기보다 설명 보강 필요 사항으로 판단
- 조치 내용: §7 "PER/PBR/시가총액 percentile 실 규모 미검증" 항목 다음 문단에 "PER≤0 결측 처리는 Ingestion Batch(UNIT-02) 소관이며 raw_fundamentals 미적재 상태라 이 유닛에서 실측 불가"라는 경계 설명을 추가해 v1 최종본에 반영 완료. 신규 결함 번호는 부여하지 않음(코드 결함이 아니라 책임 경계 명확화 사항).

## 최종 판정
- [x] PASS (결함 0건, 최소 2회 검증 완료) — 다음 단계로 handoff 가능
- [ ] FAIL — 재작업 필요, 사유:

**비고**: 최종 판정은 결함 0건이 아니라 DEF-010(Low, Deferred, AC 비위반) 1건이 있는 상태의 PASS다. `unit-06-test.md` §8에 이 근거를 명시했다. 템플릿의 "결함 0건인 경우" 문구는 이번 산출물에는 해당하지 않으며, `unit-06-test.md` §6이 "그 외 결함 없음 — 근거: ..." 형식으로 근거를 남겼다.
