# 내부 검증 로그 (Internal Verification Log)

## 대상 산출물
- 파일: `docs/harness/units/unit-09-test.md`
- 작성 에이전트: 06-unit-tester
- 적용 Tier: High (`decisions.md` DEC-021) — 규칙 B 원문(최소 2회, 1차 결함 0건이어도 2차 생략 불가) 적용
- 버전: v1 → **v2**(규칙 F 피드백 루프 — DEF-U09-01 CORS 조치 회귀 확인)

---

# v1 검증 (이력 보존, 원문 유지)

## 1차 검증 (작성자 관점 자가 재검토)
- 검증자(역할): 06-unit-tester (작성자 자신)
- 일시: 2026-09-18
- 체크리스트
  - [x] 입력 계약(`unit-09-note.md` §3/§4 AC-1~AC-9, 오케스트레이터 지시 최우선 항목 1·2)에 명시된 모든 항목을 실제로 반영했는가 — AC-1~AC-9 전 항목이 §6 표에 TC로 1:1 대응됨을 재확인. 최우선 항목 1(실 계정 종단간+브라우저 상호작용)은 §4에, 최우선 항목 2(§2 편차4 판정)는 §5에 각각 대응 섹션을 두어 반영.
  - [x] 출력 계약(`test-report-template.md` 전 섹션)에 명시된 모든 필수 항목이 빠짐없이 존재하는가 — 1~12절(개요/범위/환경/케이스/커버리지/결함/Teardown/리스크/판정/내부검증) 전부 채움, "해당 없음" 처리 섹션 없음(전부 실질 내용 존재).
  - [x] 상위 단계 산출물과 용어/수치/범위가 모순되지 않는가 — `stock_master` 픽스처 6행 수치가 `unit-09-note.md`/`unit-08-note.md`가 언급한 값과 일치, DEF-006/007 번호가 `unit-03-test.md`/`traceability.md`와 일치, DEF-U09-01 신규 번호가 기존 DEF-001~DEF-010/DEF-U07-01/DEF-U08-01/02와 충돌하지 않음(네이밍 규칙 `DEF-U<유닛번호>-<순번>` 일관 적용).
  - [x] 추측으로 채운 항목이 있는가 — §5의 설계 정합성 판정은 추측이 아니라 04-ux-design.md 원문 대조(§1-2/§1-3/§2-3) 근거를 명시. §4-a의 "차단 기준 추정"(자격증명 문자열 노출 여부)은 "관찰됨"으로 명확히 한정해 단정적 사실 주장과 구분했다.
  - [x] 20년차 실무자 기준으로 봤을 때 구조적/논리적 결함이 없는가 — DEF-U09-01을 발견하고도 "UNIT-09 파일에는 없다"는 이유로 결함을 축소·은폐하지 않고 FAIL로 명시한 것, 근본원인을 UNIT-01/공용기반까지 추적한 것이 규칙 F의 정신에 부합.
  - [x] 오탈자, 형식 오류, 누락된 표/그림 참조가 없는가 — 표 형식·마크다운 문법 재확인.
- 발견된 결함 목록:
  1. (테스트 결과서 자체의 결함 아님) 최초 브라우저 자동화 스크립트(`browser_test.mjs`)의 입력창 클리어 로직(`clickCount:3`)이 신뢰성이 없어 텍스트가 누적되는 스크립트 버그가 있었음 — 이는 산출물(테스트 결과서)의 결함이 아니라 검증 과정 중의 도구 결함이었고, 최종 결과서(§6)에는 교정된 방식(Ctrl+A+Backspace)으로 재실행한 결과만 반영했다. 결과서 자체에는 결함 없음.
- 조치 내용: 해당 없음(결과서 자체에 수정할 결함 없음, v1 그대로 유지)

## 2차 검증 (독립 심사자 관점 — 역할 전환 재검토)
> Tier가 High이므로 1차 결함 0건이어도 이 절 생략 불가(규칙 B 원문 적용).
- 검증자(역할): 06-unit-tester ("오늘 처음 이 결과서를 받아본 심사자"로 역할 전환)
- 일시: 2026-09-18
- 체크리스트
  - [x] 1차 검증에서 지적된 항목이 실제로 반영되었는가 — 스크립트 버그는 결과서 자체와 무관하므로 별도 반영 불필요함을 재확인.
  - [x] 엣지 케이스/예외 상황이 누락되지 않았는가 — 재검토 결과 다음을 추가로 의심하고 확인했다: (a) DEF-U09-01이 테스트 환경(서로 다른 로컬 포트)만의 인위적 산물일 가능성 → `.env.example`의 로컬 개발 기본 설정(프론트 3000/백엔드 8000, 서로 다른 포트)과 본질적으로 동일한 구성임을 재확인해 기각. (b) "우회 후 전부 Pass"라는 결론이 우회 장치 자체의 버그로 인한 위양성(false negative)일 가능성 → CORS 헤더가 실제로 응답에 포함됨을 `curl -D -`로 별도 확인(헤더 유무 자체를 검증) 후 브라우저 테스트를 재실행해 이중 확인. (c) TC-021/022(DEF-006 프론트 영향)가 실은 프론트엔드의 새로운 결함(예: 결과 개수 상한 미적용)을 가리는 것은 아닌지 → `MAX_SEARCH_RESULTS=100` 상한 로직은 Fake 리포지토리에도 `rows[:100]`으로 동일하게 반영했으므로 이 테스트가 상한 로직 자체를 가릴 위험은 없음을 코드 대조로 확인. (d) 경쟁조건 테스트(TC-027/028)의 타이밍이 우연히 맞았을 가능성 → 지연 시간(1.5초)과 대기 시간(총 2.3초)의 여유폭이 충분히 커 타이밍 우연에 좌우될 가능성이 낮음을 확인, 동일 스크립트를 2회 실행해 일관된 결과 확인.
  - [x] 문서만 보고 다음 단계(오케스트레이터/후속 조치 담당)가 추가 질문 없이 작업을 시작할 수 있는가 — §11의 권고 3단계(CORS 수정 배정 → UNIT-07/09 회귀 재검증 → 07단계 진행)가 구체적 실행 가능한 지시로 서술되어 있음을 확인.
  - [x] 되돌리기 어려운 결정(비가역적 결정)에 대한 근거가 명시되어 있는가 — §5의 설계 정합성 직접 판정(질문 미격상)에 대한 4가지 근거를 명시. 이 판정 자체가 비가역적이지는 않음(추후 오케스트레이터가 이견 시 언제든 재검토 가능하도록 근거를 전부 남겨둠).
  - [x] 보안/성능/운영 관점에서 명백히 위험한 내용이 없는가 — 결과서 어디에도 실제 DB 자격증명 값(비밀번호 등)이 노출되지 않았음을 재검색으로 확인(`devpass`/`localdevpass` 등 문자열 미포함, "재사용을 시도했으나 거부됨"이라는 사실만 서술). `.harness-tmp/` 관련 서술도 실제 정리 완료를 `git status` 첨부로 증명.
- 발견된 결함 목록:
  1. 없음(위 4가지 의심 사항 모두 기각 — 결과서의 결론이 그대로 유지됨)
- 조치 내용: 없음(v1 최종 유지)

## v1 최종 판정
- [x] PASS (결함 0건, 최소 2회 검증 완료) — 다음 단계로 handoff 가능
  - 주의: 이 "PASS"는 **`unit-09-test.md`라는 산출물 자체의 완전성/신뢰성**에 대한 판정이며, `unit-09-test.md`(v1)가 내린 **UNIT-09에 대한 판정은 FAIL**이다(DEF-U09-01, Critical, Open — `unit-09-test.md` v1 §11 참조). 두 판정의 대상이 다름에 유의.
- [ ] FAIL

---

# v2 검증 (2026-09-18, DEF-U09-01 CORS 조치 회귀 확인에 대한 문서 품질 검증)

## 1차 검증 (작성자 관점 자가 재검토)
- 검증자(역할): 06-unit-tester(작성자 본인, 자가 재검토)
- 일시: 2026-09-18
- 체크리스트
  - [x] 입력 계약(오케스트레이터가 이번 세션에 위임한 CORS 회귀 확인 범위 4개 — UNIT-07/09 실제 두 오리진 재현, pytest 4건 독립 재실행+변형 1건, 최소 정적분석/빌드 재확인, `traceability.md` REQ-001/003 동기화)에 명시된 항목을 UNIT-09 관점에서 실제로 반영했는가 — §6-2/§7에서 1:1 매핑 확인(TC-037/038/039/040/033/034/036).
  - [x] 출력 계약(`test-report-template.md` 전 섹션)이 v2에도 빠짐없이 존재하는가 — 1~12절 전부 v2 갱신 또는 "v2에서 변경 없음" 명시, 섹션 생략 없음.
  - [x] 상위 단계 산출물과 용어/수치/범위가 모순되지 않는가 — `unit-01-note.md`(v4) §0-c/§3/§6-1이 서술한 수정 내용(파일 범위, pytest 4건 테스트명, 포트 구성)과 이번 v2의 TC-033~041이 정확히 일치하는지 대조 완료.
  - [x] 추측으로 채운 항목이 있는가 — "CORS 에러 0건"이라는 판정이 `page.on('console')` 필터링 결과(정규식 `/CORS|blocked by CORS policy/i`)에 근거함을 명시했고, "렌더링 확인"도 DOM 텍스트 포함 여부를 실측한 결과임을 명시(추측 없음).
  - [x] 20년차 실무자 기준으로 봤을 때 구조적/논리적 결함이 없는가 — v1의 FAIL 판정을 소급 수정하지 않고 "v1 이력 보존 + v2 신규 판정"으로 분리한 것, DEF-U09-01의 상태를 "Open"에서 "Fixed"로 명확히 전환하고 그 근거(TC-037/038/039)를 구체적으로 남긴 것이 규칙 F의 취지(재작업 이력 보존)에 부합.
  - [x] 오탈자, 형식 오류, 누락된 표/그림 참조가 없는가 — TC 번호 연속성(TC-033~041, 기존 TC-032와 충돌 없음) 및 §8/§11의 "v1 이력 보존" 표기 일관성 재확인.
- 발견된 결함 목록:
  1. 없음
- 조치 내용: 수정 없음, 2차 검증으로 진행

## 2차 검증 (독립 심사자 관점 — 역할 전환 재검토)
- 검증자(역할): 06-unit-tester(오늘 이 v2 섹션을 처음 받아본 심사자 역할로 전환)
- 일시: 2026-09-18
- 체크리스트
  - [x] 1차 검증에서 지적된 항목이 실제로 반영되었는가 — 지적 사항 없었음.
  - [x] 엣지 케이스/예외 상황이 누락되지 않았는가 — "이 PASS 판정이 5단계 보고(uvicorn+curl 프리플라이트)를 재포장한 것에 불과한 것은 아닌가"를 의심하며 재검토했다. (1) 5단계는 `TestClient` 인프로세스 + `curl` OPTIONS 프리플라이트까지만 확인했고, 이번 v2는 실제 소켓 기반 두 프로세스(uvicorn 서버 + next start 서버) + 실제 헤드리스 Chrome + 실제 DOM 렌더링까지 확인해 증거 수준이 명확히 더 강함을 재확인(오케스트레이터가 요구한 "5단계 curl 프리플라이트 확인보다 강한 증거" 조건 충족). (2) TC-039(부정 통제)가 "테스트가 결함을 놓칠 가능성"을 실제로 반증하는지 재검토 — 동일 요청을 허용/비허용 오리진으로 바꿔가며 헤더 유무 차이를 관측했으므로 테스트 자체의 민감도가 확인됨. (3) Fake DI 오버라이드가 CORS 미들웨어까지 우회하지는 않는지 재검토 — `app.dependency_overrides`(라우트 DI)와 `app.add_middleware`(앱 초기화 시점 등록)는 서로 다른 레이어이며, 미들웨어는 라우트 진입 전에 이미 적용되므로 DI 오버라이드로 우회될 수 없음을 코드 구조로 재확인. (4) 서버 재기동(TC-038)이 형식적 재실행이 아닌지 재검토 — `netstat`로 리스닝 PID를 특정해 실제로 종료·재기동했음을(PID 변경) 확인.
  - [x] 문서만 보고 다음 단계(07 통합테스트) 에이전트가 추가 질문 없이 작업을 시작할 수 있는가 — §11이 v2 PASS 판정과 근거를 명확히 제시하고, §10 리스크(프로덕션 호스팅 CORS 미확정, favicon 404)를 07단계가 참고할 수 있도록 남겨뒀는지 확인 — 충분함.
  - [x] 되돌리기 어려운 결정(비가역적 결정)에 대한 근거가 명시되어 있는가 — 해당 없음(검증 결과 보고, 비가역적 결정 없음).
  - [x] 보안/성능/운영 관점에서 명백히 위험한 내용이 없는가 — `.harness-tmp/cors-regression/`이 완전히 삭제되고 서버 프로세스도 종료됐는지(§9 v2 Teardown), DB 자격증명 노출이 없는지(이번 v2도 DB 자격증명을 다루지 않음, `dummy:dummy` 더미 값만 사용) 재확인.
- 발견된 결함 목록:
  1. 없음
- 조치 내용: 수정 없음, v2를 최종본으로 확정

## v2 최종 판정
- [x] PASS (결함 0건, 최소 2회 검증 완료) — 다음 단계로 handoff 가능
  - 이 "PASS"는 `unit-09-test.md`(v2)라는 테스트 결과서 자체의 품질/완결성에 대한 내부 검증 판정이며, 이 판정 대상 테스트서(v2)가 보고하는 **테스트 대상(UNIT-09, DEF-U09-01 CORS 조치)**의 판정도 동일하게 **PASS**(v2 §11)로, 이번에는 두 판정이 방향까지 일치한다(v1과 달리 결함이 Fixed로 확정됐기 때문).
- [ ] FAIL — 재작업 필요, 사유:

## 절차 흐름 (참고용 다이어그램)
> 위 텍스트가 최종 근거다.
