# 내부 검증 로그 (Internal Verification Log)

## 대상 산출물
- 파일: `docs/harness/units/unit-07-test.md`
- 작성 에이전트: `06-unit-tester`
- 적용 Tier: High
- 버전: v1 → v2(규칙 F 피드백 루프 — DEF-U07-01 조치 독립 재검증) → **v3**(규칙 F 피드백 루프 — DEF-U09-01 CORS 조치의 UNIT-07 관점 회귀 확인)

---

# v1 검증 (이력 보존, 원문 유지)

## 1차 검증 (작성자 관점 자가 재검토)
- 검증자(역할): 06-unit-tester(작성자 본인, 자가 재검토)
- 일시: 2026-09-17
- 체크리스트
  - [x] 입력 계약(unit-07-note.md §4 AC-1~7)에 명시된 모든 항목을 실제로 반영했는가 — §5 커버리지 표로 7/7 확인
  - [x] 출력 계약(test-report-template.md 전 섹션)에 명시된 모든 필수 항목이 빠짐없이 존재하는가 — 1~10절 전부 작성, 해당 없는 섹션 없음
  - [x] 상위 단계 산출물과 용어/수치/범위가 모순되지 않는가 — 03-system-design.md §3-2/§4-2 워크드 예제(동률 처리, NULL 자동제외, 알파벳순 union)와 실측값이 전부 일치함을 TC-016~023에서 확인
  - [x] 추측으로 채운 항목이 있는가 — DEF-U07-01의 원인 (a)(CSS 전환/effect 실행 시점 경쟁조건)는 "추정"이라고 명시했고, 정확한 브라우저 내부 메커니즘까지는 단정하지 않았다(재작업은 5단계가 원인을 확정지어도 되고, 대안 구현으로 우회해도 됨을 열어둠)
  - [x] 20년차 실무자 기준으로 봤을 때 구조적/논리적 결함이 없는가 — DEF-U07-01을 "실행해보니 에러 없음"이 아니라 기대 결과(포커스 유지, 값 커밋) 대비 실제 결과(포커스 이탈, 값 미커밋)를 명시적으로 비교해 Fail 처리했다(원칙 위반 없음 확인)
  - [x] 오탈자, 형식 오류, 누락된 표/그림 참조가 없는가 — 표 컬럼 수/구분선 정합성 재확인
- 발견된 결함 목록:
  1. (경미) §4 TC-033 표의 "재현 절차" 서술이 다소 길어 가독성이 떨어짐 — 표 자체는 유지하되 §6 결함 목록에서 더 상세히 재서술했으므로 중복 서술로 보완됨(치명적이지 않음, 조치 불필요로 판단)
- 조치 내용: 경미한 사항이라 별도 수정 없이 v1 유지, 2차 검증으로 진행

## 2차 검증 (독립 심사자 관점 — 역할 전환 재검토)
- 검증자(역할): 06-unit-tester(오늘 이 문서를 처음 받아본 심사자 역할로 전환)
- 일시: 2026-09-17
- 체크리스트
  - [x] 1차 검증에서 지적된 항목이 실제로 반영되었는가 — 경미 사항이라 조치 불필요 판단 유지, 문서 재확인 결과 문제 없음
  - [x] 엣지 케이스/예외 상황이 누락되지 않았는가 — "결과가 정상 통과했다고 다음 단계(07 통합테스트)에 넘겨도 되는가"를 의심하며 재검토. 특히 아래 3개 관점에서 재점검했다:
    1. **"백엔드가 전부 Pass인데 프론트엔드 결함 하나로 전체를 FAIL 처리하는 게 맞는가?"** — 재확인 결과, REQ-003은 기획서(02-planning.md §4-2)상 "조건 기반 스크리닝"이라는 하나의 사용자 기능이며, 그 기능의 입력 경로(조건 입력 폼)가 주 타깃 디바이스(모바일)에서 실질적으로 봉쇄되면 백엔드가 아무리 정확해도 최종사용자 관점에서 기능 자체가 동작하지 않는 것과 같다. UNIT-01의 DEF-001(설계서 결함으로 FAIL)과 동일한 논리로, 결함의 위치가 백엔드냐 프론트엔드냐가 아니라 "REQ 충족 여부"가 판정 기준이어야 한다 — FAIL 판정 유지가 타당하다.
    2. **"진단용 코드 패치(TC-039)가 실제로 결함 재현에 영향을 주지 않았는가?"** — 패치를 적용하기 *전*(TC-033/035/036, 원본 코드)과 *후*(TC-039, 임시 패치) 양쪽 모두 별도로 재현 로그를 남겼고, 원본 코드 상태의 결함이 패치와 무관하게 독립적으로 3회 재현됐음을 재확인했다. 진단 패치는 원인 분리 목적으로만 사용됐고 최종 결함 판정은 원본(미수정) 코드 기준임을 명확히 했다.
    3. **"Docker/브라우저 등 테스트 환경 자체의 결함일 가능성은 없는가?"** — 데스크톱 뷰포트(TC-038)에서는 동일한 코드베이스·동일한 브라우저·동일한 서버로 정상 동작함을 함께 확인했으므로, 환경 자체의 결함이 아니라 `trapActive` 조건(모바일 전용 코드 경로)에 국한된 애플리케이션 결함임을 배제 근거로 명시했다 — 이 부분이 1차 검증에서는 명시적으로 언급되지 않았던 지점이라 2차 검증에서 §6 DEF-U07-01 설명에 이미 포함되어 있는지 재확인했고, TC-038의 "비고"란에 이미 명시되어 있음을 확인(추가 수정 불필요).
  - [x] 문서만 보고 다음 단계(5단계 재작업자) 에이전트가 추가 질문 없이 작업을 시작할 수 있는가 — §9 "재작업 요청 사항" 1~4번이 파일 경로·구체적 수정 방향(useRef 패턴)·재검증 범위(TC-033~038)까지 명시되어 있어 충분하다고 판단
  - [x] 되돌리기 어려운 결정(비가역적 결정)에 대한 근거가 명시되어 있는가 — 이번 테스트서는 설계 결정이 아니라 검증 결과 보고이므로 해당 없음(비가역적 결정 없음)
  - [x] 보안/성능/운영 관점에서 명백히 위험한 내용이 없는가 — DB 자격증명(`devpass`)이 로그에 노출되지 않았는지 문서 전체를 재검색해 확인(쉘 명령 설명에만 등장, 실제 값은 본문에 출력하지 않음 — UNIT-06 관례와 동일)
- 발견된 결함 목록:
  1. 없음(위 체크리스트 3개 관점 재검토 결과 기존 판정을 뒤집을 근거를 찾지 못함, 오히려 판정 근거가 보강됨)
- 조치 내용: 추가 수정 없음, v1을 최종본으로 확정

## v1 최종 판정
- [x] PASS (결함 0건, 최소 2회 검증 완료) — 다음 단계로 handoff 가능
  - **주의**: 이 "PASS"는 **`unit-07-test.md`라는 테스트 결과서 자체의 품질/완결성**에 대한 내부 검증 판정이다. `unit-07-test.md`가 보고하는 **테스트 대상(UNIT-07)** 자체의 판정은 그 문서 §9에 명시된 대로 v1 시점 **FAIL**이며, 이 검증 로그의 PASS와 모순되지 않는다(이 로그는 "테스트서가 제대로 작성됐는가"를, 테스트서 §9는 "코드가 요구사항을 충족하는가"를 각각 판정한다).

---

# v2 검증 (2026-09-17, DEF-U07-01 조치 독립 재검증에 대한 문서 품질 검증)

## 1차 검증 (작성자 관점 자가 재검토)
- 검증자(역할): 06-unit-tester(작성자 본인, 자가 재검토)
- 일시: 2026-09-17
- 체크리스트
  - [x] 입력 계약(`unit-07-note.md` v2 §8-b가 위임한 5개 항목)에 명시된 모든 항목을 실제로 반영했는가 — §5 커버리지 절에서 1:1 매핑 확인(TC-046/047 계열, TC-048a/b, TC-049, TC-041~045/042)
  - [x] 출력 계약(test-report-template.md 전 섹션)이 v2에도 빠짐없이 존재하는가 — 1~10절 전부 v2 갱신, "단위+통합 병합" 해당 여부도 확인(이 feature(REQ-003/UNIT-07)는 Tier=High이므로 06·07 병합 조건(Low 등급 전용)에 해당하지 않음 — 테스트 유형을 "단위(Unit)"로 유지하고 병합 표기를 하지 않은 것이 맞는 판단인지 재확인, 맞음)
  - [x] 상위 단계 산출물과 용어/수치/범위가 모순되지 않는가 — `unit-07-note.md`(v2) §8-b가 요구한 문구("가능하면 다른 방식으로 교차검증", "CPU 스로틀링", "다른 재렌더 트리거")를 그대로 이행했는지 원문 대조 완료
  - [x] 추측으로 채운 항목이 있는가 — TC-048b(50배 스로틀링 측정 불가)를 "결함 없음"으로 단정하지 않고 "환경 한계로 측정 불가"라고 정확히 구분해 서술했는지 재확인(맞음). TC-051의 1회성 이상 현상도 추측으로 덮지 않고 재현 시도 과정을 그대로 기록했는지 재확인(맞음)
  - [x] 20년차 실무자 기준으로 봤을 때 구조적/논리적 결함이 없는가 — "5단계 자체 검증을 그대로 신뢰하지 않는다"는 원칙을 실제로 지켰는지(같은 도구 재사용이 아니라 Playwright-core로 전환, 5단계가 안 한 CPU 스로틀링/리사이즈 트리거 추가) 재확인
  - [x] 오탈자, 형식 오류, 누락된 표/그림 참조가 없는가 — TC 번호 연속성(TC-041~051) 및 v1 표와의 교차 참조("TC-033 대체" 등) 정합성 재확인
- 발견된 결함 목록:
  1. 없음
- 조치 내용: 수정 없음, 2차 검증으로 진행

## 2차 검증 (독립 심사자 관점 — 역할 전환 재검토)
- 검증자(역할): 06-unit-tester(오늘 이 문서를 처음 받아본 심사자 역할로 전환)
- 일시: 2026-09-17
- 체크리스트
  - [x] 1차 검증에서 지적된 항목이 실제로 반영되었는가 — 지적 사항 없었음, 재확인 결과 문제 없음
  - [x] 엣지 케이스/예외 상황이 누락되지 않았는가 — "이 PASS 판정이 5단계 보고를 재포장한 것에 불과한 것은 아닌가"를 의심하며 재검토했다. 구체적으로:
    1. **"Playwright-core 전환이 형식적 교체에 불과하지 않은가?"** — 실제로 puppeteer-core와 다른 API(`page.setViewportSize` vs `page.setViewport`, `page.context().newCDPSession()` 등)를 사용했고, CDP 세션을 통해 5단계가 시도하지 않은 `Emulation.setCPUThrottlingRate`까지 활용해 실질적으로 다른 검증 범위를 커버했음을 스크립트 내용으로 재확인. 단순 라이브러리명만 바꾼 재탕이 아니다.
    2. **"CPU 스로틀링 결과가 우연이 아닌가?"** — TC-048a를 300ms/1000ms/2000ms 세 가지 독립 대기시간으로 3회 실행해 전부 Pass했고, 원본(재작업 전) 코드였다면 단일 rAF만으로는 동일 조건에서도 실패했을 것이라는 대조군을 별도로 두지 않았다는 점을 지적할 수 있는지 검토했다 — 그러나 이 v2 재검증의 목적은 "수정 후 코드가 우려사항을 견디는가"이지 "수정 전/후 대조"가 아니며, 수정 전/후 대조는 이미 5단계 note(§0-a)와 v1(TC-033/035)이 각각 별도로 수행했으므로 중복 불필요로 판단.
    3. **"TC-051의 1회성 404가 사실은 재현 가능한 결함인데 재현 시도 횟수(3회)가 부족했던 것은 아닌가?"** — 재조사 시점에 실행 환경 정황(직전 CPU 스로틀링 50배 테스트가 타임아웃되던 시점과 겹침)이라는 합리적 대안 설명이 있고, 재현 실패가 3회 연속(우연으로 보기에 충분한 횟수)이었으며, 무엇보다 이 404가 포커스 트랩의 DOM 상태에 실질적 영향을 주지 않았음(직후 재오픈 정상)을 같은 실행 로그 안에서 확인했으므로 결함 미등록이 타당하다고 재확인. 다만 완전히 배제하지 못했음을 §8 리스크로 남긴 것이 적절하다고 판단.
  - [x] 문서만 보고 다음 단계(07 통합테스트) 에이전트가 추가 질문 없이 작업을 시작할 수 있는가 — §9가 PASS 판정과 근거를 명확히 제시하고, §8 리스크(CPU 50배, TC-051 이상현상, 실기기 미검증)를 07단계가 참고할 수 있도록 남겨뒀는지 확인 — 충분함
  - [x] 되돌리기 어려운 결정(비가역적 결정)에 대한 근거가 명시되어 있는가 — 해당 없음(검증 결과 보고, 비가역적 결정 없음)
  - [x] 보안/성능/운영 관점에서 명백히 위험한 내용이 없는가 — `.harness-tmp/unit07-verify/`가 완전히 삭제되고 서버 프로세스도 종료됐는지, 자격증명 노출이 없는지(이번 v2는 DB 자격증명을 다루지 않음) 재확인
- 발견된 결함 목록:
  1. 없음
- 조치 내용: 수정 없음, v2를 최종본으로 확정

## v2 최종 판정
- [x] PASS (결함 0건, 최소 2회 검증 완료) — 다음 단계로 handoff 가능
  - 이 "PASS"는 `unit-07-test.md`(v2)라는 테스트 결과서 자체의 품질/완결성에 대한 내부 검증 판정이며, 이 판정 대상 테스트서(v2)가 보고하는 **테스트 대상(UNIT-07, DEF-U07-01 조치)**의 판정도 동일하게 **PASS**(v2 §9)로, 이번에는 두 판정이 방향까지 일치한다(v1과 달리 결함이 Fixed로 확정됐기 때문).
- [ ] FAIL — 재작업 필요, 사유:

---

# v3 검증 (2026-09-18, DEF-U09-01 CORS 조치의 UNIT-07 관점 회귀 확인에 대한 문서 품질 검증)

## 1차 검증 (작성자 관점 자가 재검토)
- 검증자(역할): 06-unit-tester(작성자 본인, 자가 재검토)
- 일시: 2026-09-18
- 체크리스트
  - [x] 입력 계약(오케스트레이터가 이번 세션에 위임한 CORS 회귀 확인 범위 4개 — UNIT-07/09 실제 두 오리진 재현, pytest 4건 독립 재실행+변형 1건, 최소 정적분석/빌드 재확인, `traceability.md` REQ-003 동기화)에 명시된 항목을 UNIT-07 관점에서 실제로 반영했는가 — §4-3/§5에서 1:1 매핑 확인(TC-101/102/103/104/105/106/107).
  - [x] 출력 계약(`test-report-template.md` 전 섹션)이 v3에도 빠짐없이 존재하는가 — 1~10절 전부 v3 갱신 또는 "v3에서 변경 없음" 명시.
  - [x] 상위 단계 산출물과 용어/수치/범위가 모순되지 않는가 — `unit-01-note.md`(v4)/`unit-09-test.md`(v2)가 서술한 CORS 수정 내용·파일 범위·pytest 4건 테스트명과 이번 v3의 TC-101~108이 정확히 일치하는지 대조 완료. `unit-09-test.md`(v2) TC-040과 본 문서 TC-105가 동일 스크립트(`variant_multi_origin_check.py`)를 재사용한 교차 확인임을 양쪽 문서에 명시했는지 확인.
  - [x] 추측으로 채운 항목이 있는가 — "CORS 에러 0건"이 콘솔 로그 필터링 결과에 근거함을 명시, "렌더링 확인"이 DOM 텍스트("삼성전자","SK하이닉스","등락률") 실측 결과임을 명시. TC-108의 404 원인도 추측이 아니라 `page.on('response')`로 URL을 직접 캡처해 특정했다.
  - [x] 20년차 실무자 기준으로 봤을 때 구조적/논리적 결함이 없는가 — v2(포커스 트랩)의 PASS 판정을 소급 수정하지 않고 v3를 별도 절(§4-3, §9 v3 근거)로 추가한 것, DEF-U07-01과 DEF-U09-01(승계 확인)을 §6에서 명확히 구분해 서술한 것이 규칙 F의 재작업 이력 보존 원칙에 부합.
  - [x] 오탈자, 형식 오류, 누락된 표/그림 참조가 없는가 — TC 번호 연속성(TC-101~108, 기존 TC-001~051과 충돌 없음) 재확인.
- 발견된 결함 목록:
  1. 없음
- 조치 내용: 수정 없음, 2차 검증으로 진행

## 2차 검증 (독립 심사자 관점 — 역할 전환 재검토)
- 검증자(역할): 06-unit-tester(오늘 이 v3 섹션을 처음 받아본 심사자 역할로 전환)
- 일시: 2026-09-18
- 체크리스트
  - [x] 1차 검증에서 지적된 항목이 실제로 반영되었는가 — 지적 사항 없었음.
  - [x] 엣지 케이스/예외 상황이 누락되지 않았는가 — "이 PASS 판정이 UNIT-09(v2)의 결과를 그대로 베낀 것에 불과한 것은 아닌가"를 의심하며 재검토했다. (1) UNIT-09는 `/stocks`+`stockSearchApi.ts`를, 이번 v3는 `/screener`+`screenApi.ts`를 각각 독립적으로 실제 브라우저 상호작용(폼 제출, 뷰포트 1280×900)으로 재현했으므로 단순 복붙 검증이 아님을 스크립트(`cors_browser_test.mjs`의 TC-A/TC-B 두 케이스)로 재확인. (2) TC-103(부정 통제)이 UNIT-09 v2와 별도로 이번 v3에서도 독립 실행됐는지 확인(동일 백엔드 프로세스를 공유했으나 요청 자체는 이번 세션에서 별도로 재발행). (3) TC-101/102의 렌더링 확인이 스크린샷이 아니라 실제 DOM 텍스트 assert(`containsSamsung`, `containsSK`)에 기반하는지 재확인. (4) 서버 재기동(TC-102)이 형식적 재실행이 아닌지 재검토 — `netstat`으로 리스닝 PID를 특정해 실제로 종료·재기동했음을(PID 변경) 확인.
  - [x] 문서만 보고 다음 단계(07 통합테스트) 에이전트가 추가 질문 없이 작업을 시작할 수 있는가 — §9가 v2+v3 통합 판정과 근거를 명확히 제시하고, §8 리스크(프로덕션 호스팅 CORS 미확정, favicon 404, CPU 스로틀링 50배, 실기기 미검증)를 07단계가 참고할 수 있도록 남겨뒀는지 확인 — 충분함.
  - [x] 되돌리기 어려운 결정(비가역적 결정)에 대한 근거가 명시되어 있는가 — 해당 없음(검증 결과 보고, 비가역적 결정 없음).
  - [x] 보안/성능/운영 관점에서 명백히 위험한 내용이 없는가 — `.harness-tmp/cors-regression/`이 완전히 삭제되고 서버 프로세스도 종료됐는지(§7 v3 Teardown), DB 자격증명 노출이 없는지(더미 값만 사용) 재확인.
- 발견된 결함 목록:
  1. 없음
- 조치 내용: 수정 없음, v3를 최종본으로 확정

## v3 최종 판정
- [x] PASS (결함 0건, 최소 2회 검증 완료) — 다음 단계로 handoff 가능
  - 이 "PASS"는 `unit-07-test.md`(v3)라는 테스트 결과서 자체의 품질/완결성에 대한 내부 검증 판정이며, 이 판정 대상 테스트서(v3)가 보고하는 **테스트 대상(UNIT-07, DEF-U07-01 + DEF-U09-01 CORS 영향)**의 판정도 동일하게 **PASS**(v3 §9)다.
- [ ] FAIL — 재작업 필요, 사유:
