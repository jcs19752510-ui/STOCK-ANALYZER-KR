# 내부 검증 로그 (Internal Verification Log)

## 대상 산출물
- 파일: `docs/harness/units/unit-01-test.md` (+ 동시 갱신물: `docs/harness/traceability.md`(REQ-005/REQ-012 "단위테스트" 컬럼))
- 작성 에이전트: 06-unit-tester
- 버전: v0(초안) → v1(1차 검증 반영) → v2(2차 검증 반영, FAIL 최종) → v3(DEF-001/DEF-002 재작업 재검증, 1차 반영) → v4(2차 검증 반영, CONDITIONAL PASS 최종) → v5(실제 Postgres 확정 검증, 1차 반영) → v6(2차 검증 반영, CONDITIONAL PASS 유지 - 조건 변경) → **v7(DEF-003/004 수정 독립 재검증, 1차 반영) → v8(2차 검증 반영, PASS 최종 확정)**

> 아래 "1차 검증"/"2차 검증"은 v1(초회) 당시 로그를 그대로 보존한다. 이번 재검증(규칙 F 피드백 루프, DEF-001/DEF-002 재작업 완료 대응)의 검증 로그는 하단 "v2 재검증 — 1차/2차 검증" 절에 신규로 추가했다. 코디네이터가 실제 PostgreSQL 환경을 제공한 뒤 AC-3/AC-5를 확정 검증한 이번 라운드의 로그는 하단 "v3 재검증 — 1차/2차 검증" 절에 추가했다. 5단계가 DEF-003/DEF-004를 수정한 뒤 그 자체 검증 결과를 신뢰하지 않고 6단계가 독립 재현한 이번 라운드의 로그는 하단 "v4 재검증 — 1차/2차 검증" 절에 추가했다.

## 1차 검증 (작성자 관점 자가 재검토)
- 검증자(역할): 20년차 QA 겸 개발자 (작성자 본인 관점)
- 일시: 2026-09-14
- 체크리스트
  - [x] 입력 계약(`unit-01-note.md`의 AC-1~AC-6, 수동확인 5건, 편차 6건)을 실제로 반영했는가 — AC-1~AC-6의 모든 불릿을 TC-001~042로 1:1 매핑했고, 수동확인 5건(Postgres 마이그레이션/CLI upsert/uvicorn 실기동/market_open_time 정확성/휴장일 목록/편차2 경계조건) 전부를 §2 제외범위 또는 §4 테스트 케이스 또는 §7 리스크에서 다뤘는지 재확인함 — 5건 모두 위치를 찾음(마이그레이션·CLI upsert·uvicorn 실기동은 §2/§7, market_open_time·휴장일 목록은 §2 제외사유+§7 리스크, 편차2 경계조건은 §4 TC-008~010+§6 DEF-001).
  - [x] 출력 계약(`test-report-template.md`) 9개 섹션이 빠짐없이 존재하는가 — §1~§9 전부 작성, 어떤 섹션도 "해당 없음"으로 생략 없이 근거와 함께 채움.
  - [x] "이상 없음"/PASS 판정마다 근거(어떤 케이스로 확인했는지)가 있는가 — §4 표 전 행에 실행 절차와 실제 결과를 병기했고, §6 결함목록 하단에 "DEF-001 제외 전부 결함 없음, 근거는 §4 예상/실제 결과 대조"를고 명시함.
  - [x] 5단계의 자체 테스트(21건 pass, ruff 통과)를 그대로 신뢰하지 않고 독립 재실행했는가 — TC-001~007, TC-011~015, TC-025~028, TC-041, TC-042는 전부 이번 세션에서 `pytest`/`ruff`를 직접 재실행한 결과이지 note의 로그를 베낀 것이 아님(§3 실행 환경에 재실행 근거 명시).
  - [x] 추측으로 채운 항목이 있는가 — DEF-001의 "설계 의도" 판단이 추측이 아니라 `03-system-design.md` §3-3/§5-4 원문 교차 인용에 근거했는지 재확인(있음, §6 DEF-001 설명에 원문 인용 포함). "장중 당일 반환이 실제 의도일 수도 있다"는 반대 가능성은 추측으로 덮지 않고 §8 결론에 "이 판단은 3단계가 최종 확정해야 한다"고 명시적으로 한계를 남김.
  - [x] 20년차 QA 기준으로 구조적/논리적 결함이 없는가 — "실행해보니 에러 없음"만으로 PASS 처리한 항목이 있는지 전수 재검토 → 없음(모든 PASS 행에 기대값과 실제값이 병기됨).
- 발견된 결함 목록:
  1. 초안에서 DEF-001의 재현 절차가 "TC-008 참조"로만 축약돼 있어, 이 문서만 단독으로 읽는 독자(7단계 등)가 재현하려면 §4 표를 다시 찾아가야 하는 불편함이 있었음(자기완결성 부족).
  2. DEF-001 severity를 "High"로만 표기하고, "왜 Critical이 아닌지"에 대한 명시적 근거가 초안에는 없었음(근거 없는 severity 판정은 신뢰도가 낮음).
  3. TC-029/TC-036(6단계가 임시 스크립트로만 확인하고 회귀 테스트로 남기지 않은 경로)을 발견하고도 초안에서는 결함으로 별도 등록하지 않고 비고란에만 적어 넘어갈 뻔했음 — "발견했으면 반드시 결함 목록에 남긴다"는 원칙에 어긋남.
- 조치 내용: (수정 후 → v1)
  1. §6 DEF-001의 "재현 절차" 열에 날짜·시각·예상 반환값을 인라인으로 다시 기술(표 자체만으로 재현 가능하도록 자기완결화).
  2. §6 DEF-001 심각도 설명에 "현재 `GET /calendar/last-trading-day`가 `is_latest_trading_day`를 하드코딩해 이 결함이 아직 사용자 노출 값에 영향을 주지 않는다"는 근거를 명시적으로 추가해 Critical이 아닌 High로 분류한 이유를 남김.
  3. DEF-002를 신설해 TC-029/TC-036 관련 테스트 커버리지 공백을 결함 목록에 정식 등록(Low, Open).

## 2차 검증 (독립 심사자 관점 — "오늘 처음 이 결과서를 받아본 7단계 통합테스터")
- 검증자(역할): 07-integration-tester 관점(오늘 처음 이 문서만 보고 UNIT-01을 통합테스트로 넘겨받아도 되는지 판단하는 입장) — 1차 검증자가 놓쳤을 법한 순환논리·암묵적 가정을 의심하며 재검토
- 일시: 2026-09-14
- 체크리스트
  - [x] 1차 검증에서 지적된 3건이 실제로 반영됐는가 — v1을 재확인, 3건 모두 반영 확인.
  - [ ] → [발견] AC-1 불릿1("평일 개장 시각 이후 + 당일이 거래일 → 당일 반환")은 5단계 자신이 작성한 인수 조건이며, 이 문구 자체가 DEF-001에서 결함으로 지목한 리터럴 조건식과 동일한 케이스를 "정상"으로 규정하고 있다. TC-001이 이 AC를 그대로 PASS 처리한 것을 무비판적으로 읽으면 "AC를 통과했으니 이 동작은 정상"이라는 순환논리로 오해될 위험이 있었는데, 초안에는 이 구분(=TC-001은 "AC 문구 재현 여부"만 확인한 것이고, "AC 문구 자체의 타당성"은 별도로 TC-008~010이 검증한다는 구분)이 §4 표 안에 암묵적으로만 존재하고 명시적 문장으로 남아있지 않았음.
  - [ ] → [발견] 엣지 케이스 재검토: `MAX_LOOKBACK_DAYS`의 정확한 경계(400회 vs 401회 스캔에서 예외가 발생하는 정확한 지점)를 케이스로 확인하지 않았다. AC-1에는 "초과 시 예외"라고만 되어 있어 정확한 임계값 자체는 AC 범위를 벗어나지만, 향후 이 상수를 변경하는 리팩터링에서 off-by-one 결함이 들어와도 현재 테스트 스위트(5단계 21건 + 6단계 추가분)로는 못 잡는다는 잔존 리스크가 초안 §7에 명시돼 있지 않았음.
  - [x] 이 문서만 보고 7단계가 추가 질문 없이 판단을 시작할 수 있는가 — §8 결론이 "FAIL, 이유는 DEF-001, 재작업은 3단계부터"라고 명확히 결론과 다음 행동을 지시하고 있어 가능함을 확인. 다만 위 두 건 보완 후 최종 확인.
  - [x] 되돌리기 어려운 판단(FAIL 처리, 3단계로 근본원인 escalation)에 대한 근거가 명시되어 있는가 — `decisions.md` DEC-017과 상호 참조되어 있어 근거 추적 가능함을 확인.
  - [x] 보안/운영 관점에서 명백히 위험한 내용이 이 결과서에서 누락되지 않았는가 — DB 자격증명 미노출, 입력 검증 경계 케이스(TC-030~035)까지 다뤘음을 재확인. 위험한데 다루지 않은 사각지대는 "실제 Postgres 대상 검증"(TC-021/039)뿐이며 이는 은폐가 아니라 §2/§3/§7에서 반복적으로 명시적으로 "미검증"이라 밝히고 있어 문제 없음.
- 발견된 결함 목록:
  1. TC-001(=AC-1 불릿1)의 PASS 판정이 "AC 문구 재현 여부" 확인이라는 점과, "그 AC 문구 자체가 설계 의도와 부합하는지"는 TC-008~010이 별도로 재검증한다는 구분이 문서에 명시적으로 드러나 있지 않아 순환논리로 오독될 위험.
  2. `MAX_LOOKBACK_DAYS` 경계값(off-by-one)의 잔존 리스크가 §7에 명시되지 않음.
- 조치 내용: (수정 후 → v2, 최종)
  1. §9(내부 검증) 2차 요약 문단에 이 구분을 명시적으로 서술해 순환논리 위험을 제거함(현재 반영된 문장: "TC-001(AC-1 불릿1 자체)은 '현재 코드가 AC 문구대로 동작하는가'라는 좁은 의미로만 PASS 처리하되... 별도로 TC-008~010에서 독립적으로 재검증했음을 §4 표에 명확히 구분해 남겼다").
  2. §9 2차 요약 말미에 `MAX_LOOKBACK_DAYS` 정확한 임계값 테스트 부재를 "추가 결함으로 등록하지 않되 후속 검토 후보로 §7에 남긴다"는 판단과 함께 명시(근거 없이 임의로 결함화하지 않는다는 원칙을 지키면서도, 잔존 리스크 자체는 숨기지 않고 기록).

### v1 최종 판정
- [x] PASS (이 **테스트 결과서 자체**의 완결성·추적성·근거 충분성 기준으로는 결함 0건, 최소 2회 검증 완료 — 발견된 3+2건은 전부 v1/v2에서 조치됨) — 다음 단계(7단계 통합테스트 또는 재작업 후 6단계 재수행)로 handoff 가능
- 참고: 이 최종 판정은 "테스트 결과서라는 산출물의 품질"에 대한 PASS이며, **테스트 대상인 UNIT-01 코드 자체의 판정은 `unit-01-test.md` §8에 별도로 명시된 대로 FAIL(DEF-001)이다.** 두 판정을 혼동하지 않는다.

---

## v2 재검증 — 1차 검증 (작성자 관점 자가 재검토, 규칙 F 피드백 루프 대응)
- 검증자(역할): 20년차 QA 겸 개발자(작성자 본인 관점)
- 일시: 2026-09-14 (재작업 이후 재검증)
- 입력: `unit-01-note.md`(v2, DEF-001/DEF-002 대응 재작업 완료), `03-system-design.md`(v4, §3-3 전면 재작성), `shared/calendar_service/last_trading_day.py`(재구현), `tests/unit/test_last_trading_day.py`/`test_public_api.py`(확장)
- 체크리스트
  - [x] DEF-001의 "예상 결과"가 이번에도 추측이 아니라 `03-system-design.md` v4 §3-3 원문(13행 진리표)에 근거하는지 — `unit-01-test.md` TC-043의 "예상 결과" 열을 설계서 원문 표와 1:1 재대조해 일치 확인.
  - [x] "5단계가 36건 pass라고 했으니 믿는다"는 서술이 없는지 — 직접 `python -m pytest tests/unit -q`/`python -m ruff check .`를 재실행해 `36 passed`, `All checks passed!`를 6단계 세션에서 재확인(TC-059/060). note의 보고를 그대로 베끼지 않았음.
  - [x] DEF-001 재현 절차를 "동일한 방식"이 아니라 "더 엄격한 방식"으로 재시도했는가 — v1의 TC-008(08:59:59~23:59:59, 8개 표본점)보다 범위·해상도를 확장해 00:00:00~23:59:59 전 구간·초 단위(총 172,800회)로 독립 스크립트를 새로 작성해 재실행(TC-046). 결과: 불일치 0건.
  - [x] DEF-002 해소 근거가 "동작이 정상이다"가 아니라 "회귀 테스트가 실제로 존재하고 통과한다"인지 — `tests/unit/test_public_api.py` 파일을 직접 열어 `test_health_degraded_when_db_check_fails`/`test_last_trading_day_scan_limit_exceeded_returns_503` 두 함수의 실재를 확인한 뒤 `pytest -v`로 개별 재실행(TC-057/058).
  - [x] `market_hours.py` 삭제로 인한 참조 깨짐이 없는지 프로젝트 전체를 grep으로 확인했는가 — `find`로 파일 부재, `grep -rn "market_hours|market_open_time"`으로 실제 import/호출이 0건(주석만 존재)임을 확인(TC-053).
  - [x] `services/public_api/api/calendar.py`가 실제로 `CalendarIntegrityError` 공통 베이스를 캐치하는지 코드를 직접 열어 확인했는가 — `except CalendarIntegrityError as exc:` 1곳 확인, `CalendarDataError`(설계서/note가 API 레벨 예시로 들지 않은 하위 예외)까지 직접 API 호출로 503 매핑을 검증(TC-055, 범위를 벗어나도 위험 케이스라 추가 검증).
  - [x] AC-3 불릿4/AC-5(Postgres 실기동)를 이번에도 정직하게 "미검증"으로 남겼는지, 임의로 통과 처리하지 않았는지 — `which psql`/`pg_ctl` 없음, `docker info` 서버 연결 실패를 이번 세션에서 재확인(TC-063)한 후 §6/§8/§5에서 일관되게 "미검증(NOT TESTED)"으로 표기했는지 재확인.
  - [x] TC-046의 "총 172,800회" 수치 재검산 — 86,400초/일(KRX) + 86,400초/일(NXT) = 172,800. 계산 일치, 수정 불필요.
- 발견된 결함(문서 자체의): 없음. 다만 TC-054(API 레벨 마감 경계 재현) 최초 시도에서 캘린더 픽스처에 09-12/13 휴장 행을 누락해 424가 잘못 관측된 시행착오가 있었음 — 즉시 원인을 "테스트 스크립트 준비 오류"로 특정하고 캘린더를 정정해 재실행, 결과를 정정된 값으로 §4-2에 기록(은폐하지 않고 시행착오 자체를 §4-2/§6에 남김).
- 조치 내용: 없음(문서 구조상 이미 반영, TC-054 비고에 시행착오 경위를 명시).

## v2 재검증 — 2차 검증 (독립 심사자 관점 — "오늘 처음 이 v2 결과서를 받아본 7단계 통합테스터")
- 검증자(역할): 07-integration-tester 관점, 1차 검증자가 놓쳤을 법한 순환논리·암묵적 가정을 의심
- 일시: 2026-09-14
- 체크리스트
  - [ ] → [발견] TC-046이 "6단계가 별도로 새로 작성한 스크립트"라고 서술하는데, 그 스크립트가 사용한 캘린더 픽스처 값(2026-09-11/14 거래일, 09-12/13 휴장, close 15:30/20:00)이 `tests/unit/test_last_trading_day.py`의 `week_calendar`와 완전히 동일하다 — "편향 배제"라는 취지와 "동일 값 재사용"이 언뜻 모순으로 읽힐 위험.
    - **재검토 결과**: 순환논리 아님. 값 자체는 설계서(`03-system-design.md` §3-3 진리표 전제 문단)에서 직접 가져온 것이므로 5단계 테스트와 같은 것이 당연하고(둘 다 같은 설계서를 따라야 함), 6단계가 독립적으로 확보한 것은 "값"이 아니라 "전 구간·초 단위 전수 실행이라는 검증 방식"이다. 이 구분을 `unit-01-test.md` §4-2 TC-046 비고 및 §9 2차 검증 절에 명시적 문장으로 남겨 오독 가능성을 제거했다.
  - [x] TC-054 시행착오를 은폐하지 않고 기록했는지 — `unit-01-test.md` §4-2 TC-054 비고, §6 결함 목록 하단에 모두 명시되어 있음을 재확인. 오히려 "테스트 자체의 결함 가능성을 의심한다"는 원칙을 실제로 실천한 사례로 인정.
  - [x] §8의 CONDITIONAL PASS가 실질적 조건을 담고 있는지(형식적 조건이 아닌지) — (a) Postgres 실 마이그레이션 성공/롤백, (b) 실 upsert 성공, (c) uvicorn 실기동 + 실 DB 조회, 3가지 구체 조건이 명시되어 있어 형식적이지 않음을 확인.
  - [x] traceability.md REQ-005/REQ-012 갱신 문구가 이 판정(CONDITIONAL PASS, DEF-001/002 Fixed, AC-3/AC-5 미검증)과 정확히 일치하는지 — `unit-01-test.md` 작성 직후 `traceability.md`를 갱신하며 문구를 동일한 근거(TC 번호, Fixed 상태, CONDITIONAL PASS)로 교차 기재해 불일치가 없는지 실제로 대조 완료.
  - [x] "환경 제약으로 미검증"이 매번 반복되는 것이 "검증을 회피하는 핑계"로 오독될 위험이 없는지 — `docker info`/`which psql` 실행 로그를 v1과 v2 양쪽에서 직접 재현해 기록했고, 단순 서술이 아니라 매 세션 실제로 명령을 실행한 증거를 남겼으므로 핑계가 아니라 사실 확인으로 판단.
- 발견된 결함 목록(문서 자체의): 1건(TC-046의 "픽스처 재사용"과 "독립 검증"의 관계가 불명확했던 서술, 위에서 재검토·보완 완료). 코드 결함은 0건.
- 조치 내용: `unit-01-test.md` §4-2 TC-046 비고와 §9 2차 검증 절에 "값의 출처(설계서 원문)"와 "검증 방식의 독립성(전수 실행)"을 구분하는 문장을 명시적으로 추가함(반영 완료).

## v2 재검증 최종 판정
- [x] PASS (이 **v2 테스트 결과서 자체**의 완결성·추적성·근거 충분성 기준으로 결함 0건, 최소 2회 검증 완료 — 발견된 1건의 서술 모호성은 v2 재검증 2차에서 조치됨) — `traceability.md` 갱신 후 다음 단계(7단계 통합테스트, 단 Postgres 실기동 조건부)로 handoff 가능
- 참고: 이 최종 판정은 "v2 테스트 결과서라는 산출물의 품질"에 대한 PASS이며, **테스트 대상인 UNIT-01 코드 자체의 판정은 `unit-01-test.md`(v2) §8에 명시된 대로 CONDITIONAL PASS(DEF-001/DEF-002 Fixed, AC-3 불릿4/AC-5 Postgres 실기동 미검증)다.** 두 판정을 혼동하지 않는다.

---

## v3 재검증 — 1차 검증 (작성자 관점 자가 재검토, 실제 PostgreSQL 확정 검증 이후)
- 검증자(역할): 20년차 QA 겸 개발자(작성자 본인 관점)
- 일시: 2026-09-14 (코디네이터가 Docker PostgreSQL을 제공한 이후)
- 입력: 코디네이터 지시(실제 DB로 AC-3/AC-5 확정 검증 + GRANT/REVOKE 권한 분리 검증 포함), 컨테이너 `stock-screener-db`(postgres:16-alpine), 역할 `migrator`/`batch_worker`/`api_service`(devpass)
- 체크리스트
  - [x] AC-3/AC-5의 "확정 PASS" 판정 각각이 실제 SQL 쿼리 결과에 근거하는지 재확인 — `unit-01-test.md` TC-064/067/068/072/073의 "실제 결과" 열이 실행 로그(psql 출력)를 그대로 인용했는지 재대조, 일치.
  - [x] GRANT 미비(DEF-003)를 "코드를 안 읽어서 놓친 것"이 아니라 "실제로 재현해서 발견한 것"인지 — TC-065에서 GRANT 적용 **이전**에 먼저 실패를 관찰한 순서를 재확인(짜맞추기 아님).
  - [x] `updated_at` 미갱신(DEF-004)이 이론적 코드 리딩이 아니라 실측인지 — TC-069에서 `pg_sleep(2)`로 시간차를 두고 실제 값 변화 유무를 관측했는지 재확인.
  - [x] `raw_internal` proxy 테스트(TC-071)가 실제 UNIT-01/UNIT-02 코드에 대한 검증인 것처럼 과장 서술되지 않았는지 — 문서 전반(§0-2, §4-3, §5, §7)에서 "대역/proxy이며 실제 산출물은 아니다"라는 제한이 일관되게 명시됐는지 재확인.
  - [x] 6단계가 코드를 직접 고치지 않았는지 — GRANT는 라이브 테스트 DB에만 적용했고 마이그레이션 파일·CLI 코드는 변경하지 않았음을 재확인(git 추적 대상 파일에 diff 없음).
  - 발견된 결함(문서 자체의): §8 "AC-3/AC-5는 확정 PASS"와 "UNIT-01 전체는 CONDITIONAL PASS"가 병기되어 얼핏 모순처럼 읽힐 수 있는 서술 구조.
- 조치 내용: 최종 판정 요약에 두 판정이 서로 다른 층위(개별 AC vs 전체 유닛)임을 한 번 더 명시하는 문장을 추가(반영 완료).

## v3 재검증 — 2차 검증 (독립 심사자 관점 — "오늘 처음 이 v3 결과서를 받아본 9단계 보안검증/10단계 배포테스터")
- 검증자(역할): 09-security-reviewer/10-deployment-tester 관점 — "인프라가 실제로 준비된 상태에서 배포 가능한가"를 의심
- 일시: 2026-09-14
- 체크리스트
  - [ ] → [발견] DEF-003의 심각도(Medium) 근거가 "가용성 문제"라는 설명만으로는, 왜 이 결함이 오래 발견되지 않을 수 있는지(운영 관점의 위험)가 부족했음.
  - **재검토 결과**: `GET /health`가 `SELECT 1`만 실행하고 `reference` 스키마를 전혀 확인하지 않으므로, DEF-003이 실제 프로덕션에 존재해도 헬스체크는 계속 `ok`를 반환한다 — 이 사실을 `unit-01-test.md` §6 DEF-003 설명에 추가해 심각도 근거를 보강했다.
  - [x] AC-3/AC-5 확정 PASS와 DEF-003/004 Open 상태가 논리적으로 모순되지 않는지 — AC 문면(불릿 텍스트)을 오라클로 삼아 판정하되 문면 밖 리스크는 별도 결함으로 등록하는 방식이 v1의 TC-001/AC-1 불릿1 사례와 동일한 패턴임을 재확인, 일관성 있음.
  - [x] 라이브 DB에 적용한 GRANT/데이터를 되돌리지 않고 남겨둔 결정이 문서화되어 다음 세션에 혼란을 주지 않는지 — `unit-01-test.md` §3 "환경 관리 원칙" 문단 확인, 명시됨.
  - [x] 대역 스키마(`raw_internal`, TC-071)가 실제로 완전히 삭제되어 향후 UNIT-02 마이그레이션과 충돌 여지가 없는지 — `DROP SCHEMA ... CASCADE` 후 `\dn` 재조회로 부재 확인한 기록이 있는지 재확인, 있음.
  - [x] 코디네이터의 원 질문("UNIT-01 전체가 최종 PASS인지")에 문서가 명확히 답하는지 — §8/최종 판정 요약에서 층위를 분리해 명확히 답변하고 있음을 확인.
- 발견된 결함 목록(문서 자체의): 1건(DEF-003 심각도 근거 보강 필요, 조치 완료). 코드 결함은 0건(신규 발견 없음, 이미 1차 검증에서 반영됨).
- 조치 내용: `unit-01-test.md` §6 DEF-003 설명에 헬스체크 관련 문장 추가(완료), 최종 판정 요약에 층위 구분 문장 추가(완료).

## v3 재검증 최종 판정
- [x] PASS (이 **v3 테스트 결과서 자체**의 완결성·추적성·근거 충분성 기준으로 결함 0건, 최소 2회 검증 완료 — 발견된 2건의 서술 보강 사항은 모두 v3 1차/2차에서 조치됨) — `traceability.md` 갱신 후 다음 단계로 handoff 가능
- 참고: 이 최종 판정은 "v3 테스트 결과서라는 산출물의 품질"에 대한 PASS이며, **테스트 대상인 UNIT-01 코드 자체의 판정은 `unit-01-test.md`(v3) §8/최종 판정 요약에 명시된 대로 CONDITIONAL PASS다.** AC-3(전 불릿)/AC-5(전 불릿)는 실제 PostgreSQL로 **확정 PASS**하지만, 이번 실제 환경 검증에서 새로 발견한 **DEF-003(Medium, Open)**이 해결되기 전에는 UNIT-01을 무조건적인 PASS로 볼 수 없다(9~11단계 진입 전 필수 해결 조건). DEF-004(Low, Open)는 배포 차단 사유는 아니나 권고 수정 대상이다. 두 층위(개별 AC 판정과 전체 유닛 판정)를 혼동하지 않는다.

---

## v4 재검증 — 1차 검증 (작성자 관점 자가 재검토, DEF-003/DEF-004 수정 확인 이후)
- 검증자(역할): 20년차 QA 겸 개발자(작성자 본인 관점)
- 일시: 2026-09-15
- 입력: 코디네이터 지시(5단계의 자체 검증 결과를 신뢰하지 말고 4개 항목 독립 재확인), `unit-01-note.md`(v3, §0-b DEF-003/004 수정 이력), `db/alembic/versions/0002_grant_reference_privileges.py`, `scripts/load_calendar.py`(수정)
- 체크리스트
  - [x] 5단계가 남겨둔 DB 상태(0002 적용됨, GRANT 있음)를 그대로 이어받아 검증한 게 아니라 완전히 초기화한 뒤 처음부터 재구성했는지 — `unit-01-test.md` TC-075에서 `alembic downgrade base` 실행 로그와 초기화 후 `\dn`/`alembic_version` 결과가 실제로 인용되어 있는지 재확인, 있음.
  - [x] api_service 거부 확인(TC-076)이 5단계 기록(UPDATE만)보다 범위가 넓은지, 그 사실이 문서에 명시됐는지 확인 — INSERT/DELETE까지 포함, "5단계보다 범위 확장"이라는 문구 확인.
  - [x] updated_at 갱신(TC-079)이 5단계와 다른 오염값으로 독립 재현됐는지 — `WRONG_TEST_VALUE_V4`라는 6단계 고유 값 사용 확인, 5단계 note의 `WRONG_TEST_VALUE`와 구분됨.
  - [x] TC-080("불변 시에도 매번 갱신")이 결함으로 잘못 등록되지 않았는지 — §6 결함 표에 DEF-005 등으로 신설되지 않고 "관찰 사항"으로만 §4-4/§7에 기록됐는지 재확인, 맞음(근거: 어떤 AC/DEF도 "불변 시 불변"을 요구하지 않음).
  - [x] PASS 전환의 근거가 "v3 §8이 명시한 유일한 조건(DEF-003 Fixed)"과 정확히 대응하는지 — v3 §8 3번 문구를 재조회해 조건 문구와 이번 해소 근거가 1:1 대응하는지 확인, 대응됨.
  - 발견된 결함(문서 자체의): 없음.
- 조치 내용: 없음.

## v4 재검증 — 2차 검증 (독립 심사자 관점 — "오늘 처음 이 v4 결과서를 받아본 8단계 전체테스터/9단계 보안검증자")
- 검증자(역할): 08-full-tester/09-security-reviewer 관점 — "PASS라는 결론만 보고 다음 단계로 넘어가도 되는가"를 의심
- 일시: 2026-09-15
- 체크리스트
  - [ ] → [발견] §7에 여전히 4개 리스크 항목(raw_internal 실제 재검증, uvicorn 미기동, 역할 프로비저닝 문서화 공백, updated_at 특성)이 남아 있는데 "PASS(조건부 아님)"로 판정한 것이 모순 아닌지 재검토 필요.
  - **재검토 결과**: 모순 아님으로 확인. 4건 모두 (a) 현재 UNIT-01의 AC/REQ 문면이 요구하지 않는 범위(raw_internal 실제 스키마는 UNIT-02 산출물, uvicorn 실기동은 어떤 AC도 명시적으로 요구한 적 없음), (b) 이미 안전한 방향으로 실패하도록 처리됨(역할 프로비저닝 — 없으면 명시적 에러), (c) 결함이 아닌 구현 트레이드오프(updated_at)다. `unit-01-test.md` §7/§8 6번에 "이 항목들은 조건이 아니라 후속 유닛/문서화 작업으로 순연한다"는 문구가 일관되게 있는지 재확인, 있음.
  - [x] 6단계가 5단계 코드를 신뢰 없이 검증했다는 근거가 매 TC마다 구체적으로 남아 있는지(단순히 "확인했다"는 서술이 아닌지) — TC-075(완전 초기화 로그), TC-076(범위 확장 근거), TC-079(다른 오염값) 각각에 구체적 재현 절차가 있는지 재확인, 있음.
  - [x] 코디네이터의 4개 지시사항이 전부 명시적으로 다뤄졌는지 최종 대조 — 지시1(TC-075/081), 지시2(TC-076/077), 지시3(TC-078/079/080), 지시4(TC-082/083/084) 1:1 대응 확인.
  - [x] traceability.md가 이 PASS 판정과 정확히 일치하는 문구로 갱신될 예정인지 — 이 로그 작성 직후 traceability.md 갱신 시 재대조 예정으로 남김(다음 조치).
  - [x] 결함 목록(§6)에 DEF-001~004 네 건이 전부 Fixed로 표시되고 Open 상태인 결함이 0건인지 최종 확인 — `unit-01-test.md` §6 재조회, 4건 모두 Fixed, Open 없음 확인.
- 발견된 결함 목록(문서 자체의): 0건.
- 조치 내용: 없음.

## v4 재검증 최종 판정
- [x] PASS (이 **v4 테스트 결과서 자체**의 완결성·추적성·근거 충분성 기준으로 결함 0건, 최소 2회 검증 완료) — `traceability.md` 갱신 후 다음 단계(7단계 통합테스트)로 handoff 가능
- 참고: 이 최종 판정은 "v4 테스트 결과서라는 산출물의 품질"에 대한 PASS이며, **테스트 대상인 UNIT-01 코드 자체의 판정도 이번에는 동일하게 `unit-01-test.md`(v4) §8/최종 판정 요약에 명시된 대로 PASS(조건부 아님)다.** DEF-001(High)/DEF-002(Low)/DEF-003(Medium)/DEF-004(Low) 전부 Fixed로 최종 확인됐고, 6단계는 5단계의 자체 검증 결과를 매 라운드 신뢰하지 않고 독립적으로 재현해 이 결론에 도달했다. 남은 리스크 4건(§7)은 결함이 아니라 후속 유닛/문서화 과제로 명확히 구분해 인계한다.
