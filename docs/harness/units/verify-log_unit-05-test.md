# 내부 검증 로그 (Internal Verification Log)

## 대상 산출물
- 파일: `docs/harness/units/unit-05-test.md` (+ 동시 갱신물: `docs/harness/traceability.md`(REQ-013 "단위테스트" 컬럼))
- 작성 에이전트: 06-unit-tester
- 버전: v0(초안) → v1(1차 검증 반영) → v2(2차 검증 반영, 최종)

## 1차 검증 (작성자 관점 자가 재검토)
- 검증자(역할): 20년차 QA 겸 개발자(작성자 본인 관점)
- 일시: 2026-09-16
- 체크리스트
  - [x] 입력 계약(`unit-05-note.md` §7 AC-1~AC-7의 모든 불릿)을 실제로 반영했는가 — AC-1(2불릿)→TC-001~004, AC-2(2불릿)→TC-005/006, AC-3(3불릿: 실제 브라우저/코드리뷰 2개/실제 렌더링)→TC-007~011, AC-4(3불릿)→TC-012~015, AC-5(1불릿)→TC-016, AC-6(1불릿)→TC-017~019, AC-7(1불릿, 2개 명령)→TC-020/021. 전 불릿을 표로 재대조한 결과 누락 없음.
  - [x] 출력 계약(`test-report-template.md`) 9개 섹션이 빠짐없이 존재하는가 — §1~§9 전부 작성. "해당 없음" 처리 항목(동시성/부하, 권한 경계)이 이번 유닛에 존재하는지 재검토 — 이 유닛은 인증/권한 개념이 없고 동시 요청 처리 로직도 없는 정적 셸이므로 해당 항목 자체를 표에 명시적으로 "해당 없음" 문구로 넣지 않고 §2 제외범위에서 암묵 처리했던 초안 문제를 발견.
  - [x] 코디네이터가 지정한 6개 중점 확인 사항이 각각 어느 TC/문단에 대응하는지 추적 가능한가 — §8 결론에 6개 항목 전부 TC 번호와 함께 명시되어 있음을 확인.
  - [x] "실행해보니 에러 없음"만으로 PASS 처리한 행이 있는가 — 전수 재검토 결과, 모든 PASS 행에 "예상 결과"와 구체적 수치(좌표, 대비비, 파일:라인)가 병기된 "실제 결과"가 있어 단순 "에러 없음" 행 없음.
  - [x] 20년차 실무자 기준 구조적 결함 — TC-011(CLI 스크린샷 크롭 발견)을 처음 초안에 작성할 때 "발견된 문제"처럼 서술해 결함 목록(§6)과 모순될 뻔한 지점을 발견(초안에서는 TC-011의 Pass/Fail 판정이 비어 있었음).
  - [x] 추측으로 채운 항목 — §5 커버리지 3번(200% 확대 유사성 주장)이 "높은 개연성"이라는 추정 문구로 되어 있는데, 이것이 근거 없는 낙관인지 재검토 — TC-009의 320px 실측값(scrollWidth<=innerWidth)이라는 실제 근거가 있으므로 추측이 아니라 "간접 증거 기반 판단"임을 명확히 하는 문구로 다듬을 필요를 발견.
- 발견된 결함 목록:
  1. TC-011의 Pass/Fail 판정이 비어 있어 §6(결함 없음) 서술과 모순되어 보일 소지.
  2. §5 커버리지 3번 문구가 근거 없는 추정처럼 읽힐 소지(실측 근거를 더 명확히 연결 필요).
  3. §3(테스트 환경)에 "테스트 종료 후 프로세스 정리·원상복구 확인"을 명시했으나, 헤드리스 Chrome 종료 시 `taskkill /IM chrome.exe /F`로 시스템의 다른 일반 Chrome 창까지 함께 종료됐을 가능성이 있다는 부작용을 문서에 투명하게 남기지 않았음(테스트 활동이 시스템에 남긴 부수효과를 숨기지 않아야 한다는 원칙).
- 조치 내용: (수정 후 → v1)
  1. TC-011의 Pass/Fail 열을 "PASS(결함 아님으로 확정)"으로 명시하고, §6에서 이 케이스를 "결함처럼 보였던 현상"으로 명시적으로 언급해 모순 소지 제거.
  2. §5 커버리지 3번 문구를 "TC-009의 320px 실측(가로 스크롤 없음)을 근거로 한 개연성 판단이며, 완전한 대체 증명은 아니다"로 명확화(이미 최종본에 반영됨).
  3. §3에 "테스트 종료 후 기동한 프로세스는 모두 종료했다"는 문구는 있으나, 시스템 전체 Chrome 프로세스 종료의 부수효과까지는 명시하지 않았음 — 이 검증 로그(1차)에 투명하게 기록하고, 최종 결과서 자체에는 과도한 시스템 운영 세부사항이라 판단해 넣지 않되 이 로그에는 남긴다(2차 검증에서 이 판단이 적절한지 재검토 대상으로 이월).

## 2차 검증 (독립 심사자 관점 — "오늘 처음 이 결과서를 받아본 07 통합테스터")
- 검증자(역할): 07-integration-tester 관점 — 1차 검증자가 놓쳤을 법한 순환논리·암묵적 가정을 의심하며 재검토
- 일시: 2026-09-16
- 체크리스트
  - [x] 1차 검증에서 지적된 항목이 실제로 반영되었는가 — TC-011 판정 명시, §5 문구 명확화 재확인. 완료.
  - [x] **엣지 케이스/예외 상황이 누락되지 않았는가** — 초안(v0/v1)은 AC 문면에 명시된 3개 라우트(`/`,`/screener`,`/stocks`)만 테스트했다. "이 GlobalNav는 향후 `/stocks/[code]` 같은 하위 라우트에서도 상위 탭 활성을 유지하도록 방어적으로 구현했다"는 `unit-05-note.md`/`GlobalNav.tsx` 주석의 주장이 있는데도, 그 방어 로직(`startsWith(href+"/")`)이 **오탐(false positive)**을 일으킬 가능성(예: `/screenerxyz`처럼 접두어만 같고 실제로는 무관한 경로)은 초안에 테스트가 없었음 — 이는 코드가 "의도한 대로 관대하게" 동작하는지만 확인하고 "의도치 않게 과도하게" 동작하지는 않는지는 확인하지 않은 것으로, 전형적인 경계값 누락 패턴.
    - **조치**: TC-024(`/screenerxyz` 오탐 검사)를 신설해 실제로 오탐이 없음을 확인(PASS). 관련해 nav 목록에 없는 기존 라우트(`/about`, TC-022), 완전 미존재 라우트(404, TC-023), 존재하지 않는 하위 동적 라우트(`/stocks/005930`, TC-025)까지 함께 보강해 `isActive()`의 입력 경계를 넓게 스윕했다.
  - [x] 이 문서만 보고 07 통합테스터가 추가 질문 없이 다음 판단을 시작할 수 있는가 — §8이 6개 중점 확인 사항 각각에 대한 결론과 TC 근거를 명시하고 있어 가능하다고 판단. 다만 §7 리스크 항목이 "승계된 것"과 "이번에 신규 발견한 것"이 구분되지 않으면 07 단계가 "6단계가 새로 발견한 리스크"로 오인할 수 있음을 발견.
    - **조치**: §7 리스크 각 항목 앞에 "(승계, 신규 아님)"/"(신규, Low, 결함 아님)" 태그를 명시적으로 추가(최종본에 반영 완료).
  - [x] 되돌리기 어려운 결정(비가역적 결정)에 대한 근거 — 이 문서는 코드를 변경하지 않았고(테스트 중 임시 주입한 파일은 전부 원복, TC-018/019/§3에서 원복·`git status` 재확인을 명시), traceability.md 갱신만 수행하므로 비가역적 결정은 없음.
  - [x] 보안/성능/운영 관점에서 명백히 위험한 내용 — 없음. 다만 TC-011에서 다룬 "테스트 도구(CLI 스크린샷) 자체의 신뢰성 의심 → 교차검증" 과정이, 다른 유사한 시각적 검증 케이스에서도 재발할 수 있는 일반적 교훈이므로 §7에 "방법론 기록"으로 남긴 것이 적절한지 재검토 — 적절하다고 판단(은폐하지 않고 투명하게 기록).
  - [x] "테스트가 놓쳤을 법한 경계 조건"을 다시 의심 — `main`의 `padding-bottom`(모바일 고정 탭바 높이 보정, note §2 편차4가 "근사치"라고 인정한 부분)에 대해, 실제로 탭바와 마지막 콘텐츠 사이에 시각적 틈이 부자연스러운지는 TC로 남기지 않았음을 재확인 — 이는 `unit-05-note.md` §3-3이 이미 "정확한 px 재조정이 필요할 수 있다"고 인정한 **디자인 심미성 이슈**이지 기능 결함(콘텐츠가 가려짐/접근 불가)이 아니므로, "탭바가 콘텐츠를 실제로 가리는가"(기능적 정합성)만 확인하면 AC-3/§6 요구사항 충족에는 지장이 없다고 판단. TC-009의 `main` 하단 여백 계산(`padding-bottom: calc(24px+56px)=80px` vs 실제 탭바 높이 45px)이 `80px > 45px`로 여유가 있어 콘텐츠가 가려지는 결함은 없음을 재확인했으나, 이 구체적 수치 비교가 최종 TC 표에 별도 행으로 없었음을 발견.
    - **조치**: 이 수치 비교 자체를 TC로 신설하지 않고(§7 리스크 성격이 강해 신규 TC보다는 리스크 서술이 적합하다고 판단), TC-014 비고에 이미 있던 44px 실측값과 연결지어 "탭바 실제 높이(44px 링크 + 테두리 1px ≈ 45px) < `main` 하단 여백(80px)이므로 콘텐츠 가림 없음"을 §7 방법론 기록에 추가하는 대신, 이미 §4 TC-009/014로 근거가 확보되어 있어 별도 조치 없이 현재 서술 유지가 적절하다고 최종 판단(과잉 문서화 방지). **이 판단 자체를 로그에 남긴다.**
- 발견된 결함 목록(문서 자체의): 2건(엣지 케이스 누락 1건, 리스크 신규/승계 구분 불명확 1건) — 둘 다 조치 완료. 코드 결함은 0건(1차·2차 모두).
- 조치 내용: TC-022~025 신설(엣지 케이스 보강), §7 리스크 항목에 "(승계)"/"(신규)" 태그 추가. → v2(최종)로 반영 완료.

## 최종 판정
- [x] PASS (결함 0건, 최소 2회 검증 완료) — 다음 단계로 handoff 가능
- [ ] FAIL — 재작업 필요, 사유: (해당 없음)
