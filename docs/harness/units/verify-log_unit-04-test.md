# 내부 검증 로그 (Internal Verification Log)

## 대상 산출물
- 파일: `docs/harness/units/unit-04-test.md` (+ 동시 갱신물: `docs/harness/traceability.md`(REQ-006/007/008/009/010 "단위테스트" 컬럼))
- 작성 에이전트: 06-unit-tester
- 버전: v0(초안) → v1(1차 검증 반영) → v2(2차 검증 반영, CONDITIONAL PASS 최종) → **v3(DEF-001/DEF-002 수정에 대한 독립 재검증, unit-04-test.md v2에 대응, 최종 PASS)**

## 1차 검증 (작성자 관점 자가 재검토)
- 검증자(역할): 20년차 QA 겸 개발자(작성자 본인 관점)
- 일시: 2026-09-15
- 체크리스트
  - [x] 입력 계약(`unit-04-note.md` §7 AC-1~AC-5의 모든 불릿)을 실제로 반영했는가 — AC-1(6불릿)→TC-006~012, AC-2(5불릿)→TC-013~017, AC-3(4불릿)→TC-019/020~024/025/026, AC-4(2불릿)→TC-028~031, AC-5(1항목)→TC-032~033으로 전 불릿 1:1 매핑 재확인. 누락 없음.
  - [x] 출력 계약(`test-report-template.md`) 9개 섹션이 빠짐없이 존재하는가 — §1~§9 전부 작성, "해당 없음" 처리한 항목(동시성/부하, 권한 경계)에 사유(인증 자체 없음)를 명시했는지 재확인, 있음.
  - [x] 코디네이터가 지정한 5개 중점 확인 사항이 각각 어느 TC/문단에 대응하는지 추적 가능한가 — (1) 면책 배너 실렌더링→TC-006~012, (2) 금지어 다른 케이스 재현→TC-020~024(5종, "추천 종목입니다"와 겹치지 않게 선정) + TC-026(대조표 전수 대조), (3) 기준시각 배지 서버값 그대로 표시 + 목업 검증 방식→TC-013~017/TC-030 서술 및 §2 제외범위에서 "아직 실 데이터 연결 없음, dead code임을 grep으로 확인"이라고 명시, (4) REQ-009 grep 충분성 판단·보강→TC-030/031(블랙박스 동일성 테스트) 신설 및 TC-030 비고에 판단 근거 서술, (5) 백엔드 회귀→TC-001/002. 5개 전부 문서 내 위치를 특정할 수 있음.
  - [x] "실행해보니 에러 없음"만으로 PASS 처리한 행이 있는가 — 전수 재검토 결과 모든 PASS 행에 "예상 결과"와 "실제 결과"가 구체적으로 병기되어 있고 단순 "에러 없음"으로만 채운 행은 없음(예: TC-005는 라우트 개수·로그 문구까지 명시).
  - [x] DEF-001/DEF-002의 심각도(Medium)가 "현재 영향 없다"는 이유로 근거 없이 낮게 매겨진 것은 아닌지 자가 검토 — 초안에서 "현재는 dead code/단일 라인이라 영향 없음"이라는 문장만 있고, "그럼에도 왜 Low가 아니라 Medium인지"(향후 실제 화면 연결 시 전체 페이지 크래시로 이어질 잠재력, REQ-008 CI 게이트의 설계 의도 자체를 무력화할 수 있는 구조적 결함이라는 점)에 대한 명시적 근거가 부족했음.
  - [x] TC-030(블랙박스 동일성 검증)이 코디네이터의 "grep 검증 방식이 충분한지 판단하고 필요하면 보강" 지시에 실제로 답하는 문장인지 확인 — 초안에는 테스트 자체는 있었으나, "왜 grep만으로 부족한가"에 대한 명시적 논거(코드에 없는 방식—ASGI 미들웨어 스택 등—으로 개인화할 가능성을 grep이 배제 못한다는 점)가 비고란에 없었음.
- 발견된 결함 목록:
  1. DEF-001/DEF-002 심각도 근거 설명 부족(위 참조).
  2. TC-030 비고에 "grep 검증이 왜 불충분한지"에 대한 명시적 논거 부족.
  3. §8 결론에서 "AC 범위를 벗어난 위험 기반 테스트에서 발견한 결함을 은폐하거나 축소하지 않았다"는 규칙 I 관련 문장이 없어, 심사자가 "AC 범위 밖이니 무시해도 되는 결함 아닌가"로 오독할 위험.
- 조치 내용: (수정 후 → v1)
  1. §6 DEF-001/DEF-002 설명에 "왜 Medium인지"(잠재적 전체 페이지 크래시, CI 게이트 무력화 가능성)를 각각 추가.
  2. §4 TC-030 비고에 "5단계의 grep 기반 정적 검증의 한계(코드에 없는 코드는 grep으로 못 찾는다, 런타임 사실을 증명 못한다)"를 명시적으로 서술.
  3. §8 결론에 규칙 I 인용 문장("근거 없이 위험을 낮게 평가하지 않는다")을 추가해 CONDITIONAL PASS 판단의 정당성을 명확히 함.

## 2차 검증 (독립 심사자 관점 — "오늘 처음 이 결과서를 받아본 07 통합테스터")
- 검증자(역할): 07-integration-tester 관점 — 1차 검증자가 놓쳤을 법한 순환논리·암묵적 가정을 의심하며 재검토
- 일시: 2026-09-15
- 체크리스트
  - [ ] → [발견] TC-013~017(AC-2 렌더링 검증)이 "임시 QA 라우트"를 만들어서 검증했다고 서술하는데, 이 임시 라우트가 실제로 삭제되고 원래 빌드가 재현되는지(즉, 테스트가 산출물에 영구적인 흔적을 남기지 않았는지) 이 문서만으로는 확인할 수 없었음 — "코드를 직접 고치지 않는다"는 6단계 원칙 위반 여부가 불명확.
    - **재검토 결과**: 실제로 `frontend/src/app/qa-freshness-temp/` 디렉터리를 생성 후 검증이 끝나자마자 삭제했고, 삭제 후 `npm run build`를 재실행해 라우트가 다시 3개(`/`,`/_not-found`,`/about`)로 돌아온 것과 `git status`(전체가 아직 미추적 상태라 diff 비교는 의미 없으나 `find`로 디렉터리 자체 부재)를 확인한 사실이 세션 로그에는 있으나 `unit-04-test.md` 본문에는 "검증 후 즉시 삭제"라고만 짧게 언급되어 있어 재현성이 약함. TC-013 비고에 "삭제 후 `npm run build` 재실행으로 원상복구(라우트 3개로 복귀) 확인"이라는 문장을 추가해 자기완결성을 높였다.
  - [x] DEF-002(멀티라인 우회) 재현 절차가 이 문서만 보고 재현 가능한 수준으로 구체적인가 — 파일 경로(`frontend/src/lib/` 임시 `.ts`)와 내용(백틱 멀티라인 "손실보\n전")이 명시되어 있어 재현 가능함을 확인.
  - [x] TC-030의 "요청자를 구분하지 않는다"는 결론이 순환논리가 아닌지 — DI로 오버라이드한 것은 리포지토리(DB 접근)뿐이고 라우트 핸들러 자체(헤더/쿠키 처리 로직)는 실제 프로덕션 코드 그대로 실행되므로, "테스트가 검증하고 싶은 대상"(핸들러가 헤더/쿠키를 무시하는지)과 "테스트가 실제로 실행한 코드"가 일치함을 재확인 — 순환논리 아님.
  - [x] CONDITIONAL PASS의 조건이 형식적이지 않은지 — "UNIT-06~08 착수 전 DEF-001 해결", "멀티라인 카피 도입 전 DEF-002 해결"이라는 구체적 트리거 조건이 있어 형식적이지 않음을 확인. 다만 이 조건을 누가 어떻게 게이트할지(예: UNIT-06 착수 전 자동 체크 여부)가 문서에 없어 사람이 수동으로 기억해야 하는 약점이 있음 — 이는 이번 문서의 결함이 아니라 traceability.md 비고 갱신으로 보완할 사항으로 판단(다음 조치에서 반영).
  - [x] 이 문서만 보고 07 통합테스터가 추가 질문 없이 다음 판단을 시작할 수 있는가 — §8이 "REQ-006/007/009/010 PASS, REQ-008 CONDITIONAL PASS, 전체 판정 CONDITIONAL PASS"로 층위를 명확히 구분해 서술하고 있어 가능하다고 판단.
  - [x] 보안/규제 관점에서 명백히 위험한 내용이 누락되지 않았는가 — DEF-001/DEF-002 모두 "현재 영향 없음"과 "향후 위험"을 함께 서술해 은폐가 없음을 재확인. REQ-009 검증이 3개 엔드포인트 한정이라는 제약도 §7에 명시되어 누락 없음.
- 발견된 결함 목록(문서 자체의): 1건(TC-013 비고의 재현성 보강 필요, 위에서 조치 완료). 코드 결함(DEF-001/002)은 이미 1차 검증에서 서술 보강했고 신규 코드 결함은 없음.
- 조치 내용: TC-013 비고에 임시 라우트 삭제 후 원상복구 확인 문장 추가(완료). traceability.md 갱신 시 DEF-001/DEF-002를 REQ-006/REQ-008 비고란에 "후속 유닛 착수 전 해결 필요" 조건으로 명시(다음 조치, 실제 traceability.md 갱신에서 반영).

## 3차 검증 (v3 — DEF-001/DEF-002 수정에 대한 독립 재검증, `unit-04-test.md` v2 대응)
> v1/v2 라운드는 `unit-04-test.md` v1(CONDITIONAL PASS)에 대한 검증이었다. 5단계가 DEF-001/DEF-002를 조치한 뒤(코디네이터 지시), 이 3차 라운드는 그 재작업을 반영한 `unit-04-test.md` v2에 대한 검증이다.

### 1차 재검증 (작성자 관점 자가 재검토)
- 검증자(역할): 20년차 QA 겸 개발자(작성자 본인 관점)
- 일시: 2026-09-16
- 체크리스트
  - [x] DEF-001/DEF-002 재발 방지를 위해 note가 신설한 AC-2/AC-3 신규 불릿을 실제로 100% 커버했는가 — AC-2 신규 불릿(빈 문자열/malformed 입력에 폴백 반환, 정상 회귀 없음) → TC-042(직접 호출 5종)·TC-043(실제 렌더링 6종)으로 커버, AC-3 신규 불릿(멀티라인 분리 시 탐지) → TC-034~037(3가지 변형 + 원복)로 커버. 누락 없음.
  - [x] 5단계의 자체 재현 보고("exit code 1로 정확히 탐지됨을 확인", "예외 없이 폴백 문구 반환 확인")를 그대로 인용해 PASS 처리한 곳이 있는가 — 전수 재검토 결과, 모든 TC-034~045가 6단계가 **직접 실행한** 명령과 출력을 인용하고 있으며 note의 서술을 그대로 옮겨적은 행이 없음을 확인. 단, 백엔드 회귀(TC-052/053)는 note와 동일한 수치(79 passed)가 나왔는데, 이것이 "note를 베낀 것"이 아니라 "독립 실행 결과가 우연히 동일한 것"인지 재확인 — 실행 로그의 pytest 경고 문구("httpx" deprecation)까지 동일하게 재현되는 것을 직접 확인해 실제로 재실행했음을 재확인.
  - [x] TC-036(정규식 패턴 멀티라인 분리) 최초 시도가 bash printf 이스케이프 문제로 실패했던 과정을 "결함처럼 보였다가 테스트 오류로 판명"된 사례로 투명하게 남겼는가 — §4-2 TC-036 비고에 명시했으나, 초안에서는 "재구성 후 재확인"이라고만 되어 있어 무엇이 잘못됐고 어떻게 확인했는지(`cat -A`로 실제 바이트 확인)가 불충분했음.
  - [x] DEF-008/DEF-009를 "AC 범위 밖"이라는 이유로 실제보다 가볍게 서술하지 않았는지 — 초안에서 DEF-008의 "왜 지금은 발생하지 않는지"에 대한 근거(호출부 코드 인용)가 다소 추상적이었음.
  - [x] §8 결론이 "5단계 재작업 결과를 신뢰하지 않고 독립 재현했다"는 서술에 그치지 않고, 이 저장소의 다른 유닛(UNIT-01/02/03)의 Low·Open 결함 처리 관행과 일관된 근거를 제시하는지 확인 — 초안에는 관행 언급이 없어 "왜 Low 결함 2건이 있는데도 PASS인가"에 대한 판단 근거가 약했음.
- 발견된 결함 목록:
  1. TC-036 비고의 "테스트 자체 오류 발견 및 교정 과정" 서술 부족.
  2. DEF-008 "현재 발생하지 않는 이유" 근거가 추상적.
  3. §8에 Low·Open 결함이 최종 PASS를 막지 않는 판단 근거(레포 내 기존 관행과의 일관성)가 부족.
- 조치 내용: (수정 후 → v2 문서 최종본)
  1. TC-036 비고에 "최초 시도가 JS 문자열 이스케이프를 텍스트로 잘못 기입해 미탐지됐던 실수를 발견해 실제 물리적 줄바꿈으로 재구성 후 재확인(TC-041 참조)"를 명시.
  2. DEF-008 설명에 `DataFreshnessBadge.tsx`의 실제 호출부 코드(삼항 분기, 타입 선언)를 구체적으로 인용해 근거 보강.
  3. §8에 "이 저장소의 기존 판정 관행(UNIT-01 DEF-004, UNIT-02 DEF-005, UNIT-03 DEF-006/007 — 전부 Low·Open이면서 PASS)과 일관되게" 문장을 추가.

### 2차 재검증 (독립 심사자 관점 — "오늘 처음 이 v2 결과서를 받아본 07 통합테스터")
- 검증자(역할): 07-integration-tester 관점
- 일시: 2026-09-16
- 체크리스트
  - [x] 1차 재검증에서 지적된 3건이 실제로 반영되었는가 — TC-036/DEF-008/§8 전부 반영 확인.
  - [x] DEF-001의 "Fixed" 판정이 원래 결함(빈 문자열/malformed 문자열)의 범위를 정확히 충족하는지, 아니면 판정 범위를 몰래 넓히거나 좁힌 것은 아닌지 — v1 TC-018이 지목한 정확히 그 두 입력(`''`, `'not-a-date'`)과 note의 AC-2 신규 불릿이 명시한 범위가 TC-042/043과 1:1로 대응함을 재확인. 범위를 넓힌 것(TC-044/045, null/0/undefined)은 DEF-001의 "Fixed" 판정과 분리해 별도 DEF-008로 기록했으므로 혼동 없음.
  - [x] DEF-002의 "Fixed" 판정이 원래 결함(물리적 줄바꿈 분리)의 범위를 정확히 충족하는지 — v1 TC-027과 동일한 시나리오(TC-034)가 정확히 재현됐고, 추가 변형(TC-035/036)도 구조적 해결임을 뒷받침하는 보강 증거로만 사용됐을 뿐 "Fixed" 판정 자체를 흐리지 않음. DEF-009(이스케이프 우회)는 DEF-002와 다른 결함으로 명확히 분리되어 있어 "DEF-002가 완전히 해결되지 않았다"는 오독 위험이 없음을 확인.
  - [x] 오탐(false positive) 검증(TC-038/039)이 실제로 "과도하게 민감해지지 않았는지"를 검증하는 데 충분한 강도인지, 아니면 형식적인 케이스만 넣은 것은 아닌지 재검토 — TC-039에서 "인접 단어 경계 근접" 케이스를 시도했으나 한국어 문법(서술어 어간에 어미가 공백 없이 직접 결합) 때문에 진짜 위험한 경계 사례를 인위적으로 구성하기 어려웠다는 한계를 6단계가 스스로 인정하고 §7에 기록했는지 확인 — 기록되어 있음(테스트 설계의 한계를 숨기지 않음).
  - [x] `git status --short`로 작업 트리 클린 상태를 매 케이스 후 확인했다는 서술이 실제로 근거가 있는지, 아니면 관행적 문구인지 — 세션 로그상 TC-037/TC-039-cleanup/TC-043 이후 실제로 `git status --short` 실행 결과(빈 출력)를 확인한 기록이 있음을 재확인.
  - [x] 이 문서만 보고 07 통합테스터가 추가 질문 없이 "UNIT-04는 최종 PASS다"라고 판단하고 다음 업무단위 통합테스트를 시작할 수 있는가 — §8이 판정 근거·Low 결함 2건의 비차단 사유·향후 권고를 명확히 구분해 서술하고 있어 가능하다고 판단.
  - [x] `traceability.md` 갱신이 이 문서의 v2 최종 PASS 판정 및 DEF-008/DEF-009 신규 등록 내용과 정확히 대응하는지 상호 대조 — 별도로 traceability.md 갱신 시 확인 예정(다음 조치).
- 발견된 결함 목록(문서 자체의): 0건. 코드 결함(DEF-001/002 Fixed, DEF-008/009 신규)은 이미 재현 근거와 함께 정확히 기록되어 추가 조치 불필요.
- 조치 내용: 없음(추가 결함 미발견). `traceability.md` 갱신에서 REQ-006/REQ-008 "단위테스트" 컬럼을 이 v2 문서의 최종 PASS 판정으로 갱신하고 DEF-008/DEF-009를 비고에 반영(다음 조치).

## 최종 판정
- [x] PASS (결함 0건, 최소 2회 검증 완료) — 다음 단계로 handoff 가능
- 참고(v1/v2 라운드 당시): 당시 최종 판정은 "`unit-04-test.md`(v1)라는 테스트 결과서 자체의 완결성"에 대한 PASS였으며, 테스트 대상인 UNIT-04 코드 자체의 판정은 CONDITIONAL PASS였다.
- **참고(v3 라운드, 최종)**: 이번 3차 검증(1차+2차 재검증, 결함 0건)은 `unit-04-test.md` **v2**(DEF-001/DEF-002 재검증 반영판)의 완결성·추적성·근거 충분성에 대한 PASS이며, **테스트 대상인 UNIT-04 코드 자체의 최종 판정도 `unit-04-test.md` v2 §8에 명시된 대로 PASS(조건부 아님)다.** DEF-001/DEF-002는 독립 재현을 통해 Fixed로 확정했고, 위험 기반으로 추가 발견한 DEF-008/DEF-009(둘 다 Low, Open)는 AC 비위반·현재 영향 없음을 근거와 함께 명시해 최종 PASS 판정을 막지 않는 것으로 결론지었다. 문서 검증 판정과 코드 판정이 이번에는 동일하게 "PASS"로 일치한다.
