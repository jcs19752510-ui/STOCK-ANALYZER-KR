# 내부 검증 로그 (Internal Verification Log)

## 대상 산출물
- 파일: `docs/harness/03-system-design.md` (+ 동시 갱신물: `docs/harness/traceability.md`(설계 매핑 컬럼), `docs/harness/decisions.md`(DEC-005~016))
- 작성 에이전트: 03-system-designer
- 버전: v0(초안) → v1 → v2(PASS) → **v3(규칙 F 피드백 루프, 최종 PASS)**

## 1차 검증 (작성자 관점 자가 재검토 — "기획서의 모든 기능 범위가 설계서에서 커버되는가")
- 검증자(역할): 20년차 아키텍트 (작성자 본인 관점)
- 일시: 2026-09-14
- 체크리스트
  - [x] 입력 계약(`02-planning.md` v3 PASS, `traceability.md`, `decisions.md` DEC-001~004)을 실제로 반영했는가 — §0에 승계 사항 요약, §2-2에 이용약관 재확인 대신 승계 근거 명시
  - [x] 출력 계약 9개 섹션(아키텍처/기술스택/데이터모델/API/NFR/보안/운영/트레이드오프/변경이력)이 빠짐없이 존재하는가 — §1~§9 전부 존재
  - [x] `02-planning.md` §4-1 In-Scope REQ-001~013, REQ-022 전 항목이 설계서 어딘가에 매핑되는가 — REQ-001(§3-2, §4-2), REQ-002/003/004(§1-1, §3-2, §4-2, §4-3), REQ-005(§3-3), REQ-006(§3-2, §3-4), REQ-007~010(§6-4), REQ-011(§1-2, §2-2, §3-2, §5-4), REQ-012(§3-2, §3-3), REQ-013(§2-1), REQ-022(§7-4, 의도적 비구현) — 전부 확인, 누락 없음
  - [x] §4-3(기획서) "데이터 가공 원칙" 5개 항목이 설계 전반에 강제 반영됐는가 — §1-1 3중 방어(스키마/DB권한/프로세스 분리)로 아키텍처 레벨 강제, §3-2 원본 컬럼 부재로 스키마 레벨 강제, §4-3(설계서) API 레이어 연결까지 확인
  - [x] REQ-022가 "구현할 기능"으로 오인되지 않도록 명확히 구분됐는가 — §0, §7-4에서 반복 명시, `traceability.md`에도 "의도적으로 코드/화면 설계 없음" 표기
  - [x] REQ-016(인증) Out-of-Scope 근거가 "설계하지 않음"으로 명확히 처리됐는가 — §0, §6-1에 명시
  - [ ] → [발견] 신규 상장 종목처럼 이동평균 계산에 필요한 과거 데이터가 부족한 경우의 결측치 처리 규칙이 §3-2에 없었음(엣지 케이스 누락)
  - [ ] → [발견] `/screen` 엔드포인트의 `page_size` 상한이 없어 악의적/실수 요청 시 과도한 응답 크기를 유발할 수 있음
  - [ ] → [발견] API 시각 표기 타임존(KST) 원칙이 예시 JSON에는 `+09:00`으로 나타나 있으나 §4-1 공통 원칙에 명문화되어 있지 않아, 구현자가 서버 타임존 설정을 임의로 선택할 위험
  - [ ] → [발견] §8-2 미해결 사항에 모니터링/알림 도구 벤더 미확정이 §7-2에서만 언급되고 §8-2 목록에는 빠져 있어 "다음 단계로 명시적 이관"이 안 됨
  - [x] 되돌리기 어려운 결정(DB, 스키마 분리 아키텍처, 배치 스케줄러, 백엔드/프론트엔드 스택)이 `decisions.md`에 근거와 함께 기록됐는가 — DEC-005~009 확인
- 발견된 결함 목록:
  1. 신규 상장 종목 등 이동평균 계산 불가 케이스의 결측치 처리 규칙 누락
  2. `/screen` 페이지네이션 상한 미정의
  3. 타임존 표기 원칙이 공통 원칙 절에 명문화되지 않음
  4. 모니터링 도구 미확정 사항이 §8-2 미해결 목록에서 누락
- 조치 내용: (수정 후 → v1)
  1. §3-2에 결측치 처리 원칙 추가(`null` 반환, 스크리닝 자동 제외, 화면 "데이터 부족" 표시, 0/임의값 대체 금지)
  2. §4-2 `/screen` 파라미터에 `page_size`(기본 50, 최대 200, 초과 시 400) 명시
  3. §4-1에 "모든 시각 값은 Asia/Seoul(KST, UTC+9) ISO 8601"을 공통 원칙으로 명문화
  4. §8-2에 모니터링/알림 채널 구체 도구 미확정 항목(7번) 추가

## 2차 검증 (독립 심사자 관점 — "이 설계서로 실제 구현할 개발자")
- 검증자(역할): 5단계 단위 개발자(05-unit-developer) 관점 — 오늘 처음 이 설계서를 받아 코드를 짜야 하는 입장에서, 애매한 인터페이스나 정의되지 않은 예외 처리를 의심하며 재검토
- 일시: 2026-09-14
- 체크리스트
  - [x] 1차 검증 지적 사항이 실제로 반영됐는가 — 4건 모두 재확인 완료
  - [ ] → [발견] §4-1 에러 코드 표에서 503이 `DATA_PIPELINE_STALE` 하나로만 정의되어 있는데, §5-4 장애대응 표는 "DB 커넥션 풀 고갈/쿼리 타임아웃"도 503으로 반환한다고 서술 — 원인이 전혀 다른 두 상황(데이터 신선도 문제 vs 인프라 장애)이 동일 상태코드/에러코드로 뭉뚱그려져 있어, 개발자가 클라이언트/모니터링 로직에서 두 경우를 구분할 수 없음
  - [ ] → [발견] `/screen`의 `sort_by` 파라미터가 어떤 값을 허용하는지(허용 필드 목록, 오름차순/내림차순 지정 방법)가 정의되어 있지 않아 구현자가 임의로 정해야 하는 상태
  - [ ] → [발견] `derived_metrics_daily.per/pbr/market_cap`을 "파생/요약 성격의 밸류에이션 지표"로만 서술했으나, 채택 데이터소스가 이미 계산된 PER/PBR/시가총액을 원문으로 제공할 가능성이 있음 — 이 경우 "가공 없이 그대로 통과"시키면 §4-3(기획서) 원칙 1을 위반할 소지가 있는데 설계서가 이 구분을 명확히 하지 않음(원본인지 우리가 계산한 것인지 모호)
  - [ ] → [발견] REQ-004 `top_sectors_by_value`, `stock_master.sector`가 참조하는 업종 분류 데이터가 어느 출처에서 오는지 설계서 어디에도 언급이 없음 — 채택 데이터소스가 업종 코드를 포함하는지 확인되지 않은 상태에서 "당연히 있다"고 가정한 것으로 보여, 5단계에서 막힐 수 있는 잠재적 블로커
  - [x] 3중 방어(스키마/DB권한/프로세스 분리)가 실제로 "우회 불가능"한 수준으로 구체적인가 — GRANT 대상 role, 배포 단위 분리, 자격증명 미주입까지 명시되어 있어 코드 레벨에서 우회할 방법이 없음을 확인
  - [x] REQ-005/006 로직에 캘린더 데이터 공백이라는 실제로 발생 가능한 예외 케이스가 "조용한 오답"이 아니라 명시적 에러(424)로 처리되는가 — 확인됨, 프로덕션에서 실제로 발생할 법한 시나리오(연초 캘린더 미등록)까지 서술됨
  - [x] 롤백 전략이 이론이 아니라 실제 테이블 설계(`current_published_batch`)로 뒷받침되는가 — 확인됨, "포인터 미갱신 = 자동 롤백"이 구체적 메커니즘으로 서술됨
  - [x] 되돌리기 어려운 결정에 대한 근거가 `decisions.md`에 있는가, 그리고 NXT 데이터 커버리지처럼 확신이 부족한 판단은 규칙 A 질문 대상인지 재검토했는가 — DEC-010에서 "파급 범위가 작고 스키마가 이미 확장 가능해 재설계 비용이 없다"는 근거로 질문 대상에서 제외한 판단이 타당함을 재확인(스키마상 market 차원이 실제로 존재함을 §3-2에서 교차 확인)
- 발견된 결함 목록:
  1. 503 상태코드가 "데이터 신선도 문제"와 "인프라 장애"라는 서로 다른 원인에 중복 사용되어 있어 에러 처리 로직이 모호함
  2. `/screen`의 `sort_by`/정렬방향 파라미터 명세 누락
  3. PER/PBR/시가총액이 원본 값인지 가공 값인지 경계가 모호해 §4-3(기획서) 원칙 위반 소지
  4. 업종 분류 데이터 출처가 설계서 어디에도 명시되지 않음(잠재적 블로커)
- 조치 내용: (수정 후 → v2, 최종)
  1. §4-1 에러 코드 표에 503 `SERVICE_UNAVAILABLE`(인프라 장애 전용)을 신설해 `DATA_PIPELINE_STALE`(데이터 신선도 전용)과 분리, §5-4 DB 타임아웃 행도 새 코드를 참조하도록 수정
  2. §4-2 `/screen` 파라미터에 `sort_by` 허용값 목록(`return_pct`/`market_cap`/`per`/`pbr`/`volume_anomaly_score`, 그 외는 400)과 `sort_dir`(`asc`/`desc`, 기본 `desc`) 명시
  3. §3-2에 "원천이 이미 계산해 제공하더라도 최소 1단계 이상의 변환(순위/구간화/배율 변환 등)을 거친 값만 `public_serving`에 노출하고, 원문을 반올림 수준으로만 통과시키는 것은 원칙 위반으로 간주한다"는 보수적 원칙을 명문화하고, 이 값도 우선 `raw_internal`에 원문 그대로 적재하도록 함
  4. §8-2 미해결 사항에 8번 항목으로 "업종 분류 데이터 출처 미확정" 추가, 영향 범위(REQ-004 일부 세부 항목에 한정, 핵심 규제 대응 REQ에는 영향 없음)를 명시해 5단계 착수 전 확인 대상으로 이관

## v2 최종 판정 (참고용, v3로 대체됨)
- [x] PASS (v2 기준 결함 0건, 최소 2회 검증 완료) — 4단계(UX 설계) 및 이후 단계로 handoff 가능
- 커버리지 확인: `02-planning.md` §4-1 In-Scope REQ-001~013, REQ-022 = 14개 항목 전부 설계서에 매핑 완료(100%). Out-of-Scope REQ-014~021은 설계 대상에서 의도적으로 제외(사유는 `02-planning.md`/`traceability.md` 승계, 재론 불필요). REQ-016은 예외적으로 "설계하지 않기로 결정"한 근거만 §6-1에 명시.
- 잔여 비차단 미해결 사항(§8-2, PASS를 막지 않음, 다음 단계로 명시적 이관됨): (1) REQ-022 자체(성격상 이 단계에서 해결 불가, 배포 게이트로 이관), (2) NXT 데이터 커버리지 확인, (3) 휴장일 캘린더 확정 리스트, (4) 공공데이터포털 API 호출 한도, (5) 애널리틱스 도구, (6) 호스팅 벤더, (7) 모니터링 도구, (8) 업종 분류 데이터 출처. 8개 항목 모두 "코드 변경 없이 확인/데이터로 해결 가능"한 구조로 설계되어 있어 재설계 리스크는 낮음.

---

## v3 재검증 (규칙 F 피드백 루프 — 4단계(UX설계)가 04-ux-design.md §7-1 Q1~Q7로 제기한 기술 스펙 공백 대응)

### 배경
4단계(UX설계) 에이전트가 화면 설계 과정에서 v2 설계서의 데이터 모델/API 명세를 화면 요구사항과 대조하다가 7가지 기술 스펙 공백(Q1~Q7)을 발견해 `04-ux-design.md` §7-1에 "확인 필요 항목"으로 문서화했다. 이는 비즈니스 판단이 아니라 설계 완성도 문제이므로, 사용자에게 되묻지 않고 아키텍트가 직접 확정했다(본 v3 개정). 아래 2회 검증은 이 v3 개정 작업 자체를 대상으로 한다.

### 1차 검증 (작성자 관점 — "Q1~Q7이 실제로 전부, 정확히 해소됐는가 + 기존 v2 원칙과 충돌하지 않는가")
- 검증자(역할): 20년차 아키텍트 (작성자 본인 관점)
- 일시: 2026-09-14
- 체크리스트
  - [x] Q1(market 의미 중복)이 실제로 해소됐는가 — §3-1-1 신설로 상장시장(KOSPI/KOSDAQ) vs 거래소 세션(KRX/NXT) 표를 만들고, §3-2 전 엔티티(`stock_master`, `raw_ohlcv`, `market_calendar`, `derived_metrics_daily`, `market_summary_daily`)와 §3-3, §3-4, §4-2 전 엔드포인트에 어느 축인지 주석을 달아 재확인. 04-ux-design.md의 가정 1/2/3과 배치되지 않는지 교차 확인 — 가정 1(상장시장), 가정 2(거래소 세션은 배지에서만 세션명으로 표기)와 정확히 일치함을 확인
  - [x] Q2(matched_metrics의 sort_by 포함 여부)가 해소됐는가 — §4-2 `GET /screen` 행에 "필터 조건 ∪ sort_by" 규칙 명시, §4-3 화이트리스트 원칙과 충돌 없음(추가되는 지표도 여전히 가공된 값만 노출되므로 §4-3 기획서 원칙 위반 아님)을 재확인
  - [x] Q3(시장 필터 부재)이 해소됐는가 — `GET /screen`에 `market` 파라미터 추가, `derived_metrics_daily.market`(이미 존재하는 컬럼) 활용이라 추가 조인/신규 컬럼 없이 구현 가능함을 재확인
  - [x] Q4(방향성 미정의)가 해소됐는가 — "값이 작을수록 상위"로 확정하고 worked example(125위/2500종목→5.0) 포함. 04-ux-design.md §2-4의 "등락률 순위: 상위 {값}%" 잠정 카피와 부호가 일치하는지 재확인 — 일치함(값이 작을수록 상위이므로 "상위 5%" 표기가 그대로 유효)
  - [x] Q5(PER/PBR/시가총액 가공 방식 미확정)가 해소됐는가 — `*_raw`(비노출)/`*_percentile`(노출) 이원 컬럼 구조 확정. 이 결정이 `GET /screen`의 `per_max`/`pbr_max`/`market_cap_min/max`(원시 스케일 필터, 04-ux-design.md §2-2에서 이미 "PER(배) 이하"로 화면 설계됨)와 §4-3(기획서) "가공 없이 노출 금지" 원칙을 동시에 만족시키는지 재확인 — 필터링은 `*_raw`(DB 내부, API 비노출), 응답 노출은 `*_percentile`만으로 분리해 두 요구가 상충하지 않음을 확인
  - [x] Q6(통화 단위 미명시)이 해소됐는가 — KRW 확정, `_krw` 접미사 필드명 반영(`total_trading_value_krw`, `trading_value_krw`, `market_cap_raw_krw`). `market_cap_min`/`market_cap_max` 요청 파라미터 단위와 04-ux-design.md의 "억원" 입력 UI 간 변환 책임을 프론트엔드로 명시했는지 재확인 — §4-1에 명시됨
  - [x] Q7(market-summary 생략 시 동작 미정의)이 해소됐는가 — 기본값 `ALL` + 서버 직접 집계(`market_summary_daily`에 `ALL` 행 신설) 확정. 04-ux-design.md의 기존 "코스피/코스닥 각각 호출" 방식과 하위 호환되는지 재확인 — `market=KOSPI`/`market=KOSDAQ` 개별 호출이 계속 지원되므로 하위 호환 확인, 4단계 문서 자체는 차기 개정 대상으로 §8-2 항목9에 이관
  - [x] 이번 개정이 §1-1의 3중 방어(스키마/DB권한/프로세스 분리) 원칙을 훼손하지 않는가 — `*_raw` 컬럼이 `public_serving`에 새로 생기는 것이 1차 방어(원본 컬럼 부재)와 모순되지 않는지 재검토한 결과, PER/PBR/시가총액 원시값은 §4-3(기획서)이 금지하는 "원본 시세(OHLCV)"가 아니라 "재무비율/파생값"이므로 애초에 1차 방어의 적용 대상이 아니며, 대신 §4-3(설계서) 화이트리스트 직렬화(2차/5차 안전장치)로 별도 방어함을 명확히 함(§1-1에 v3 참고 문단 추가로 이 구분을 명시)
  - [x] REQ-002/003/004 `traceability.md` 매핑과 `decisions.md` DEC-011~016이 실제로 갱신됐는가 — 확인 완료
- 발견된 결함 목록: 없음(1차 검증에서는 Q1~Q7 전부 반영 확인, 신규 결함 미발견)

### 2차 검증 (독립 심사자 관점 — "이 v3 설계서로 실제 구현할 개발자")
- 검증자(역할): 5단계 단위 개발자(05-unit-developer) 관점 — v3로 갱신된 설계서만 보고 스크리닝/시장요약 기능을 구현해야 하는 입장에서, v3가 새로 도입한 개념(이원 컬럼, ALL 행, 방향성 규칙)에 구현 시 바로 막힐 애매함이 남아있는지 의심하며 재검토
- 일시: 2026-09-14
- 체크리스트
  - [x] Q1~Q7 반영 사항이 실제로 §3-1-1/§3-2/§4-2에 정확히 존재하는가 — 7건 모두 재확인 완료
  - [ ] → [발견] `GET /screen`의 `sort_by=per`/`pbr`/`market_cap`이 정렬 시 원시값(`*_raw`) 기준인지 percentile 기준인지, 그리고 `sort_dir`의 방향이 "원시값 크기 기준"인지 "percentile(상위/하위) 기준"인지 v3 초안에 명시되지 않아 구현자가 임의로 정해야 하는 상태였음(PER처럼 "작을수록 상위"인 지표는 두 기준이 반대 방향이라 혼동 위험이 큼)
  - [ ] → [발견] `return_rank_pct`/`per_percentile` 등 percentile 값이 "일 단위로 코스피+코스닥 통합 전체를 대상으로 1회 계산"되는 값인지, 아니면 `GET /screen?market=KOSPI` 같은 필터링된 조회 시점에 그 부분집합 기준으로 재계산되는지 v3 초안이 명시하지 않아, "코스피만 필터링했는데 상위 %가 이상하게 나온다"는 버그 리포트로 이어질 수 있는 잠재적 혼동 지점이었음
  - [x] `*_raw`/`*_percentile` 이원 컬럼 구조가 실제로 §4-3 응답 화이트리스트와 맞물려 원시값 노출을 물리적으로 차단하는가 — Pydantic 모델 미선언 + CI 린트 이중 강제까지 구체적으로 명시되어 확인됨
  - [x] `market_summary_daily.market='ALL'` 행이 실제로 "합산이 아니라 원본에서 직접 집계"라는 설계 의도가 배치 구현자에게 애매함 없이 전달되는가 — §3-2에 이유(업종 상위 리스트 병합 손실 방지)까지 명시되어 있어 구현자가 "그냥 두 행을 더하면 되겠지"로 오해할 여지가 낮음
  - [x] PER≤0(적자기업) 등 `*_raw`가 `null`인 경우 percentile 계산에서 어떻게 처리되는지 애매하지 않은가 — "raw가 null이면 percentile도 null"로 명시, 결측치 처리 원칙과 일관됨을 확인
  - [x] `decisions.md` DEC-011~016의 비가역성 평가가 타당한가 — 미구현 상태(traceability.md 전 unit "Not Started")에서의 확정이라 재작업 비용이 낮다는 점이 각 항목에 정확히 반영되어 있음을 재확인
- 발견된 결함 목록:
  1. `sort_by=per`/`pbr`/`market_cap`의 정렬 기준(원시값 vs percentile)과 `sort_dir` 방향 해석이 명시되지 않음
  2. percentile 계산이 "일 단위 전체 종목 기준 고정값"인지 "조회 시점 필터링 기준 재계산값"인지 불명확함
- 조치 내용: (수정 후 → v3, 최종)
  1. §4-2 `GET /market-summary` 설명 문단 뒤에 "v3 신규 — `sort_by=per`/`pbr`/`market_cap`의 실제 정렬 기준 명확화" 문단을 추가: 정렬은 항상 `*_raw` 컬럼 기준(인덱스 활용, §5-1과 정합), `sort_dir`은 percentile 방향이 아니라 원시값의 산술적 크기 방향을 따름을 명시, 동률 처리 시 `stock_code` 오름차순을 2차 정렬 키로 고정해 페이지네이션 결정성 확보
  2. §3-2 `derived_metrics_daily` 결측치 처리 문단 뒤에 "percentile 계산 범위 확정" 문단을 추가: 모든 percentile은 Derivation Batch가 거래일 1회, 코스피+코스닥 통합 전체 종목 기준으로 계산·저장하는 고정값이며 `market` 필터로 조회해도 재계산되지 않음을 명시. 시장별 재계산 percentile은 현재 요구사항에 없으므로 만들지 않음(YAGNI)을 명문화

## v3 최종 판정
- [x] PASS (v3 기준 결함 0건, 최소 2회 검증 완료 — 1차 결함 0건, 2차 결함 2건 전부 조치)
- 04단계 피드백 커버리지 확인: `04-ux-design.md` §7-1 Q1~Q7 = 7개 항목 전부 v3 설계서에 확정 반영 완료(100%). §8-3(v3 신설)에 Q번호별 확정 결과 요약표로 재확인 가능.
- 기존 기능 커버리지 재확인: v3는 Q1~Q7과 그로부터 파생된 2건의 국소 보완(정렬 기준/percentile 계산 범위)만 다루며, REQ-001~013/022의 기존 매핑 범위를 축소하거나 제거하지 않았음(§3, §4 전체 재대조 완료) — 기존 100% 커버리지 유지.
- `traceability.md`(REQ-002/003/004 설계 매핑), `decisions.md`(DEC-011~016) 동시 갱신 완료.
- 잔여 비차단 미해결 사항(§8-2, PASS를 막지 않음): 기존 1~8번 항목 유지 + v3 신규 9번(04-ux-design.md의 market-summary 호출 방식을 단일 호출로 갱신 권고, 국소적 카피 변경), 10번(matched_metrics의 PER/PBR/시가총액이 백분위로 표시됨에 따른 04-ux-design.md 카피 갱신 권고, 국소적). 두 항목 모두 4단계 문서의 차기 개정 시 반영하면 되는 수준이라 5단계(개발) 착수를 막지 않음.

---

## v4 재검증 (규칙 F 피드백 루프 — 6단계(단위테스트)가 `unit-01-test.md` DEF-001로 제기한 §3-3 근본 결함 대응)

### 배경
6단계(06-unit-tester)가 UNIT-01(`get_last_trading_day()` 구현체)을 검증하는 과정에서, v3 설계서 §3-3의 의사코드 자체가 주석("오늘 마감 전 데이터는 존재할 수 없으므로 오늘을 후보에서 제외")과 실제 조건식(개장 시각 기준 비교)에서 내적으로 모순됨을 발견했다(DEF-001, High). 5단계는 조건식을 문자 그대로(리터럴) 구현했을 뿐이므로 근본 원인은 설계서에 있고, `decisions.md` DEC-017이 재작업을 3단계로 위임했다. 이번 v4는 이 재작업을 다룬다.

### 1차 검증 (작성자 관점 — "DEF-001이 실제로 해소됐는가 + 사용자 요청 5개 항목이 전부 반영됐는가 + 기존 설계와 새 모순을 만들지 않는가")
- 검증자(역할): 20년차 아키텍트 (작성자 본인 관점)
- 일시: 2026-09-14
- 체크리스트
  - [x] 조건식이 "마감 시각" 기준으로 수정되고, "개장 시각" 개념이 완전히 제거됐는가 — §3-3 pseudocode에서 `market_open_time` 참조가 사라지고 `row.session_close_at` 기반 `_already_closed()`로 대체됐음을 재확인. 오늘이 거래일이고 마감 전이면 전일, 마감(포함) 이후면 당일을 반환하는 요청사항 1과 정확히 일치
  - [x] 마감 기준 시각이 REQ-006 다원화(15:30/18:00/20:00)와 명확히 정합되게 선택됐는가 — KRX=정규장 15:30, NXT=애프터마켓 20:00 채택, 시간외단일가(18:00) 미채택 근거(§3-1-1 2값 체계 유지, REQ-011 데이터 커버리지 미확인)가 §3-3 본문과 `decisions.md` DEC-018에 명시됨을 확인
  - [x] §3-4 `data_freshness.session_close_at`(REQ-006 화면 표기용)과 §3-3의 판단 기준이 동일 컬럼(`reference.market_calendar.session_close_at`)을 재사용해 소스가 갈라지지 않는가 — §3-2 컬럼 설명에도 상호 참조 각주를 추가해 재확인
  - [x] 배치 신선도 알림 cron 로직(사용자 요청상 "§5-4"로 지칭됐으나 실제 위치는 §7-2)이 이번 수정과 정합되게 재검토됐는가 — §7-2에 "v4 정합성 확인" 및 "운영 스케줄링 제약"(cron을 개장 전으로 고정) 문단을 추가하고, 오표기(§5-4→§7-2)를 §0-2에서 명시적으로 바로잡았음을 확인
  - [x] 실행 가능한 pseudocode와 경계 시각별 진리표가 §3-3에 추가돼 재발 방지 장치가 되는가 — `_already_closed()` 헬퍼 분리로 "과거 날짜는 무조건 마감" / "오늘만 실제 비교"가 코드 구조로 명확히 나뉘었고, KRX/NXT 각 마감 전후·마감 정각·휴장일·캘린더 공백까지 총 13행의 진리표로 커버됨을 확인
  - [x] `decisions.md` DEC-018이 DEC-017이 위임한 두 갈래 판단((a) 마감시각 기준 vs (b) 주석 정정)을 실제로 결론짓고 근거를 남겼는가 — §7-2 cron 전제와의 정합성을 근거로 (a)를 선택했음을 확인, DEC-017/DEC-011과 상충하지 않음도 재확인
  - [x] 이번 개정이 §1-1(3중 방어)·§3-1-1(market 두 축 표준화)·§4(API 계약) 등 기존 v3 확정 사항을 훼손하지 않는가 — §3-3/§3-2/§7-2/§9 외 다른 절은 손대지 않았고, `market` 파라미터가 거래소 세션 구분이라는 정의도 그대로 유지됨을 재확인
- 발견된 결함 목록: 없음(1차 검증에서는 요청 5개 항목 전부 반영 확인, 신규 결함 미발견)

### 2차 검증 (독립 심사자 관점 — "이 v4 pseudocode를 그대로 리터럴로 옮겨 재구현할 5단계 개발자" 관점, DEF-001 재발 방지가 실제로 되는지 집중 검토)
- 검증자(역할): 5단계 단위 개발자(05-unit-developer) 관점 — v4 pseudocode만 보고 `last_trading_day.py`/`market_hours.py`를 다시 구현해야 하는 입장에서, v3와 동일한 방식(문자 그대로 옮기기)으로 옮겨도 이번에는 안전한지 의심하며 재검토
- 일시: 2026-09-14
- 체크리스트
  - [x] pseudocode에 등장하는 모든 심볼(`row.is_trading_day`, `row.session_close_at`)이 §3-2 기존 스키마에 실재하는가, 신규 컬럼이 필요한가 — `reference.market_calendar`에 이미 존재하는 두 컬럼만 사용하므로 마이그레이션 불필요함을 확인
  - [x] "오늘"과 "과거"의 분기 로직이 `_already_closed()` 내부에서 모호함 없이 분리되어 있는가 — `candidate < as_of.date()`로 명확히 분리, 조건 순서도 위에서 아래로 그대로 코드화 가능
  - [ ] → [발견] `row.is_trading_day and _already_closed(candidate, row, as_of)`에서, 휴장일 행은 `calendar_file.py` 구현 관례상 `session_close_at`이 `None`으로 저장될 수 있는데(§3-2), 5단계가 가독성을 이유로 두 불리언 값을 미리 각각 변수에 담아 계산한 뒤 `and`로 묶는 방식으로 "번역"하면 `_already_closed()`가 `is_trading_day=False`인 행에 대해서도 호출되어 `None`과 시각을 비교하다 예외가 발생할 수 있음 — pseudocode 자체는 안전(단락 평가)하지만 이 전제가 문서에 명시되어 있지 않아 v3와 유사한 "설계자 의도가 코드로 안전하게 전달되지 않는" 위험이 잠재함
  - [x] 진리표의 마감 정각(`>=`) 처리 관례가 §3-4 `data_freshness`의 "마감 완료" 표기 시점과 실제로 일치하는지, 사용자에게 노출되는 시각 표기와 내부 계산 경계가 어긋나지 않는지 — 일치함을 재확인(둘 다 마감 시각을 "이미 마감됨"에 포함)
  - [x] KRX/NXT 두 세션을 별도로 호출해야 하는 API(`GET /calendar/last-trading-day?market=`)가 이 변경으로 인터페이스(파라미터/반환 타입)를 바꾸지 않는가 — 시그니처 불변, 내부 계산 로직만 교체되므로 §4-2 API 계약에 영향 없음을 확인
  - [x] `decisions.md` DEC-018의 비가역성(Medium) 평가가 타당한가, DEC-017과 일관되는가 — 동일 근거(UNIT-01만 영향, 아직 하드코딩된 `data_freshness`라 실사용 없음)를 승계했음을 재확인
- 발견된 결함 목록:
  1. 단락 평가(short-circuit) 순서 보존 필요성이 pseudocode 주석에만 암시되어 있고 "구현 시 필수 주의사항"으로 명문화되어 있지 않아, 5단계가 리팩터링 과정에서 실수로 깨뜨릴 위험이 있었음
- 조치 내용: (수정 후 → v4, 최종)
  1. §3-3에 "구현 시 필수 주의사항(5단계 재구현 시 반드시 지킬 것)" 항목 2번을 신설해, `is_trading_day and _already_closed(...)`의 단락 평가 순서를 반드시 보존해야 하는 이유(휴장일 행의 `session_close_at=None`)와 흔히 저지르기 쉬운 실수 패턴(두 값을 미리 변수로 분리 계산)을 구체적으로 명시하고, 코드 리뷰 체크리스트 항목으로 등록하도록 문서화함

## v4 최종 판정
- [x] PASS (v4 기준 결함 0건 남음, 최소 2회 검증 완료 — 1차 결함 0건, 2차 결함 1건 즉시 조치)
- DEF-001 해소 확인: `unit-01-test.md`가 재현한 8개 경계 시각(TC-008) 중 09:00:00~15:29:59 구간(장중, 마감 전) 전부가 v4 로직에서는 "전일"을 반환함을 §3-3 진리표(#1~#4)로 재확인 — v3 리터럴 구현이 이 구간에서 "당일"을 반환하던 결함이 근본 원인(pseudocode) 수준에서 제거됨.
- 사용자 요청 5개 항목 커버리지: (1) 마감 시각 기준으로 조건식 수정 — §3-3, (2) 마감 시각 확정(KRX 정규장 15:30/NXT 애프터마켓 20:00, 시간외단일가 18:00 미채택 근거 포함) — §3-3, `decisions.md` DEC-018, (3) 신선도 체크 cron 재검토 — §7-2(사용자 요청의 "§5-4" 표기를 실제 위치인 §7-2로 정정), (4) 실행 가능한 pseudocode+진리표 — §3-3, (5) `decisions.md`/`verify-log` 갱신 — 본 문서 및 DEC-018. 5개 항목 모두 100% 반영.
- 기존 기능 커버리지 재확인: v4는 §3-2(1개 컬럼 설명 각주 추가)/§3-3(전면 재작성)/§7-2(1개 항목 갱신)/§9(변경이력)만 수정했으며, REQ-001~004/006~013/022의 기존 매핑·API 계약·데이터 모델을 축소하거나 변경하지 않았다(§3, §4 전체 재대조 완료) — 기존 100% 커버리지 유지.
- `traceability.md`(REQ-005 비고 갱신), `decisions.md`(DEC-018 추가) 동시 갱신 완료.
- 후속 조치(이 문서 범위 밖, 명시적 이관): (1) 6단계는 이 v4를 입력으로 UNIT-01(`get_last_trading_day()`)을 재검증해야 하며, 특히 §3-3 진리표 13행을 회귀 테스트 케이스로 그대로 재사용할 것을 권고한다. (2) 5단계 재구현 시 `market_hours.py`의 `market_open_time` 제거와 `_already_closed()` 로직 이식, 단락 평가 순서 보존이 필요하다(§3-3 "구현 시 필수 주의사항" 참조).

## 범위 한정 재검증 (하네스 12→13단계 버전 업그레이드, DEC-021, Tier: High)
> 변경 범위는 "11단계 배포" 참조 3곳(§7-4 근방)을 "12단계 배포"로 정정한 것뿐이며, 아키텍처·데이터 모델·API 계약은 전혀 손대지 않았다. 규칙 B(High 등급, 완화 미적용)에 따라 이 델타에 한정해 최소 2회 재검증한다.

- 1차(작성자 관점, 2026-09-17): `grep -n "11단계" 03-system-design.md` 결과 0건 확인. §7-4의 REQ-022 배포 게이트 서술이 "12단계 배포 승인 체크리스트"로 정정된 것이 §489(§7 인접)의 "다른 모든 기술적 PASS 조건(8·9·10단계 PASS)" 서술과 여전히 정합적인지(8·9·10단계는 이번 업그레이드로 번호가 바뀌지 않았음) 재확인 — 정합. 결함 없음.
- 2차(독립 심사자 관점 — "이 정정만 처음 보는 09-security-auditor", 2026-09-17): §7-4가 "코드/화면 설계 없음, 배포 게이트로만 명시"라는 원래 취지를 이번 정정이 훼손하지 않았는지, 즉 정정이 REQ-022의 성격(코드 구현 대상 아님)까지 바꿔버리지 않았는지 확인 — 문자열(11→12)만 바뀌었고 문장 구조·의미는 동일함을 원문 대조로 확인. 결함 없음.
- 판정: **PASS (결함 0건, 2회 검증 완료)** — 기존 UNIT-01~06 산출물·§1~§8 실질 설계 내용은 이 정정으로 영향받지 않는다.
